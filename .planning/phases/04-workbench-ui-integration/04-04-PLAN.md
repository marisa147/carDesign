---
phase: 04-workbench-ui-integration
plan: "04"
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - "apps/web/src/components/workbench/parameter-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["UI-02", "UI-04"]
must_haves:
  truths:
    - "User can inspect structured brief parameters in a dedicated panel."
    - "User can edit supported fields and save them through the generation brief update API."
    - "Parameter edits do not auto-submit generation."
---

<objective>
Implement the structured parameter panel for inspecting and editing the current generation brief.
</objective>

<context>
@.planning/phases/04-workbench-ui-integration/04-CONTEXT.md
@.planning/phases/04-workbench-ui-integration/04-UI-SPEC.md
@apps/web/src/lib/api/generation.ts
@services/api/src/caragent_api/schemas.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04A | Tampering | Brief edits | mitigate | Save only supported update fields and validate non-empty update payloads through the API. |
| T-04-04B | Repudiation | Unsaved changes | mitigate | Show saved/dirty/pending state and make generation an explicit action after save. |
| T-04-04C | Reliability | Field parsing | mitigate | Convert comma/newline text fields deterministically and test visible payloads. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Specify parameter panel behavior</name>
  <files>apps/web/src/app/page.test.tsx</files>
  <action>Write failing tests that create a brief, inspect vehicle template/view/style/palette/text/coverage fields, edit style/palette/text, click `保存参数`, and assert `PATCH /generation/briefs/{brief_id}` is called. Assert no generation-submit route is called during save.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before `ParameterPanel` exists.
    - Tests cover empty/no-brief state.
  </acceptance_criteria>
  <done>Parameter behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement parameter panel</name>
  <files>apps/web/src/components/workbench/parameter-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx</files>
  <action>Add a dedicated panel with compact fields for current brief payload. Include template/view display or controls, style, coverage, palette, text, warnings, and canvas size metadata. Save through `updateGenerationBrief` and refresh canonical brief state.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The panel handles no-brief, pending, saved, and error states.
    - Edits are visible and persist after the mocked API returns updated data.
    - Field labels are accessible and visible.
  </acceptance_criteria>
  <done>User can inspect and edit structured parameters.</done>
</task>

</tasks>

<verification>
Run focused page tests, web lint, and typecheck.
</verification>

<success_criteria>
- UI-02 is covered by a dedicated editable parameter panel.
- Parameter saving uses generated API wrappers.
- Generation remains an explicit action.
</success_criteria>

<output>
After completion, create `.planning/phases/04-workbench-ui-integration/04-04-SUMMARY.md`.
</output>

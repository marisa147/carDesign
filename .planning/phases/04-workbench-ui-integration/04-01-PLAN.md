---
phase: 04-workbench-ui-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/web/package.json"
  - "pnpm-lock.yaml"
  - "apps/web/src/lib/api/assets.ts"
  - "apps/web/src/lib/api/assets.test.ts"
  - "apps/web/src/lib/api/workspaces.ts"
  - "apps/web/src/lib/api/workspaces.test.ts"
  - "apps/web/src/lib/workbench/query-keys.ts"
  - "apps/web/src/lib/workbench/store.ts"
  - "apps/web/src/lib/workbench/store.test.ts"
  - "apps/web/src/components/workbench/query-provider.tsx"
autonomous: true
requirements: ["UI-01", "UI-02", "UI-03", "UI-04", "UI-05", "UI-06"]
must_haves:
  truths:
    - "Workbench code has clear server-state and local-UI-state boundaries."
    - "Asset upload/list/rights and design brief listing are available through small typed wrappers."
    - "No provider secrets or backend internals are imported into browser code."
---

<objective>
Add the Phase 4 frontend data foundation: typed wrappers for missing routes, TanStack Query provider/query keys, and a local workbench UI state boundary.
</objective>

<context>
@.planning/phases/04-workbench-ui-integration/04-CONTEXT.md
@.planning/phases/04-workbench-ui-integration/04-UI-SPEC.md
@.planning/phases/04-workbench-ui-integration/04-RESEARCH.md
@apps/web/src/lib/api/workspaces.ts
@apps/web/src/lib/api/jobs.ts
@apps/web/src/lib/api/generation.ts
@packages/contracts/src/generated/client.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01A | Information Disclosure | Browser wrappers | mitigate | Browser wrappers accept only public API base URL and generated request payloads; tests reject provider-key fields. |
| T-04-01B | Tampering | Asset upload/rights | mitigate | Use generated route helpers and typed request objects; do not string-build API paths ad hoc. |
| T-04-01C | State confusion | Workbench state | mitigate | Query keys own server state; local store owns only selected tab/view/version and preview transforms. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Specify missing API wrapper behavior</name>
  <files>apps/web/src/lib/api/assets.test.ts, apps/web/src/lib/api/workspaces.test.ts</files>
  <action>Write failing tests for `listWorkspaceDesignBriefs`, `uploadWorkspaceAsset`, `listWorkspaceAssets`, `getAsset`, and `updateAssetRights`. Assert generated route URLs, HTTP methods, multipart `FormData` behavior, JSON body behavior, and safe browser payloads with no provider secrets.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- assets.test.ts workspaces.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail because wrappers are missing.
    - Tests inspect `FormData` for file and kind fields.
    - Tests assert design brief listing uses the existing `/workspaces/{id}/briefs` contract.
  </acceptance_criteria>
  <done>Wrapper behavior is specified before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement wrappers</name>
  <files>apps/web/src/lib/api/assets.ts, apps/web/src/lib/api/workspaces.ts</files>
  <action>Add small wrappers matching the existing `health`, `jobs`, `workspaces`, and `generation` wrapper style. Use generated URL helper functions and generated types from `@caragent/contracts`. Keep request helpers local to the wrapper files and return typed response bodies.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- assets.test.ts workspaces.test.ts</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Wrapper tests pass.
    - `FormData` uploads do not manually set a JSON content type.
    - Error text is route-family specific, for example `Asset request failed with status N`.
  </acceptance_criteria>
  <done>Workbench can call asset and brief-list routes from web code.</done>
</task>

<task type="tdd">
  <name>Task 3: Specify workbench query/store boundary</name>
  <files>apps/web/src/lib/workbench/store.test.ts</files>
  <action>Write tests for query key helpers and the local UI store. Assert query keys are stable and that preview zoom/pan/selected version/view updates do not include workspace messages, jobs, assets, or other server data.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before query/store modules exist.
    - Tests cover reset behavior for preview transform.
  </acceptance_criteria>
  <done>State boundary is specified.</done>
</task>

<task type="auto">
  <name>Task 4: Add query provider and local UI store</name>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/src/lib/workbench/query-keys.ts, apps/web/src/lib/workbench/store.ts, apps/web/src/components/workbench/query-provider.tsx</files>
  <action>Wire a client-side `QueryClientProvider` component for the workbench. Add a small local UI state store for selected tab/view/version and preview transform. Prefer Zustand per project convention; if dependency installation is blocked, keep the same exported hook/actions in an isolated fallback module and record the deviation in the summary.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- store.test.ts assets.test.ts workspaces.test.ts</automated>
    <automated>corepack pnpm --filter @caragent/web lint</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Query provider is client-only and creates one stable `QueryClient`.
    - Local store does not persist durable server records.
    - If `zustand` is added, lockfile/package files are updated through `corepack pnpm install`.
  </acceptance_criteria>
  <done>Frontend data foundation is ready for workbench components.</done>
</task>

</tasks>

<verification>
Run focused web tests, lint, and typecheck. If dependency installation is required, run it through `corepack pnpm install` and then re-run the focused checks.
</verification>

<success_criteria>
- Asset and brief-list wrappers exist and are tested.
- Workbench Query provider and local UI store boundary exist and are tested.
- No durable server data is stored only in local UI state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-workbench-ui-integration/04-01-SUMMARY.md`.
</output>

---
phase: 04-workbench-ui-integration
plan: "05"
type: execute
wave: 5
depends_on: ["04-01", "04-02", "04-04"]
files_modified:
  - "apps/web/src/components/workbench/asset-panel.tsx"
  - "apps/web/src/components/workbench/parameter-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["UI-03", "UI-02"]
must_haves:
  truths:
    - "User can upload and preview reference asset metadata from the workbench."
    - "User can update rights/source fields for uploaded assets."
    - "Assets with missing rights are visibly blocked from generation reference selection."
---

<objective>
Implement the workbench asset panel for upload, preview metadata, rights/source updates, and generation-use gating.
</objective>

<context>
@.planning/phases/04-workbench-ui-integration/04-CONTEXT.md
@apps/web/src/lib/api/assets.ts
@services/api/src/caragent_api/routes/assets.py
@services/api/src/caragent_api/schemas.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05A | Information Disclosure | Upload form | mitigate | Upload only selected file and kind; do not send local paths or secrets. |
| T-04-05B | Legal/IP | Reference assets | mitigate | Show rights/source state and block missing-rights assets from generation reference use. |
| T-04-05C | Reliability | Multipart upload | mitigate | Test generated multipart wrapper with `FormData` and refresh asset list after mutation. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Specify asset panel behavior</name>
  <files>apps/web/src/app/page.test.tsx</files>
  <action>Write failing tests that upload a reference asset, see it in the asset list, update source/rights fields, and verify missing-rights assets are labelled `权利信息缺失` and cannot be selected as generation references until rights are confirmed.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before `AssetPanel` exists.
    - Tests assert upload route receives multipart form data.
    - Tests assert confirmed asset ids are reflected in the parameter panel reference ids.
  </acceptance_criteria>
  <done>Asset panel behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement asset panel</name>
  <files>apps/web/src/components/workbench/asset-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx</files>
  <action>Add upload/list/rights UI. Use icon+text buttons, file input with visible label, compact metadata rows, and state chips for missing/confirmed rights. Refresh assets after upload and rights update.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Missing rights state is visible and blocks selection.
    - Confirmed rights state permits inclusion in current brief reference ids.
    - Upload errors show actionable error copy.
  </acceptance_criteria>
  <done>User can manage reference assets in the workbench.</done>
</task>

<task type="auto">
  <name>Task 3: Connect confirmed references to parameters</name>
  <files>apps/web/src/components/workbench/parameter-panel.tsx, apps/web/src/components/workbench/asset-panel.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Expose selected confirmed asset ids in the parameter panel and save them through the generation brief update route. Keep missing-rights assets listed but blocked.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Selected reference asset ids survive parameter save and mocked refetch.
    - Missing-rights assets are never sent in `reference_asset_ids`.
  </acceptance_criteria>
  <done>Asset references integrate with the structured brief.</done>
</task>

</tasks>

<verification>
Run focused page tests and typecheck.
</verification>

<success_criteria>
- UI-03 is satisfied through upload/list/rights UI.
- Asset rights/source gating is visible and enforced before generation reference selection.
- Parameter panel can include confirmed references.
</success_criteria>

<output>
After completion, create `.planning/phases/04-workbench-ui-integration/04-05-SUMMARY.md`.
</output>

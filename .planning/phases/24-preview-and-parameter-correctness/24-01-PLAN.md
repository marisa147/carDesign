# Phase 24 Plan: Preview And Parameter Correctness

## Scope

1. Verify whether 3D capture already uses the actual Three.js canvas and whether API image validation already checks magic bytes and actual dimensions.
2. If needed, wire a capture callback from the viewer canvas to the panel and remove any scaffold PNG fallback.
3. Update parameter diff logic so empty strings and empty arrays are submitted as intentional changes.
4. Ensure clearing reference assignments submits both `reference_asset_ids: []` and `reference_usage: []`.
5. Add focused frontend/API regression tests and rerun aggregate validation.

## Verification

- `corepack pnpm --filter @caragent/web test -- page.test.tsx`
- `cd services/api && uv run pytest -q tests/test_jobs.py -k preview_3d`
- `corepack pnpm validate`

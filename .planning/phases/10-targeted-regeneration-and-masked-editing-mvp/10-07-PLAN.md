---
phase: 10-targeted-regeneration-and-masked-editing-mvp
plan: "07"
type: execute
wave: 4
depends_on: ["10-02", "10-05", "10-06"]
files_modified:
  - "apps/web/src/components/workbench/preview-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/lib/workbench/store.ts"
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/lib/workbench/store.test.ts"
autonomous: true
requirements: ["V2-EDIT-05"]
must_haves:
  truths:
    - "User can compare parent and child versions for a targeted edit."
    - "Comparison shows whether the child came from recomposition or provider generation."
    - "Changed-region highlighting is metadata-backed and does not claim pixel-perfect diffing."
  artifacts:
    - "Version comparison UI in the workbench."
    - "Changed target/region highlight using edit metadata."
    - "Tests for parent/child route labels and comparison selection."
  key_links:
    - "10-CONTEXT D-17 -> comparison scope"
    - "10-VALIDATION -> 10-07 web tests"
    - "10-PATTERNS -> PreviewSpec and workbench state patterns"
---

<objective>
Add version comparison UI for targeted edits and changed-region highlighting.

Purpose: Users need to understand what changed, why it changed, and whether the change was a local recomposition or a provider-generated masked edit.
Output: A tested comparison mode that displays parent/child versions, route type, selected target, prompt delta, provider evidence when relevant, and changed-region highlight.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-CONTEXT.md
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-PATTERNS.md
@apps/web/src/components/workbench/preview-panel.tsx
@apps/web/src/components/workbench/workbench-app.tsx
@apps/web/src/lib/workbench/store.ts
@apps/web/src/app/page.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for targeted edit comparison</name>
  <read_first>
    - `apps/web/src/app/page.test.tsx`
    - `apps/web/src/lib/workbench/store.test.ts`
  </read_first>
  <files>apps/web/src/app/page.test.tsx, apps/web/src/lib/workbench/store.test.ts</files>
  <action>Add failing tests where a child version has `parent_version_id` and targeted edit metadata. Assert comparison UI shows parent and child, route label, selected target, prompt delta, provider/model when available, and changed-region highlight. Cover recomposition and provider-generated child versions.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/workbench/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before comparison UI exists.
    - Tests assert metadata-first comparison labels, not pixel-diff claims.
    - Tests cover child without parent gracefully.
  </acceptance_criteria>
  <done>Comparison UI behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add comparison state and parent/child lookup helpers</name>
  <read_first>
    - `apps/web/src/lib/workbench/store.ts`
    - `apps/web/src/components/workbench/preview-panel.tsx`
  </read_first>
  <files>apps/web/src/lib/workbench/store.ts, apps/web/src/lib/workbench/store.test.ts, apps/web/src/components/workbench/preview-panel.tsx</files>
  <action>Add comparison mode state, selected comparison child id, and helper logic to find the parent version/artifact from loaded versions/artifacts. Clear invalid comparison state when versions change.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/lib/workbench/store.test.ts src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Comparison state is deterministic and reset-safe.
    - Missing parent/artifact shows a graceful fallback.
    - Existing selected version behavior remains stable.
  </acceptance_criteria>
  <done>Workbench can enter and maintain targeted edit comparison state.</done>
</task>

<task type="auto">
  <name>Task 3: Render parent/child comparison and changed-region highlight</name>
  <read_first>
    - `apps/web/src/components/workbench/preview-panel.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/preview-panel.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Render a compact comparison view inside the preview area. Show parent/child metadata, route type, selected target, prompt delta, provider/model/cost when available, and a changed-region overlay from edit region/mask metadata. Keep visual layout stable on desktop and mobile.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Recomposition-only and provider-generated labels are visible.
    - Changed-region highlight uses stored metadata.
    - UI copy avoids production-ready or pixel-perfect claims.
  </acceptance_criteria>
  <done>Users can compare targeted edit parent and child versions.</done>
</task>

<task type="auto">
  <name>Task 4: Connect comparison affordances to version history</name>
  <read_first>
    - `apps/web/src/components/workbench/preview-panel.tsx`
    - `apps/web/src/components/workbench/workbench-app.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/preview-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Add clear affordances to compare a targeted child version with its parent from version history. Keep the normal version selection path intact and ensure keyboard/accessibility labels are present.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Child versions with edit metadata expose comparison controls.
    - Non-targeted versions do not show confusing edit comparison controls.
    - Existing history selection remains usable.
  </acceptance_criteria>
  <done>Version history can launch targeted edit comparison.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Version metadata to comparison UI | Stored metadata may be missing or malformed and must not crash the workbench. |
| Comparison labels to user expectations | UI must not imply pixel-perfect diff or production validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Spoofing | route label | mitigate | Derive route label from durable job/model/version metadata, not transient UI state. |
| T-10-20 | Information Disclosure | provider evidence | mitigate | Show provider/model/cost summaries only from sanitized public fields. |
| T-10-21 | Repudiation | comparison evidence | mitigate | Display parent id, child id, target, prompt delta, and route source. |
</threat_model>

<verification>
Run focused web workbench and store tests.
</verification>

<success_criteria>
- V2-EDIT-05 is visible in the workbench.
- Users can tell recomposition-only from provider-generated child versions.
- Changed-region highlight is clear, metadata-backed, and scoped as concept preview.
</success_criteria>

<output>
After completion, create `.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-07-SUMMARY.md`.
</output>

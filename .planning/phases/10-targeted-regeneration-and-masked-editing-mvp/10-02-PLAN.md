---
phase: 10-targeted-regeneration-and-masked-editing-mvp
plan: "02"
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - "apps/web/src/lib/workbench/store.ts"
  - "apps/web/src/lib/workbench/store.test.ts"
  - "apps/web/src/components/workbench/preview-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/components/workbench/parameter-panel.tsx"
  - "apps/web/src/lib/api/iteration.ts"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["V2-EDIT-01", "V2-EDIT-02"]
must_haves:
  truths:
    - "User can select a PreviewSpec safe zone or overlay layer for a targeted edit."
    - "Mask preview is visible before submission."
    - "Targeted edit submission uses generated contract types from 10-01."
  artifacts:
    - "Workbench selection state and tests."
    - "Preview target controls and mask highlight."
    - "Targeted edit form/payload builder wired into child iteration submit."
  key_links:
    - "10-CONTEXT D-06/D-09 -> UI selection model"
    - "10-PATTERNS -> web workbench patterns"
    - "10-VALIDATION -> 10-02 focused workbench tests"
---

<objective>
Add the workbench region/layer selection model and mask preview controls.

Purpose: Users need an explicit, inspectable target before asking for a localized edit. The first MVP target model should reuse PreviewSpec safe zones and overlay layers and submit typed edit metadata through the existing child iteration path.
Output: A tested workbench UI flow for selecting a target, previewing the mask/changed region, choosing edit route intent, and submitting targeted edit metadata.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-CONTEXT.md
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-PATTERNS.md
@apps/web/src/lib/workbench/store.ts
@apps/web/src/components/workbench/preview-panel.tsx
@apps/web/src/components/workbench/workbench-app.tsx
@apps/web/src/lib/api/iteration.ts
@apps/web/src/app/page.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for selecting edit targets and previewing masks</name>
  <read_first>
    - `apps/web/src/lib/workbench/store.test.ts`
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>apps/web/src/lib/workbench/store.test.ts, apps/web/src/app/page.test.tsx</files>
  <action>Add failing tests that select a safe zone and an overlay layer from a version PreviewSpec, toggle mask preview, enter a prompt delta, and verify the submitted iteration payload contains the selected target/region/edit intent.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/workbench/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because target state and controls do not exist.
    - Tests assert non-targeted iteration still works.
    - Tests cover selected version changes resetting or validating stale target state.
  </acceptance_criteria>
  <done>Workbench targeted edit UX is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend workbench UI state for targeted editing</name>
  <read_first>
    - `apps/web/src/lib/workbench/store.ts`
  </read_first>
  <files>apps/web/src/lib/workbench/store.ts, apps/web/src/lib/workbench/store.test.ts</files>
  <action>Add store state/actions for edit mode, selected edit target, selected route preference, mask preview visibility, prompt delta draft, and comparison focus. Ensure reset behavior clears edit state when the workbench or selected version changes.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/lib/workbench/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Store actions are small and deterministic.
    - Invalid/stale targets can be cleared without throwing.
    - Existing preview zoom/view state remains unchanged.
  </acceptance_criteria>
  <done>Workbench state can represent a targeted edit draft.</done>
</task>

<task type="auto">
  <name>Task 3: Add selectable PreviewSpec regions/layers and mask overlay</name>
  <read_first>
    - `apps/web/src/components/workbench/preview-panel.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/preview-panel.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Make safe zones and overlay layers selectable in edit mode, show selected target state, render a stable mask/region highlight, and provide accessible labels for target controls. Keep dimensions stable and avoid layout shift around the existing preview canvas.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - User can identify and select safe-zone and overlay-layer targets.
    - Mask preview does not obscure all preview information.
    - Existing overlay/safe-zone toggles still work.
  </acceptance_criteria>
  <done>Preview panel supports target selection and visible mask preview.</done>
</task>

<task type="auto">
  <name>Task 4: Wire targeted edit submission into child iteration flow</name>
  <read_first>
    - `apps/web/src/components/workbench/workbench-app.tsx`
    - `apps/web/src/lib/api/iteration.ts`
    - `apps/web/src/components/workbench/parameter-panel.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/lib/api/iteration.ts, apps/web/src/app/page.test.tsx</files>
  <action>Build a typed edit intent payload from selected target, prompt delta, route preference, provider selection, and mask preview metadata. Submit through `submitChildIteration` while preserving the existing non-targeted change request path.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/api/iteration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Targeted edit payload matches generated contract types.
    - Disabled or incomplete target state blocks targeted submit with a clear local message.
    - Local deterministic route remains available without hosted credentials.
  </acceptance_criteria>
  <done>Workbench can submit a typed targeted edit request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PreviewSpec data to UI controls | Version metadata may be incomplete or old and must not break selection. |
| UI draft to API payload | User-edited prompt delta and target fields must match contract shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | selected target | mitigate | Only allow targets present in the selected version PreviewSpec and clear stale selection on version changes. |
| T-10-05 | Denial of Service | preview canvas | mitigate | Keep masks as lightweight normalized metadata in browser state and avoid unbounded DOM/image work. |
| T-10-06 | Information Disclosure | provider diagnostics | mitigate | Reuse Phase 9 sanitized provider status helpers and never render raw provider payloads in edit controls. |
</threat_model>

<verification>
Run focused web store and workbench tests.
</verification>

<success_criteria>
- V2-EDIT-01 has a visible target selection and mask preview path.
- V2-EDIT-02 edit metadata can be submitted from the workbench.
- Existing generation, provider selector, and non-targeted iteration flows still work.
</success_criteria>

<output>
After completion, create `.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-02-SUMMARY.md`.
</output>

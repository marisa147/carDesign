---
phase: 10-targeted-regeneration-and-masked-editing-mvp
plan: "03"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - "services/worker/src/caragent_worker/recomposition.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/src/caragent_worker/providers/local.py"
  - "services/core/src/caragent_core/editing.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/worker/tests/test_image_providers.py"
autonomous: true
requirements: ["V2-EDIT-03", "V2-EDIT-02"]
must_haves:
  truths:
    - "Layer transform/text/logo edits can complete without hosted provider calls."
    - "Recomposition creates a child version/artifact and preserves parent data."
    - "The route is traceable as deterministic recomposition."
  artifacts:
    - "Recomposition helper with focused tests."
    - "Worker route that chooses recomposition for safe edit intents."
    - "Model-run/job/version metadata showing recomposition route."
  key_links:
    - "10-CONTEXT D-10 -> deterministic recomposition"
    - "10-RESEARCH -> recomposition route"
    - "10-VALIDATION -> 10-03 worker tests"
---

<objective>
Implement the deterministic layer recomposition path for safe targeted edits.

Purpose: Phase 10 should avoid unnecessary full regeneration when a requested change only moves, scales, hides, changes opacity, edits text, or swaps a logo layer.
Output: A tested worker path that applies allowed PreviewSpec layer changes, writes a child artifact/version, and records a recomposition-only route without calling hosted providers.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-CONTEXT.md
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-RESEARCH.md
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-PATTERNS.md
@services/worker/src/caragent_worker/tasks/jobs.py
@services/worker/src/caragent_worker/providers/local.py
@services/core/src/caragent_core/services/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for recomposition-safe edit intents</name>
  <read_first>
    - `services/worker/tests/test_generation_tasks.py`
    - `services/worker/tests/test_image_providers.py`
  </read_first>
  <files>services/worker/tests/test_generation_tasks.py, services/worker/tests/test_image_providers.py</files>
  <action>Add failing tests for overlay layer move, scale, opacity, visibility, text edit, and logo swap. Assert no hosted provider is selected, no quota guard is required, child version has parent lineage, and route metadata is `deterministic_recomposition`.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because recomposition routing/helper does not exist.
    - Tests assert parent version parameters are unchanged.
    - Tests assert invalid recomposition fields are rejected or routed to provider-mask path later.
  </acceptance_criteria>
  <done>Recomposition behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PreviewSpec recomposition helper</name>
  <read_first>
    - `services/worker/src/caragent_worker/providers/local.py`
    - `services/core/src/caragent_core/editing.py`
  </read_first>
  <files>services/worker/src/caragent_worker/recomposition.py, services/worker/src/caragent_worker/providers/local.py, services/core/src/caragent_core/editing.py</files>
  <action>Create a helper that validates an edit intent against the parent PreviewSpec, applies allowed layer/safe-zone changes, produces updated PreviewSpec metadata, and renders or reuses local preview image bytes consistently with the existing local provider style.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Helper accepts only allowed recomposition changes.
    - Output keeps canvas/template/safe-zone context.
    - Generated image metadata includes target id, changed fields, and recomposition route.
  </acceptance_criteria>
  <done>Worker has a reusable deterministic recomposition helper.</done>
</task>

<task type="auto">
  <name>Task 3: Route recomposition jobs inside the worker pipeline</name>
  <read_first>
    - `services/worker/src/caragent_worker/tasks/jobs.py`
    - `services/core/src/caragent_core/services/jobs.py`
  </read_first>
  <files>services/worker/src/caragent_worker/tasks/jobs.py, services/core/src/caragent_core/services/jobs.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Before provider execution, detect edit intents that are recomposition-safe. Create a model-run or equivalent trace record, write the recomposed artifact, create a child version with parent lineage and updated PreviewSpec parameters, append durable job events, and mark the job succeeded.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - No external provider is selected for recomposition route.
    - Job events and model-run/version parameters include route and target evidence.
    - Existing full generation and hosted provider flows still pass.
  </acceptance_criteria>
  <done>Safe targeted edits can complete through deterministic recomposition.</done>
</task>

<task type="auto">
  <name>Task 4: Harden recomposition validation and parent immutability</name>
  <read_first>
    - `services/worker/tests/test_generation_tasks.py`
  </read_first>
  <files>services/worker/tests/test_generation_tasks.py, services/worker/src/caragent_worker/tasks/jobs.py</files>
  <action>Add regression coverage for invalid target id, stale parent version, missing parent artifact, unsafe provider-required changes, and cancellation before/after recomposition. Ensure failures do not create partial child versions or mutate parent data.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Invalid recomposition requests fail with user-safe categories.
    - Cancellation remains durable.
    - Parent version/artifact records are unchanged after failure.
  </acceptance_criteria>
  <done>Recomposition path is rollback-safe for invalid and canceled jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit intent to worker recomposition | User-controlled changes must not mutate arbitrary metadata or files. |
| Parent artifact to child artifact | Parent output is immutable and recomposition creates a new artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | PreviewSpec layer metadata | mitigate | Whitelist recomposition fields and validate target ids against parent PreviewSpec. |
| T-10-08 | Repudiation | route trace | mitigate | Persist route, target, changed fields, and parent version id in model-run/version/job metadata. |
| T-10-09 | Denial of Service | image rendering | mitigate | Bound canvas/mask dimensions and reuse local provider image constraints. |
</threat_model>

<verification>
Run focused worker generation and image provider tests.
</verification>

<success_criteria>
- V2-EDIT-03 is implemented for safe layer changes.
- Recomposition does not require hosted credentials or quota.
- Child lineage and route evidence are durable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-03-SUMMARY.md`.
</output>

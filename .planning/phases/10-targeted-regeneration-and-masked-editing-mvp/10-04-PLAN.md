---
phase: 10-targeted-regeneration-and-masked-editing-mvp
plan: "04"
type: execute
wave: 2
depends_on: ["10-01", "09-01", "09-02", "09-03"]
files_modified:
  - "services/core/src/caragent_core/provider_capabilities.py"
  - "services/worker/src/caragent_worker/providers/base.py"
  - "services/worker/src/caragent_worker/providers/bfl.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/generation.py"
  - "services/api/src/caragent_api/routes/operations.py"
  - "services/api/tests/test_generation.py"
  - "services/api/tests/test_operations.py"
  - "services/worker/tests/test_config.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/worker/tests/test_image_providers.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
autonomous: true
requirements: ["V2-EDIT-04", "V2-EDIT-02"]
must_haves:
  truths:
    - "Mask-aware provider calls are capability-gated."
    - "Worker repeats authoritative hosted and mask preflight before execution."
    - "Default tests do not make real hosted provider calls."
  artifacts:
    - "Provider capability map extended with mask/edit support fields."
    - "Normalized request contract for optional mask/edit metadata."
    - "API/worker tests for unsupported provider, missing mask, and allowed mock provider path."
  key_links:
    - "10-CONTEXT D-11/D-14 -> provider mask route"
    - "09-CONTEXT D-04/D-12 -> hosted gates and capability flags"
    - "10-VALIDATION -> 10-04 capability checks"
---

<objective>
Define the mask-aware generation request contract and provider capability checks.

Purpose: Visual-content edits may need a provider call, but Phase 10 must only route masked generation when the selected provider/model explicitly supports it and all hosted guards pass.
Output: Typed capability checks, normalized mask/edit request fields, and tests that block unsupported paths while allowing mocked provider execution.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-CONTEXT.md
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-RESEARCH.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-CONTEXT.md
@services/core/src/caragent_core/provider_capabilities.py
@services/worker/src/caragent_worker/providers/base.py
@services/worker/src/caragent_worker/providers/bfl.py
@services/worker/src/caragent_worker/tasks/jobs.py
@services/api/src/caragent_api/routes/generation.py
@services/api/src/caragent_api/routes/operations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-check provider mask support before implementation</name>
  <read_first>
    - `.planning/phases/09-hosted-provider-rollout-mvp/09-RESEARCH.md`
    - `services/worker/src/caragent_worker/providers/bfl.py`
  </read_first>
  <files>.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-RESEARCH.md</files>
  <action>Verify the currently selected hosted provider's official mask/edit API support before adding real adapter behavior. If network or credentials are unavailable, document the limitation and implement capability-gated mock/local behavior only, keeping real hosted mask calls disabled.</action>
  <verify>
    <automated>Select-String -Path .planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-RESEARCH.md -Pattern "mask","provider","capability"</automated>
  </verify>
  <acceptance_criteria>
    - Research notes state whether real hosted mask calls are implemented, mocked, or deferred.
    - No unverified provider payload shape is treated as production truth.
    - Default validation path remains free.
  </acceptance_criteria>
  <done>Provider mask support assumptions are explicit before code routing.</done>
</task>

<task type="auto">
  <name>Task 2: Extend provider capability map and operations status</name>
  <read_first>
    - `services/core/src/caragent_core/provider_capabilities.py`
    - `services/api/src/caragent_api/routes/operations.py`
    - `services/api/tests/test_operations.py`
  </read_first>
  <files>services/core/src/caragent_core/provider_capabilities.py, services/api/src/caragent_api/routes/operations.py, services/api/src/caragent_api/schemas.py, services/api/tests/test_operations.py</files>
  <action>Add safe capability fields for mask-aware generation, supported edit routes, mask input requirements, and provider caveats. Expose them through operations status without secrets. Add tests for local, BFL disabled, and BFL configured states.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - Browser-safe status tells UI whether mask provider route is supported or blocked.
    - Capability data includes user-safe caveats and blocked reasons.
    - No credential values appear in operations responses.
  </acceptance_criteria>
  <done>Provider mask capability is browser-visible and safe.</done>
</task>

<task type="auto">
  <name>Task 3: Extend normalized provider request for mask/edit metadata</name>
  <read_first>
    - `services/worker/src/caragent_worker/providers/base.py`
    - `services/worker/tests/test_image_providers.py`
  </read_first>
  <files>services/worker/src/caragent_worker/providers/base.py, services/worker/src/caragent_worker/providers/bfl.py, services/worker/tests/test_image_providers.py</files>
  <action>Add optional mask/edit metadata to `ImageGenerationRequest` or a nested field used by provider adapters. Keep provider-specific payload construction inside adapters. Add tests that mocked providers receive the normalized mask metadata and unsupported adapters fail safely.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Generic request can carry mask artifact id, region, prompt delta, and edit route.
    - BFL adapter behavior matches verified research or remains explicitly blocked.
    - Existing local/full-generation provider tests still pass.
  </acceptance_criteria>
  <done>Provider adapters have a normalized mask/edit request surface.</done>
</task>

<task type="auto">
  <name>Task 4: Add API and worker preflight for provider-mask route</name>
  <read_first>
    - `services/api/src/caragent_api/routes/generation.py`
    - `services/worker/src/caragent_worker/tasks/jobs.py`
    - `services/api/tests/test_generation.py`
    - `services/worker/tests/test_generation_tasks.py`
  </read_first>
  <files>services/api/src/caragent_api/routes/generation.py, services/worker/src/caragent_worker/tasks/jobs.py, services/api/tests/test_generation.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Block provider-mask targeted edits when the selected provider/model lacks capability, mask metadata is missing/invalid, hosted flags are disabled, credentials are missing, or quota/rate/cost guards fail. The API may block obvious failures; the worker must repeat authoritative checks.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py tests/test_operations.py</automated>
    <automated>cd services/worker && uv run pytest -q tests/test_config.py tests/test_generation_tasks.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Unsupported mask routes fail before accidental full regeneration.
    - Worker failure metadata is sanitized and classified.
    - Contract artifacts remain current.
  </acceptance_criteria>
  <done>Mask-aware provider route is capability-gated at API and worker boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit metadata to hosted provider | Mask payloads and prompt deltas may contain user data and must be capability-gated. |
| Provider adapter to durable logs | Raw provider failures must be sanitized before persistence or UI display. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Elevation of Privilege | hosted mask enablement | mitigate | Require feature flags, provider capability, credentials, and quota/cost/rate guards before calls. |
| T-10-11 | Information Disclosure | provider errors | mitigate | Reuse provider secret redaction and store only user-safe categories/messages. |
| T-10-12 | Tampering | mask payload | mitigate | Validate mask artifact ownership, dimensions, region bounds, and selected parent version. |
</threat_model>

<verification>
Run focused API operations/generation tests, worker provider/generation tests, and `corepack pnpm contracts:check`.
</verification>

<success_criteria>
- V2-EDIT-04 has an explicit provider mask request contract.
- Unsupported provider-mask paths are blocked clearly.
- Default validation does not require hosted credentials or paid calls.
</success_criteria>

<output>
After completion, create `.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-04-SUMMARY.md`.
</output>

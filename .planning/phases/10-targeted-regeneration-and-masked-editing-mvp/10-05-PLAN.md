---
phase: 10-targeted-regeneration-and-masked-editing-mvp
plan: "05"
type: execute
wave: 3
depends_on: ["10-01", "10-03", "10-04"]
files_modified:
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/src/caragent_core/models.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/api/tests/test_jobs.py"
  - "services/api/tests/test_generation.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
autonomous: true
requirements: ["V2-EDIT-02", "V2-EDIT-03", "V2-EDIT-04"]
must_haves:
  truths:
    - "Every targeted edit creates a child version with parent lineage."
    - "Artifacts, model runs, and job events record whether the route was recomposition-only or provider-generated."
    - "Parent versions and artifacts are not overwritten."
  artifacts:
    - "Worker targeted-edit pipeline with durable ledger updates."
    - "API exposure of targeted edit metadata on jobs/versions/artifacts/model-runs."
    - "Focused API and worker tests for recomposition and provider-mask branches."
  key_links:
    - "10-CONTEXT D-01/D-05 -> parent child lineage and durable metadata"
    - "10-PATTERNS -> worker and ledger patterns"
    - "10-VALIDATION -> 10-05 worker/api checks"
---

<objective>
Complete the worker targeted-edit pipeline with parent/child lineage and artifact ledger updates.

Purpose: Phase 10 needs one durable execution path that accepts targeted edit job metadata, chooses recomposition or provider-mask generation, writes artifacts/model-runs/events, and exposes child version evidence through the API.
Output: A tested end-to-end worker/API path for targeted edits across recomposition and mocked/provider-capable mask routes.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-CONTEXT.md
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-PATTERNS.md
@services/worker/src/caragent_worker/tasks/jobs.py
@services/core/src/caragent_core/services/jobs.py
@services/api/src/caragent_api/routes/jobs.py
@services/api/src/caragent_api/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red end-to-end tests for targeted edit child lineage</name>
  <read_first>
    - `services/worker/tests/test_generation_tasks.py`
    - `services/api/tests/test_jobs.py`
  </read_first>
  <files>services/worker/tests/test_generation_tasks.py, services/api/tests/test_jobs.py</files>
  <action>Add failing tests that submit a targeted edit job, execute the worker path, then assert child version parent id, artifact metadata, model-run parameters, job events, route label, target id, mask metadata, and prompt delta are all durable and API-readable.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before pipeline persistence is complete.
    - Tests cover recomposition and a mocked provider-mask capable path.
    - Tests assert parent version/artifact rows remain unchanged.
  </acceptance_criteria>
  <done>Targeted edit pipeline requirements are specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Persist targeted edit route metadata through job/model/artifact/version helpers</name>
  <read_first>
    - `services/core/src/caragent_core/services/jobs.py`
    - `services/core/src/caragent_core/models.py`
  </read_first>
  <files>services/core/src/caragent_core/services/jobs.py, services/core/src/caragent_core/models.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Extend existing persistence helpers, or add narrowly scoped helpers, so targeted edit route, target, prompt delta, mask artifact, provider details, cost, and changed region are written consistently to job events, model runs, artifacts, and child version parameters.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Helper behavior is deterministic and covered by tests.
    - Route metadata names are stable for web comparison UI.
    - Existing non-targeted generation metadata remains compatible.
  </acceptance_criteria>
  <done>Ledger helpers can persist targeted edit evidence consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Connect worker branch selection to persistence</name>
  <read_first>
    - `services/worker/src/caragent_worker/tasks/jobs.py`
    - `services/worker/src/caragent_worker/recomposition.py`
    - `services/worker/src/caragent_worker/providers/base.py`
  </read_first>
  <files>services/worker/src/caragent_worker/tasks/jobs.py, services/worker/tests/test_generation_tasks.py</files>
  <action>In the worker, parse edit intent from job metadata, choose deterministic recomposition for safe changes, choose provider-mask route for provider-required changes, and write final success/failure state through the helpers from Task 2. Preserve current cancellation and hosted fallback behavior where applicable.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Recomposition branch succeeds without provider calls.
    - Provider-mask branch uses normalized request metadata and capability checks.
    - Job status transitions and progress events remain coherent.
  </acceptance_criteria>
  <done>Worker can execute targeted edit jobs end to end.</done>
</task>

<task type="auto">
  <name>Task 4: Expose targeted edit evidence through jobs API and contracts</name>
  <read_first>
    - `services/api/src/caragent_api/routes/jobs.py`
    - `services/api/src/caragent_api/schemas.py`
    - `services/api/tests/test_jobs.py`
  </read_first>
  <files>services/api/src/caragent_api/routes/jobs.py, services/api/src/caragent_api/schemas.py, services/api/tests/test_jobs.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Ensure job/version/artifact/model-run API responses expose targeted edit metadata already stored in durable parameters/metadata. Add typed response fields only where metadata is insufficient for the web comparison UI. Regenerate/check contracts.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_generation.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Web can read route, target, prompt delta, and changed region from API responses.
    - Contracts are current.
    - Existing job/artifact/version consumers remain compatible.
  </acceptance_criteria>
  <done>Targeted edit execution evidence is API-readable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker branch selection | User intent determines recomposition vs provider route and must be validated. |
| Ledger writes | Child artifacts/versions must be complete or safely absent on failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering | parent/child lineage | mitigate | Assert parent rows are unchanged and child version uses explicit parent id. |
| T-10-14 | Repudiation | edit execution trace | mitigate | Persist route, target, prompt delta, provider/model, and changed region in durable records. |
| T-10-15 | Denial of Service | partial artifacts | mitigate | Write job failure state and avoid orphaned child versions on invalid or failed edits. |
</threat_model>

<verification>
Run focused worker generation tests, API jobs/generation tests, and `corepack pnpm contracts:check`.
</verification>

<success_criteria>
- V2-EDIT-02 is durable across job, model-run, artifact, and child version records.
- V2-EDIT-03 and V2-EDIT-04 route outcomes are distinguishable.
- API responses contain enough evidence for the comparison UI.
</success_criteria>

<output>
After completion, create `.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-05-SUMMARY.md`.
</output>

---
phase: 10-targeted-regeneration-and-masked-editing-mvp
plan: "06"
type: execute
wave: 3
depends_on: ["10-05"]
files_modified:
  - "services/core/src/caragent_core/enums.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/components/workbench/progress-panel.tsx"
  - "apps/web/src/lib/api/iteration.ts"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/api/tests/test_generation.py"
  - "services/api/tests/test_jobs.py"
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/lib/api/iteration.test.ts"
autonomous: true
requirements: ["V2-EDIT-04", "V2-EDIT-05"]
must_haves:
  truths:
    - "Edit failures are classified by actionable cause."
    - "Retry preserves the original edit intent and parent version."
    - "User-facing diagnostics remain sanitized."
  artifacts:
    - "Failure category mapping for invalid selection, invalid mask, unsupported capability, recomposition conflict, and provider-mask failures."
    - "Retry-safe API/worker behavior for targeted edit jobs."
    - "Workbench failure and retry states with tests."
  key_links:
    - "10-CONTEXT D-15/D-18 -> failures and retry"
    - "10-VALIDATION -> 10-06 failure and retry checks"
    - "10-PATTERNS -> failure category and UI diagnostics patterns"
---

<objective>
Add edit failure classification, retry eligibility, and rollback-safe UX.

Purpose: Targeted edits introduce new ways to fail. Users and operators need clear, sanitized reasons and a retry path that preserves the selected target and parent lineage without corrupting versions.
Output: Tested failure categories, retry behavior, API metadata, and workbench states for targeted edit jobs.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-CONTEXT.md
@.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-VALIDATION.md
@services/core/src/caragent_core/enums.py
@services/worker/src/caragent_worker/tasks/jobs.py
@services/api/src/caragent_api/routes/jobs.py
@apps/web/src/components/workbench/progress-panel.tsx
@apps/web/src/components/workbench/workbench-app.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for targeted edit failure categories</name>
  <read_first>
    - `services/worker/tests/test_generation_tasks.py`
    - `services/api/tests/test_jobs.py`
  </read_first>
  <files>services/worker/tests/test_generation_tasks.py, services/api/tests/test_jobs.py</files>
  <action>Add failing tests for invalid target, invalid/missing mask artifact, unsupported provider mask capability, recomposition conflict, hosted quota/rate/cost block, provider failure, timeout, cancellation, and storage failure. Assert sanitized latest_error, job events, and retry eligibility metadata.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before new categories/metadata exist.
    - Tests assert no secret-like strings are persisted.
    - Tests assert failed targeted edits do not create successful child versions.
  </acceptance_criteria>
  <done>Failure and retry behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement edit-specific failure classification and retry metadata</name>
  <read_first>
    - `services/core/src/caragent_core/enums.py`
    - `services/worker/src/caragent_worker/tasks/jobs.py`
    - `services/api/src/caragent_api/routes/jobs.py`
  </read_first>
  <files>services/core/src/caragent_core/enums.py, services/worker/src/caragent_worker/tasks/jobs.py, services/api/src/caragent_api/routes/jobs.py, services/api/src/caragent_api/schemas.py</files>
  <action>Add or map failure categories for targeted edit errors. Persist `retry_eligible`, `retry_route`, `edit_intent`, `parent_version_id`, and safe blocked reason metadata where the retry route can reuse the original intent.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_generation.py</automated>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Retry metadata is stable and API-readable.
    - Failure messages are user-safe and do not include raw provider payloads.
    - Existing non-targeted retry behavior remains compatible.
  </acceptance_criteria>
  <done>Targeted edit failures are classified and retry eligibility is durable.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve targeted edit intent during retry</name>
  <read_first>
    - `services/api/src/caragent_api/routes/jobs.py`
    - `services/worker/src/caragent_worker/tasks/jobs.py`
  </read_first>
  <files>services/api/src/caragent_api/routes/jobs.py, services/worker/src/caragent_worker/tasks/jobs.py, services/api/tests/test_generation.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Update retry handling so targeted edit jobs copy the original edit intent, selected target, parent version id, route preference, provider intent, and mask artifact metadata into the retry job. Reject retry when the parent version or mask artifact is no longer valid.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py tests/test_jobs.py</automated>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Retried targeted edits do not become full regenerations by accident.
    - Invalid retry prerequisites produce clear non-retryable state.
    - Parent lineage remains route-derived and immutable.
  </acceptance_criteria>
  <done>Retry preserves targeted edit intent safely.</done>
</task>

<task type="auto">
  <name>Task 4: Add workbench failure and retry UX</name>
  <read_first>
    - `apps/web/src/components/workbench/progress-panel.tsx`
    - `apps/web/src/components/workbench/workbench-app.tsx`
    - `apps/web/src/lib/api/iteration.ts`
  </read_first>
  <files>apps/web/src/components/workbench/progress-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/lib/api/iteration.ts, apps/web/src/app/page.test.tsx, apps/web/src/lib/api/iteration.test.ts</files>
  <action>Show targeted edit failure category, safe blocked reason, retry eligibility, and selected target summary in the workbench. Wire retry actions to preserve original intent and provider selection. Keep diagnostic copy concise and concept-preview scoped.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/api/iteration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Users can see why a targeted edit failed and whether it can be retried.
    - Retry controls do not appear when prerequisites are invalid.
    - Failure UI does not reveal secrets or raw provider payloads.
  </acceptance_criteria>
  <done>Workbench handles targeted edit failures and retry states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Failure details to browser | Error data may include provider or config details and must be sanitized. |
| Retry request to original intent | Retry must not let users spoof parent lineage or mask ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | Information Disclosure | failure messages | mitigate | Sanitize provider/config/storage errors before persistence and display. |
| T-10-17 | Tampering | retry metadata | mitigate | Copy retry intent from original durable job metadata and revalidate parent/mask ownership. |
| T-10-18 | Repudiation | failed edits | mitigate | Persist failure category, target summary, retry eligibility, and route in events/metadata. |
</threat_model>

<verification>
Run focused API jobs/generation tests, worker generation tests, and web workbench/API tests.
</verification>

<success_criteria>
- Targeted edit failures are actionable and sanitized.
- Retry preserves original edit intent and parent version.
- UI can distinguish retryable and non-retryable edit failures.
</success_criteria>

<output>
After completion, create `.planning/phases/10-targeted-regeneration-and-masked-editing-mvp/10-06-SUMMARY.md`.
</output>

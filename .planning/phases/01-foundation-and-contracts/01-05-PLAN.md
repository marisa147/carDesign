---
phase: 01-foundation-and-contracts
plan: "05"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - "packages/contracts/package.json"
  - "packages/contracts/README.md"
  - "packages/contracts/tsconfig.json"
  - "packages/contracts/orval.config.ts"
  - "packages/contracts/openapi/.gitkeep"
  - "packages/contracts/src/index.ts"
  - "packages/contracts/src/generated/.gitkeep"
autonomous: true
requirements: ["FOUND-02", "FOUND-03"]
must_haves:
  truths:
    - "Developer can identify `packages/contracts` as the frontend/backend contract boundary."
    - "The contracts package is configured for OpenAPI-first TypeScript generation."
    - "No hand-maintained duplicate backend TypeScript types are introduced."
  artifacts:
    - path: "packages/contracts/package.json"
      provides: "Contracts package scripts and exports"
      contains: "\"name\": \"@caragent/contracts\""
    - path: "packages/contracts/orval.config.ts"
      provides: "OpenAPI-to-TypeScript generator config"
      contains: "react-query"
    - path: "packages/contracts/src/index.ts"
      provides: "Stable frontend import entry"
      contains: "generated"
  key_links:
    - from: "packages/contracts/orval.config.ts"
      to: "packages/contracts/openapi/openapi.json"
      via: "Orval input"
      pattern: "openapi/openapi.json"
    - from: "packages/contracts/package.json"
      to: "scripts/check-contracts.mjs"
      via: "`check` script created by plan 01-06"
      pattern: "check-contracts"
---

<objective>
Create the shared contract package scaffold.

Purpose: Establish the package boundary and generator configuration before committing generated OpenAPI/client artifacts.
Output: `@caragent/contracts` package metadata, TypeScript config, Orval config, source entrypoint, and artifact directories.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-contracts/01-PATTERNS.md
@services/api/src/caragent_api/scripts/export_openapi.py

<interfaces>
Existing API export contract from plan `01-02`:

```text
cd services/api && uv run python -m caragent_api.scripts.export_openapi --out ../../packages/contracts/openapi/openapi.json
```

Package export contract to create:

```typescript
// packages/contracts/src/index.ts
export * from "./generated/client";
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API OpenAPI JSON -> generated TypeScript | Backend schema becomes browser-consumed code. |
| contracts package -> web app | Frontend imports generated client only from `@caragent/contracts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02E | Information Disclosure / Elevation | package exports | mitigate | Export generated contract code only; do not export backend internals, env files, or provider key names. |
| T-01-04E | Tampering / Repudiation | Orval config | mitigate | Configure one deterministic OpenAPI input and package scripts that plan `01-06` can use for drift checks. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create contracts package metadata and scripts</name>
  <files>packages/contracts/package.json, packages/contracts/README.md, packages/contracts/tsconfig.json</files>
  <read_first>Read `01-CONTEXT.md` D-10 through D-13 and `01-RESEARCH.md` Contracts before editing.</read_first>
  <action>Create `packages/contracts/package.json` named `@caragent/contracts`, private, ESM-compatible, with scripts `generate`, `check`, `typecheck`, and `lint`. `generate` should call Orval from this package, `check` should call `node ../../scripts/check-contracts.mjs` once plan `01-06` creates it, and `typecheck` should use the package `tsconfig.json`. Before creating lockfiles, run official npm registry checks such as `pnpm view orval version` and `pnpm view typescript version`, then record chosen pins in the plan summary. Add dependencies/devDependencies needed for Orval generation and TypeScript only. Create README text explaining FastAPI/Pydantic OpenAPI is the source of truth per D-10 and that manual copied types are not allowed per D-11 and D-13.</action>
  <verify>
    <automated>node -e "const p=require('./packages/contracts/package.json'); if (p.name !== '@caragent/contracts') throw new Error('wrong package name'); for (const key of ['generate','check','typecheck','lint']) if (!p.scripts?.[key]) throw new Error('missing contracts script '+key);"</automated>
  </verify>
  <done>Contracts package has stable scripts and metadata for downstream generation.</done>
</task>

<task type="auto">
  <name>Task 2: Add Orval config and stable exports</name>
  <files>packages/contracts/orval.config.ts, packages/contracts/openapi/.gitkeep, packages/contracts/src/index.ts, packages/contracts/src/generated/.gitkeep</files>
  <read_first>Read `01-RESEARCH.md` Orval React Query Generation and `01-PATTERNS.md` Contract Package.</read_first>
  <action>Create `orval.config.ts` using `defineConfig` with input `./openapi/openapi.json` and a single generated target `./src/generated/client.ts` using React Query/TanStack Query client generation. Create `.gitkeep` files so artifact directories exist before generated files are written. Create `src/index.ts` as the only public import entry and have it export from `./generated/client` after plan `01-06` writes that generated file; if TypeScript needs the export delayed until generation, add a comment explaining that `pnpm contracts:generate` must run before package typecheck.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const cfg=fs.readFileSync('packages/contracts/orval.config.ts','utf8'); for (const token of ['defineConfig','openapi/openapi.json','src/generated/client.ts','react-query']) if (!cfg.includes(token)) throw new Error('orval config missing '+token);"</automated>
  </verify>
  <done>Contract package scaffold points to backend OpenAPI and exposes generated client as the stable boundary.</done>
</task>

</tasks>

<verification>
Run the automated checks from both tasks. Do not generate contract artifacts in this plan; plan `01-06` owns generation and drift checking.
</verification>

<success_criteria>
- `@caragent/contracts` exists as a dedicated package.
- Orval config targets backend OpenAPI and generates a single TypeScript client artifact.
- Package exports are ready for generated-client consumption without duplicating backend types by hand.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-05-SUMMARY.md`.
</output>

---
phase: 01-foundation-and-contracts
plan: "08"
type: execute
wave: 5
depends_on: ["01-06", "01-07"]
files_modified:
  - "apps/web/src/lib/config/public-env.ts"
  - "apps/web/src/lib/api/health.ts"
  - "apps/web/src/app/page.tsx"
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/components/ui/button.tsx"
  - "apps/web/src/components/ui/badge.tsx"
  - "apps/web/src/components/ui/card.tsx"
  - "apps/web/src/components/ui/alert.tsx"
  - "apps/web/src/test/setup.ts"
autonomous: true
requirements: ["FOUND-01", "FOUND-02", "FOUND-03", "FOUND-04"]
must_haves:
  truths:
    - "User sees a Phase 1 foundation shell at `/`."
    - "The health action consumes generated API contracts through `@caragent/contracts`."
    - "The shell labels future workbench regions as unavailable without exposing unsupported product controls."
  artifacts:
    - path: "apps/web/src/lib/api/health.ts"
      provides: "Generated-contract health wrapper"
      contains: "@caragent/contracts"
    - path: "apps/web/src/app/page.tsx"
      provides: "Phase 1 foundation shell"
      contains: "检查堆栈健康"
    - path: "apps/web/src/app/page.test.tsx"
      provides: "UI contract and boundary tests"
      contains: "后续阶段开放"
  key_links:
    - from: "apps/web/src/lib/api/health.ts"
      to: "packages/contracts/src/generated/client.ts"
      via: "@caragent/contracts import"
      pattern: "@caragent/contracts"
    - from: "apps/web/src/app/page.tsx"
      to: "apps/web/src/lib/api/health.ts"
      via: "health-check CTA"
      pattern: "checkStackHealth"
---

<objective>
Implement the Phase 1 web shell and generated-contract health integration.

Purpose: Prove the frontend can consume generated API contracts while presenting only the approved foundation readiness shell.
Output: Public env reader, health wrapper, root page, shell tests, and approved UI components required by the shell.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-UI-SPEC.md
@.planning/phases/01-foundation-and-contracts/01-RESEARCH.md
@packages/contracts/src/index.ts
@packages/contracts/src/generated/client.ts
@apps/web/package.json

<interfaces>
Generated contract import boundary from plan `01-06`:

```typescript
import { ... } from "@caragent/contracts";
```

Browser config boundary:

```typescript
export const publicEnv = {
  apiBaseUrl: process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8000",
};
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> API | Health CTA calls API base URL through generated contract code. |
| browser bundle -> environment | Only public Next.js env values may enter frontend code. |
| shell UI -> user expectations | Disabled future regions must not imply generation/upload/export is available. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02H | Information Disclosure / Elevation | `apps/web/src/lib/api/health.ts` | mitigate | Import from `@caragent/contracts` only; tests fail if backend paths or secret env names appear in web source. |
| T-01-04H | Tampering / Repudiation | health wrapper | mitigate | Use generated health response types/client and tests for mapping Web/API/Worker/Contracts/Local Services statuses. |
| T-01-05H | Information Disclosure | shell copy | mitigate | UI copy states only foundation checks are available and labels future regions `后续阶段开放`; no production or generation workflow is implied. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement public env and generated-contract health wrapper</name>
  <files>apps/web/src/lib/config/public-env.ts, apps/web/src/lib/api/health.ts</files>
  <read_first>Read `packages/contracts/src/index.ts`, generated client exports, and `01-CONTEXT.md` D-10 through D-16 before editing.</read_first>
  <behavior>
    - Test health wrapper imports from `@caragent/contracts` for health contract types/client.
    - Test `checkStackHealth` maps a successful health response into Web/API/Worker/Contracts/Local Services card states.
    - Test web source does not read backend-only env names or provider key names.
  </behavior>
  <action>Create `public-env.ts` exposing only `NEXT_PUBLIC_API_BASE_URL`, defaulting to `http://localhost:8000` for local mode per D-14. Create `health.ts` with exported `checkStackHealth` that imports generated health contract types/client from `@caragent/contracts`, calls the generated `/health` client, and maps response dependencies into five shell statuses: Web, API, Worker, Contracts, and Local Services. If Orval's generated function name differs from the expected health operation name, adapt the wrapper to the actual generated export and document the exact export name in the summary. Do not import `services/api`, Python files, backend config, or provider keys per D-02 and D-16.</action>
  <verify>
    <automated>rg "@caragent/contracts" apps/web/src/lib/api/health.ts</automated>
    <automated>if (rg "AI_PROVIDER_|S3_SECRET_ACCESS_KEY|DATABASE_URL|REDIS_URL|services/api|caragent_api" apps/web/src) { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>Web API boundary consumes generated contracts and exposes only public browser config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build root foundation shell</name>
  <files>apps/web/src/app/page.tsx, apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/badge.tsx, apps/web/src/components/ui/card.tsx, apps/web/src/components/ui/alert.tsx</files>
  <read_first>Read `01-UI-SPEC.md` Phase 1 Shell Scope, Visual Hierarchy, Interaction Contract, Copywriting Contract, and Component Inventory before editing.</read_first>
  <behavior>
    - Test `/` renders brand `痛车设计 Agent`, CTA `检查堆栈健康`, heading `工作台基础已就绪`, and status cards for Web, API, Worker, Contracts, and Local Services.
    - Test click on the CTA enters loading text `正在检查服务...`.
    - Test disabled future regions expose `aria-disabled="true"` and copy `后续阶段开放`.
  </behavior>
  <action>Add only official shadcn `button`, `badge`, `card`, and `alert` components, then implement `page.tsx` as the Phase 1 foundation shell per D-20 and D-21. Layout requirements: 56px top bar with brand `痛车设计 Agent`, environment badge, and primary button `检查堆栈健康`; 280px desktop left rail with active `Foundation` item and muted disabled region labels; main region with five readiness cards for Web, API, Worker, Contracts, and Local Services; 32px desktop footer/status strip with local mode, API base URL, and contract generation status. Below 900px, make the rail a horizontal segmented row; below 640px, stack cards one column with no horizontal scroll. Use UI-SPEC copy exactly: `正在检查服务...`, `工作台基础已就绪`, and `后续阶段开放`. Do not add chat input, upload, generate, export, 2D/3D preview controls, variant history, auth, marketplace, provider calls, production handoff UI, gradients, decorative imagery, nested cards, or page sections styled as floating cards.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('apps/web/src/app/page.tsx','utf8'); for (const token of ['痛车设计 Agent','检查堆栈健康','正在检查服务...','工作台基础已就绪','后续阶段开放']) if (!s.includes(token)) throw new Error('missing UI copy '+token);"</automated>
    <automated>node -e "const fs=require('fs'); const allowed=['button','badge','card','alert']; for (const c of allowed) if (!fs.existsSync(`apps/web/src/components/ui/${c}.tsx`)) throw new Error('missing ui component '+c); const files=fs.readdirSync('apps/web/src/components/ui').filter(f=>f.endsWith('.tsx')).map(f=>f.replace('.tsx','')); for (const f of files) if (!allowed.includes(f)) throw new Error('unapproved shell component '+f);"</automated>
  </verify>
  <done>Root route implements the approved foundation shell and disabled future-region treatment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add shell tests and frontend verification</name>
  <files>apps/web/src/app/page.test.tsx, apps/web/src/test/setup.ts</files>
  <read_first>Read `01-UI-SPEC.md` Accessibility Contract and `01-VALIDATION.md` frontend test expectations before editing.</read_first>
  <behavior>
    - Test required Chinese copy and five status cards are visible.
    - Test forbidden product control labels such as prompt input, upload button, generate button, export button, and canvas controls are absent.
    - Test future regions are disabled and have accessible explanatory copy.
  </behavior>
  <action>Create Vitest/Testing Library setup and page tests for the UI contract. Tests must assert required copy, disabled future regions, loading state, generated-contract health wrapper usage, and absence of deferred Phase 4+ controls. Keep the test runner non-watch by default for validation.</action>
  <verify>
    <automated>pnpm --filter @caragent/web lint</automated>
    <automated>pnpm --filter @caragent/web typecheck</automated>
    <automated>pnpm --filter @caragent/web test -- --run</automated>
  </verify>
  <done>Frontend tests enforce generated-contract consumption and UI-SPEC boundaries.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter @caragent/web lint`, `pnpm --filter @caragent/web typecheck`, and `pnpm --filter @caragent/web test -- --run`. Then run `rg "chat|upload|generate|export|Three|canvas|provider|AI_PROVIDER_|DATABASE_URL|S3_SECRET_ACCESS_KEY" apps/web/src` and inspect any match; only disabled explanatory copy approved by `01-UI-SPEC.md` may remain.
</verification>

<success_criteria>
- `/` implements only the Phase 1 foundation shell.
- Web code consumes generated API contracts from `@caragent/contracts`.
- UI tests enforce required copy, disabled future regions, and forbidden product controls.
- Browser bundle has no backend secret/config imports.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-08-SUMMARY.md`.
</output>

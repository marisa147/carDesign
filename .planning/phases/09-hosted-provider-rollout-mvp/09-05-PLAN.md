---
phase: 09-hosted-provider-rollout-mvp
plan: "05"
type: execute
wave: 3
depends_on: ["09-02-PLAN.md", "09-04-PLAN.md"]
files_modified:
  - "services/core/src/caragent_core/enums.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/src/caragent_api/routes/operations.py"
  - "services/worker/src/caragent_worker/providers/base.py"
  - "services/worker/src/caragent_worker/providers/bfl.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/tests/test_image_providers.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/api/tests/test_jobs.py"
  - "services/api/tests/test_operations.py"
autonomous: true
requirements: ["V2-PROVIDER-02", "V2-PROVIDER-04"]
must_haves:
  truths:
    - "Hosted provider moderation, validation, rate, and credit failures are user-safe and distinguishable."
    - "Failure mapping must improve operator visibility without leaking provider payloads."
    - "Existing local/provider/timeout/storage/cancel categories remain compatible."
  artifacts:
    - "Failure category additions or structured mappings with tests."
    - "User-safe messages for moderation/rate/credits/provider validation."
    - "Operations and job API tests proving redaction."
  key_links:
    - "09-CONTEXT D-15/D-16 -> normalize hosted failures and redact secrets"
    - "BFL errors docs -> moderation, 402, 429, validation surfaces"
    - "09-VALIDATION -> 09-05 focused verification"
---

<objective>
Normalize hosted moderation, validation, rate-limit, credit, and provider failure messages.

Purpose: A hosted rollout is only operable if users and operators can tell blocked content, provider validation errors, quota/rate failures, missing credits, timeouts, and generic provider errors apart without seeing secrets.
Output: Failure categories or structured metadata that make hosted failures safe and actionable in job APIs and operations status.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hosted-provider-rollout-mvp/09-CONTEXT.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-RESEARCH.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-PATTERNS.md
@services/core/src/caragent_core/enums.py
@services/worker/src/caragent_worker/providers/base.py
@services/worker/src/caragent_worker/providers/bfl.py
@services/worker/src/caragent_worker/tasks/jobs.py
@services/api/src/caragent_api/routes/jobs.py
@services/api/src/caragent_api/routes/operations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for hosted failure classification</name>
  <read_first>
    - `services/worker/tests/test_image_providers.py`
    - `services/worker/tests/test_generation_tasks.py`
    - `services/api/tests/test_jobs.py`
    - `services/api/tests/test_operations.py`
  </read_first>
  <files>services/worker/tests/test_image_providers.py, services/worker/tests/test_generation_tasks.py, services/api/tests/test_jobs.py, services/api/tests/test_operations.py</files>
  <action>Add failing tests for request moderation, content moderation, provider validation errors, insufficient credits, rate limit, timeout, provider configuration, and unknown provider errors. Assert the resulting API/job/operations messages are useful, categorized, and secret-free.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py tests/test_generation_tasks.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - Moderation is not collapsed into generic unknown failure.
    - 402/429-style failures are distinguishable.
    - Signed URLs, API keys, bearer strings, and raw provider bodies do not appear in assertions.
  </acceptance_criteria>
  <done>Failure classification behavior is specified by red tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement provider failure categories or structured subcategories</name>
  <read_first>
    - `services/core/src/caragent_core/enums.py`
    - `services/core/src/caragent_core/services/jobs.py`
    - `services/worker/src/caragent_worker/tasks/jobs.py`
  </read_first>
  <files>services/core/src/caragent_core/enums.py, services/core/src/caragent_core/services/jobs.py, services/worker/src/caragent_worker/providers/base.py, services/worker/src/caragent_worker/providers/bfl.py, services/worker/src/caragent_worker/tasks/jobs.py</files>
  <action>Add the smallest compatible failure-category extension or structured metadata needed to distinguish provider moderation, provider validation, rate limit, and credits. Update worker classification and provider exception types/metadata accordingly while preserving existing category values where clients depend on them.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Classification is deterministic from provider exception/status metadata.
    - Existing cancellation, storage, rights, timeout, provider, provider_configuration, and unknown handling still works.
    - Messages remain user-safe and concise.
  </acceptance_criteria>
  <done>Hosted failure mapping is implemented without breaking existing categories.</done>
</task>

<task type="auto">
  <name>Task 3: Expose safe failure diagnostics to API and operations</name>
  <read_first>
    - `services/api/src/caragent_api/schemas.py`
    - `services/api/src/caragent_api/routes/jobs.py`
    - `services/api/src/caragent_api/routes/operations.py`
  </read_first>
  <files>services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/routes/jobs.py, services/api/src/caragent_api/routes/operations.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Update API response schemas and route mapping if needed so the workbench and operators can inspect failure category, provider subcategory/status, stage, provider/model, and redacted message. Regenerate/check contracts.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_operations.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Job and operations APIs expose enough failure context for Phase 9 UI.
    - No secret-bearing raw payload leaves the API.
    - Contract artifacts are current.
  </acceptance_criteria>
  <done>Safe hosted failure diagnostics are API-visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External provider error to user message | Raw provider payloads can contain sensitive implementation details and must be normalized. |
| Failure category to client behavior | Categories can change UI affordances and operator decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Information Disclosure | failure diagnostics | mitigate | Redact all diagnostics and test common secret patterns. |
| T-09-13 | Repudiation | provider failure classification | mitigate | Persist category/status/stage on job events and model runs. |
| T-09-14 | Denial of Service | rate/credit handling | mitigate | Distinguish rate and credit failures so operators can stop retries or fix billing. |
</threat_model>

<verification>
Run worker provider/generation tests, API jobs/operations tests, and contract check.
</verification>

<success_criteria>
- V2-PROVIDER-04 failures are inspectable and secret-free.
- V2-PROVIDER-02 blocked/rejected hosted submissions show useful reasons.
- Existing local deterministic and legacy failure paths remain compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/09-hosted-provider-rollout-mvp/09-05-SUMMARY.md`.
</output>

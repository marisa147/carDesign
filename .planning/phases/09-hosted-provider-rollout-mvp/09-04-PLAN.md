---
phase: 09-hosted-provider-rollout-mvp
plan: "04"
type: execute
wave: 2
depends_on: ["09-02-PLAN.md", "09-03-PLAN.md"]
files_modified:
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/src/caragent_api/routes/operations.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/api/tests/test_jobs.py"
  - "services/api/tests/test_operations.py"
autonomous: true
requirements: ["V2-PROVIDER-03", "V2-PROVIDER-04"]
must_haves:
  truths:
    - "model_runs remain the canonical provider-call trace."
    - "Estimated cost and actual cost have different meanings and must not be conflated."
    - "Fallback from hosted to local is durable and visible."
  artifacts:
    - "Worker/API tests for provider trace, fallback metadata, cost, and sanitized errors."
    - "Job/model-run/event metadata updates using existing service helpers."
    - "Operations recent failures remain secret-free."
  key_links:
    - "09-CONTEXT D-11..D-16 -> trace and failure visibility"
    - "09-RESEARCH -> Trace, Cost, And Failure Mapping"
    - "Phase 9 roadmap success criteria 3 and 4"
---

<objective>
Persist provider request/response trace, cost, fallback, and sanitized error metadata.

Purpose: Users/operators must be able to inspect what provider/model was requested, what route actually ran, what cost was estimated/actual, and why a hosted call failed without exposing secrets.
Output: Durable job/model-run/artifact/version/events metadata and API-visible summaries that make hosted route and fallback behavior auditable.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hosted-provider-rollout-mvp/09-CONTEXT.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-RESEARCH.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-PATTERNS.md
@services/worker/src/caragent_worker/tasks/jobs.py
@services/core/src/caragent_core/models.py
@services/core/src/caragent_core/services/jobs.py
@services/api/src/caragent_api/routes/jobs.py
@services/api/src/caragent_api/routes/operations.py
@services/api/src/caragent_api/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for provider trace and cost semantics</name>
  <read_first>
    - `services/worker/tests/test_generation_tasks.py`
    - `services/api/tests/test_jobs.py`
    - `services/api/tests/test_operations.py`
  </read_first>
  <files>services/worker/tests/test_generation_tasks.py, services/api/tests/test_jobs.py, services/api/tests/test_operations.py</files>
  <action>Add failing tests that assert hosted generation records provider, model, prompt plan, request parameters, input artifact ids, estimated cost, actual cost when provider supplies it, null actual cost when unavailable, raw error category, and sanitized error message. Add fallback tests proving hosted attempt plus local fallback are visible.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if hosted actual cost is faked as zero.
    - Tests fail if fallback looks like an ordinary local-only generation.
    - Tests fail if raw secret-bearing payloads appear in API responses.
  </acceptance_criteria>
  <done>Trace/cost/fallback semantics are locked by red tests.</done>
</task>

<task type="auto">
  <name>Task 2: Persist hosted route and fallback metadata consistently</name>
  <read_first>
    - `services/core/src/caragent_core/services/jobs.py`
    - `services/worker/src/caragent_worker/tasks/jobs.py`
  </read_first>
  <files>services/worker/src/caragent_worker/tasks/jobs.py, services/core/src/caragent_core/services/jobs.py</files>
  <action>Update worker persistence to write provider/model intent, actual route, fallback route, provider parameters, prompt payload, input artifacts, estimated cost, provider actual cost, and sanitized failure metadata to existing model-run/job-event/artifact/design-version locations. Prefer existing columns/helpers before adding schema.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - `model_runs` are the canonical provider-call trace.
    - Job events distinguish preflight block, hosted attempt, hosted failure, fallback start, fallback success, and final failure where applicable.
    - Artifact/version metadata makes hosted/fallback route reviewable.
  </acceptance_criteria>
  <done>Provider route and fallback evidence is durable.</done>
</task>

<task type="auto">
  <name>Task 3: Surface trace and sanitized diagnostics through API</name>
  <read_first>
    - `services/api/src/caragent_api/routes/jobs.py`
    - `services/api/src/caragent_api/routes/operations.py`
    - `services/api/src/caragent_api/schemas.py`
  </read_first>
  <files>services/api/src/caragent_api/routes/jobs.py, services/api/src/caragent_api/routes/operations.py, services/api/src/caragent_api/schemas.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Ensure job/model-run responses and operations recent failures expose provider route, model, cost, category, stage, fallback state, and sanitized message fields needed by the workbench. Regenerate/check contracts if response schemas change.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_operations.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - API responses include trace enough for UI/operator inspection.
    - API responses do not expose secrets, raw BFL headers, signed result URLs, or raw provider payloads.
    - Contract artifacts are in sync.
  </acceptance_criteria>
  <done>Trace and diagnostics are visible through safe API contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider response to durable ledger | External payloads must be normalized and redacted before storage. |
| Durable ledger to browser API | API must expose useful trace without leaking raw secrets or signed URLs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Repudiation | model_runs/job_events | mitigate | Persist provider/model/cost/fallback/status metadata for each hosted attempt. |
| T-09-10 | Information Disclosure | job APIs/operations | mitigate | Store and expose sanitized provider diagnostics only. |
| T-09-11 | Tampering | cost accounting | mitigate | Separate estimated and actual cost; actual cost only from provider response or trusted adapter calculation. |
</threat_model>

<verification>
Run worker generation tests, API jobs/operations tests, and contract check.
</verification>

<success_criteria>
- V2-PROVIDER-03 is traceable through `model_runs` and related job artifacts.
- V2-PROVIDER-04 has safe diagnostics in API/operations status.
- Hosted fallback cannot be mistaken for normal local generation.
</success_criteria>

<output>
After completion, create `.planning/phases/09-hosted-provider-rollout-mvp/09-04-SUMMARY.md`.
</output>

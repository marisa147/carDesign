---
phase: 09-hosted-provider-rollout-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "services/core/src/caragent_core/provider_capabilities.py"
  - "services/api/src/caragent_api/config.py"
  - "services/api/src/caragent_api/routes/operations.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/worker/src/caragent_worker/config.py"
  - ".env.example"
  - "services/api/.env.example"
  - "services/worker/.env.example"
  - "services/api/tests/test_config.py"
  - "services/api/tests/test_operations.py"
  - "services/worker/tests/test_config.py"
autonomous: true
requirements: ["V2-PROVIDER-01", "V2-PROVIDER-05"]
must_haves:
  truths:
    - "Hosted provider metadata is typed, config-driven, and browser-safe."
    - "Local deterministic provider remains available without hosted credentials."
    - "No provider secret is exposed through operations status or generated contracts."
  artifacts:
    - "Provider capability map/helper with tests."
    - "Operations status exposing safe capability and guard metadata."
    - "Env examples documenting default-off hosted rollout settings."
  key_links:
    - "09-CONTEXT D-04/D-10 -> typed gates and capability map"
    - "09-RESEARCH -> official BFL docs and cost caveats"
    - "09-VALIDATION -> 09-01 focused verification"
---

<objective>
Create the typed hosted provider capability and runtime configuration surface for Phase 9.

Purpose: Operators need to configure provider credentials, model, timeout, retry, fallback, quota, rate, and cost policy without code changes, while the browser receives only safe metadata.
Output: A tested capability map/config layer that keeps local deterministic generation as the no-credential default and exposes safe operations status for UI use.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hosted-provider-rollout-mvp/09-CONTEXT.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-RESEARCH.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-VALIDATION.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-PATTERNS.md
@services/api/src/caragent_api/config.py
@services/api/src/caragent_api/routes/operations.py
@services/api/src/caragent_api/schemas.py
@services/worker/src/caragent_worker/config.py
@services/worker/src/caragent_worker/providers/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for provider capability defaults and safe operations metadata</name>
  <read_first>
    - `services/api/tests/test_config.py`
    - `services/api/tests/test_operations.py`
    - `services/worker/tests/test_config.py`
  </read_first>
  <files>services/api/tests/test_config.py, services/api/tests/test_operations.py, services/worker/tests/test_config.py</files>
  <action>Add failing tests that prove: local deterministic provider is the default; hosted BFL capability appears only as safe metadata; missing BFL credentials produce `credential_configured=false` and blocked reasons, not errors; quota/rate/cost guards are represented; no secret-like values appear in the operations response.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_config.py tests/test_operations.py</automated>
    <automated>cd services/worker && uv run pytest -q tests/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation for missing capability/guard fields.
    - Assertions explicitly check that keys/tokens/URLs with credentials are absent from browser-safe response JSON.
    - Local deterministic mode remains valid with empty hosted provider credentials.
  </acceptance_criteria>
  <done>Capability/config behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed provider capability map</name>
  <read_first>
    - `services/core/src/caragent_core/enums.py`
    - `services/api/src/caragent_api/schemas.py`
    - `services/worker/src/caragent_worker/providers/base.py`
  </read_first>
  <files>services/core/src/caragent_core/provider_capabilities.py, services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/config.py, services/worker/src/caragent_worker/config.py</files>
  <action>Create or extend a shared typed capability representation for `local-deterministic` and `bfl`. Include provider ids, aliases, display labels, supported operations, default/allowed model ids, size/output-format constraints, reference/mask flags for later phases, estimated-cost metadata, timeout/polling defaults, user-safe caveats, and credential/guard requirements. Keep any actual secret values in server-only settings.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_config.py</automated>
    <automated>cd services/worker && uv run pytest -q tests/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - Capability metadata is typed and deterministic.
    - BFL model defaults are configurable, not hardcoded as product truth.
    - Future mask/reference support can be represented as disabled capability flags without implementing Phase 10/11 behavior.
  </acceptance_criteria>
  <done>Capability map exists and validates default/local/hosted metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Expose capability and guard status through operations API</name>
  <read_first>
    - `services/api/src/caragent_api/routes/operations.py`
    - `services/api/src/caragent_api/schemas.py`
    - `packages/contracts/openapi/openapi.json`
  </read_first>
  <files>services/api/src/caragent_api/routes/operations.py, services/api/src/caragent_api/schemas.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Extend provider operations status with safe capability summaries, active/default provider, hosted rollout flag state, provider-call flag state, credential configured booleans, quota/rate/cost guard states, and blocked reasons. Regenerate/check contracts if schemas change.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_operations.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Operations response contains enough metadata for the UI selector to render enabled/blocked/local states.
    - Response contains no secret material.
    - Generated TypeScript contracts stay in sync with API schemas.
  </acceptance_criteria>
  <done>Browser-safe provider status is available through the existing operations endpoint.</done>
</task>

<task type="auto">
  <name>Task 4: Document default-off hosted provider configuration</name>
  <read_first>
    - `.env.example`
    - `services/api/.env.example`
    - `services/worker/.env.example`
  </read_first>
  <files>.env.example, services/api/.env.example, services/worker/.env.example</files>
  <action>Update env examples with Phase 9 provider capability/config fields, using safe placeholder values and comments that hosted provider calls remain disabled by default. Include guard settings needed before hosted calls can run.</action>
  <verify>
    <automated>Select-String -Path .env.example,services/api/.env.example,services/worker/.env.example -Pattern "V2_HOSTED_PROVIDER_ROLLOUT_ENABLED","AI_PROVIDER_CALLS_ENABLED","BFL","quota","cost"</automated>
  </verify>
  <acceptance_criteria>
    - Docs/examples show how to configure hosted rollout without code changes.
    - No real keys or secret-shaped values are committed.
    - Local deterministic provider remains documented as the default validation path.
  </acceptance_criteria>
  <done>Operator-facing provider config examples are current and safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server config to browser metadata | API must expose only safe provider status and never secrets. |
| Local provider to hosted provider | Hosted capability must not make local deterministic validation require credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | operations status | mitigate | Use typed safe DTOs with explicit credential booleans and redaction tests. |
| T-09-02 | Elevation of Privilege | provider enablement | mitigate | Require feature flag plus provider-call flag and guard settings before hosted status becomes callable. |
</threat_model>

<verification>
Run API config/operations tests, worker config tests, and `corepack pnpm contracts:check`.
</verification>

<success_criteria>
- V2-PROVIDER-01 has a typed config/capability foundation.
- V2-PROVIDER-05 remains true in default local-only mode.
- UI can consume provider metadata without learning secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/09-hosted-provider-rollout-mvp/09-01-SUMMARY.md`.
</output>

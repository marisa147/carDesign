---
phase: 09-hosted-provider-rollout-mvp
plan: "02"
type: execute
wave: 1
depends_on: ["09-01-PLAN.md"]
files_modified:
  - "services/worker/src/caragent_worker/providers/base.py"
  - "services/worker/src/caragent_worker/providers/bfl.py"
  - "services/worker/src/caragent_worker/providers/__init__.py"
  - "services/worker/tests/test_image_providers.py"
autonomous: true
requirements: ["V2-PROVIDER-01", "V2-PROVIDER-03", "V2-PROVIDER-05"]
must_haves:
  truths:
    - "BFL adapter matches current official docs for auth header, submit response, polling, result download, and cost fields."
    - "Provider-specific behavior stays behind the worker ImageProvider boundary."
    - "Hosted adapter failures are sanitized and local provider behavior remains unchanged."
  artifacts:
    - "Updated BFL provider tests using httpx MockTransport."
    - "Adapter code that uses returned polling_url when available."
    - "Cost/moderation/error result handling suitable for downstream persistence."
  key_links:
    - "BFL docs -> x-key, polling_url, result.sample, cost"
    - "09-RESEARCH -> BFL adapter section"
    - "09-PATTERNS -> Provider Boundary"
---

<objective>
Update the BFL hosted provider adapter behind the existing worker provider boundary.

Purpose: Phase 9 needs one real hosted provider path that is correct against current official docs while preserving the local deterministic provider for tests and fallback.
Output: A tested BFL adapter that submits, polls, downloads, reports cost, maps provider states, and sanitizes failures without leaking secrets.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hosted-provider-rollout-mvp/09-CONTEXT.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-RESEARCH.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-VALIDATION.md
@services/worker/src/caragent_worker/providers/base.py
@services/worker/src/caragent_worker/providers/bfl.py
@services/worker/src/caragent_worker/providers/local.py
@services/worker/tests/test_image_providers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write BFL adapter contract tests from official docs</name>
  <read_first>
    - `services/worker/tests/test_image_providers.py`
    - `.planning/phases/09-hosted-provider-rollout-mvp/09-RESEARCH.md`
  </read_first>
  <files>services/worker/tests/test_image_providers.py</files>
  <action>Add failing tests for the documented BFL flow: submit uses `x-key`; endpoint path is model/config driven; submit response `polling_url` is used for polling; `Ready` with `result.sample` downloads bytes; submit `cost` becomes actual cost when available; signed delivery URL is not returned directly to callers; moderation and error statuses raise typed/sanitized provider errors.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail against the old Bearer-header/reconstructed-polling scaffold.
    - Tests do not make network calls.
    - Tests assert API keys do not appear in exception strings.
  </acceptance_criteria>
  <done>BFL official-doc behavior is captured by red tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement documented submit, polling, download, and cost behavior</name>
  <read_first>
    - `services/worker/src/caragent_worker/providers/base.py`
    - `services/worker/src/caragent_worker/providers/bfl.py`
    - `services/worker/src/caragent_worker/config.py`
  </read_first>
  <files>services/worker/src/caragent_worker/providers/base.py, services/worker/src/caragent_worker/providers/bfl.py</files>
  <action>Update BFL provider implementation to use `x-key`, model/config-driven endpoint paths, returned `polling_url`, bounded polling, immediate result download, provider response cost, and normalized metadata. Leave `actual_cost` null if cost is unavailable instead of writing fake hosted cost.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Adapter uses official auth and polling mechanics.
    - Result bytes are returned for object-storage persistence.
    - Actual cost is sourced from provider response where available.
    - Local provider tests still pass.
  </acceptance_criteria>
  <done>BFL adapter works against mocked official-doc flows.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize BFL provider statuses and sanitized errors</name>
  <read_first>
    - `services/worker/src/caragent_worker/providers/base.py`
    - `services/worker/src/caragent_worker/tasks/jobs.py`
  </read_first>
  <files>services/worker/src/caragent_worker/providers/base.py, services/worker/src/caragent_worker/providers/bfl.py, services/worker/tests/test_image_providers.py</files>
  <action>Represent provider statuses such as pending, ready, error, request moderated, content moderated, not found, rate limit, and insufficient credits in adapter exceptions or result metadata so downstream worker code can classify them in later plans. Keep raw provider responses out of user-facing error text.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Moderation and provider error statuses are distinguishable to worker classification logic.
    - Sanitized errors never include API key, bearer token, signed result URL query secrets, or raw response bodies with sensitive fields.
    - Timeout behavior remains bounded.
  </acceptance_criteria>
  <done>BFL statuses are ready for Phase 9 failure mapping and trace persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker to BFL API | External provider receives prompt and configured parameters; credentials must remain server-only. |
| BFL delivery URL to project storage | Short-lived signed result URLs must be downloaded and re-served from project storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | BFL errors/results | mitigate | Sanitize exceptions and never expose signed delivery URLs to browser callers. |
| T-09-04 | Denial of Service | polling loop | mitigate | Keep bounded timeout and poll interval settings with tests. |
| T-09-05 | Repudiation | provider cost/status | mitigate | Return cost/status metadata for durable model-run persistence in later plans. |
</threat_model>

<verification>
Run `cd services/worker && uv run pytest -q tests/test_image_providers.py`.
</verification>

<success_criteria>
- BFL adapter matches official Phase 9 research assumptions.
- Provider tests are deterministic and network-free.
- Local deterministic provider remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/09-hosted-provider-rollout-mvp/09-02-SUMMARY.md`.
</output>

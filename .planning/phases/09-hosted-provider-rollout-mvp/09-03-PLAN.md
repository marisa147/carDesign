---
phase: 09-hosted-provider-rollout-mvp
plan: "03"
type: execute
wave: 2
depends_on: ["09-01-PLAN.md", "09-02-PLAN.md"]
files_modified:
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/generation.py"
  - "services/api/src/caragent_api/routes/operations.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
  - "services/api/tests/test_generation.py"
  - "services/api/tests/test_operations.py"
  - "services/worker/tests/test_generation_tasks.py"
autonomous: true
requirements: ["V2-PROVIDER-01", "V2-PROVIDER-02", "V2-PROVIDER-05"]
must_haves:
  truths:
    - "Hosted generation can be requested only when API preflight allows it."
    - "Worker repeats authoritative preflight immediately before external calls."
    - "Provider/model intent is persisted per job and local deterministic fallback remains available."
  artifacts:
    - "Generation submission schema extended with safe provider/model intent."
    - "API preflight tests for blocked and allowed hosted submissions."
    - "Worker routing tests proving persisted intent beats process defaults."
  key_links:
    - "09-CONTEXT D-04..D-09 -> gating and per-job provider selection"
    - "09-VALIDATION -> 09-03 focused verification"
    - "Phase 9 roadmap success criteria 2"
---

<objective>
Wire hosted provider intent and preflight into API submission and worker routing.

Purpose: A user should be able to request hosted generation only when hosted rollout is enabled, credentials/guards are configured, and quota/rate/cost checks allow it.
Output: Typed API contracts and worker routing that persist per-job provider/model intent while preserving the local deterministic path.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-hosted-provider-rollout-mvp/09-CONTEXT.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-RESEARCH.md
@.planning/phases/09-hosted-provider-rollout-mvp/09-VALIDATION.md
@services/api/src/caragent_api/schemas.py
@services/api/src/caragent_api/routes/generation.py
@services/api/src/caragent_api/routes/operations.py
@services/worker/src/caragent_worker/tasks/jobs.py
@services/core/src/caragent_core/models.py
@services/core/src/caragent_core/services/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red API tests for provider/model submission intent and preflight</name>
  <read_first>
    - `services/api/tests/test_generation.py`
    - `services/api/tests/test_operations.py`
    - `services/api/src/caragent_api/schemas.py`
  </read_first>
  <files>services/api/tests/test_generation.py, services/api/tests/test_operations.py</files>
  <action>Add failing tests for optional provider/model fields on generation submission. Cover local default submission, hosted submission blocked by missing feature flag, missing provider calls flag, missing credentials, missing quota/rate/cost guard, unsupported model/capability, and hosted submission accepted when all gates pass.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py tests/test_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove hosted jobs are not enqueued when preflight blocks them.
    - Error responses use user-safe blocked reasons.
    - Local deterministic submission behavior is unchanged when provider fields are omitted.
  </acceptance_criteria>
  <done>API preflight and request-contract behavior is specified by red tests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend submission contracts and persist provider intent</name>
  <read_first>
    - `services/api/src/caragent_api/routes/generation.py`
    - `services/core/src/caragent_core/models.py`
    - `services/core/src/caragent_core/services/jobs.py`
  </read_first>
  <files>services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/routes/generation.py, services/core/src/caragent_core/services/jobs.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Extend generation submission with safe optional provider, model, and provider-parameter intent. Persist the normalized intent on the job or metadata so worker routing can read it later. Reject unsupported/blocked hosted intent before enqueueing. Regenerate/check contracts.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py tests/test_operations.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Provider/model intent is durable and inspectable server-side.
    - API never trusts browser-provided cost, credential, or provider-health claims.
    - Generated TypeScript contracts include the new fields.
  </acceptance_criteria>
  <done>API can safely accept and persist hosted provider intent.</done>
</task>

<task type="auto">
  <name>Task 3: Route worker generation from persisted provider intent with authoritative preflight</name>
  <read_first>
    - `services/worker/src/caragent_worker/tasks/jobs.py`
    - `services/worker/src/caragent_worker/providers/__init__.py`
    - `services/worker/tests/test_generation_tasks.py`
  </read_first>
  <files>services/worker/src/caragent_worker/tasks/jobs.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Add failing then passing worker tests proving persisted job provider/model intent overrides process defaults, unsupported hosted intent fails safely, `_enforce_hosted_preflight` runs immediately before provider calls, quota/rate/cost guards still apply, and local deterministic jobs bypass hosted quota checks.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Worker cannot make hosted calls unless all authoritative gates pass at execution time.
    - Local jobs still run without hosted credentials.
    - Process default provider remains a fallback only when job intent is absent.
  </acceptance_criteria>
  <done>Worker routing and preflight match Phase 9 hosted rollout gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser request to API | User-provided provider intent must be validated before queueing. |
| API queued job to worker execution | Worker must not trust stale API preflight decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Elevation of Privilege | hosted generation submit | mitigate | Validate provider/model intent against flags, credentials, capability map, quota, rate, and cost guard before enqueueing. |
| T-09-07 | Tampering | queued job metadata | mitigate | Worker normalizes and revalidates persisted provider intent before provider instantiation. |
| T-09-08 | Denial of Service | provider quota/rate | mitigate | Keep API and worker guard checks with tests for minute/daily limits. |
</threat_model>

<verification>
Run API generation/operations tests, worker generation tests, and `corepack pnpm contracts:check`.
</verification>

<success_criteria>
- V2-PROVIDER-02 is enforced at API and worker boundaries.
- V2-PROVIDER-01 config/capability data drives preflight.
- V2-PROVIDER-05 local deterministic path remains available.
</success_criteria>

<output>
After completion, create `.planning/phases/09-hosted-provider-rollout-mvp/09-03-SUMMARY.md`.
</output>

---
phase: 06-itasha-and-template-intelligence
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - "services/worker/src/caragent_worker/providers/local.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/worker/tests/test_image_providers.py"
autonomous: true
requirements: ["QUAL-02", "QUAL-03", "QUAL-05"]
must_haves:
  truths:
    - "Generated design versions expose PreviewSpec data through existing version parameters."
    - "Generated artifact metadata mirrors safe overlay/spec information without claiming production readiness."
    - "Missing-rights reference/logo assets remain blocked before generation."
  artifacts:
    - "services/worker/src/caragent_worker/tasks/jobs.py"
    - "services/worker/src/caragent_worker/providers/local.py"
  key_links:
    - "PromptPlan.prompt_payload.preview_spec -> DesignVersion.parameters.preview_spec"
    - "PromptPlan.prompt_payload.preview_spec -> Artifact.metadata.preview_spec"
    - "GenerationBriefPayload.reference_asset_ids and overlay_logo_asset_ids -> worker rights check"
---

<objective>
Persist Phase 6 PreviewSpec metadata through the worker generation pipeline and render deterministic concept-overlay evidence in the local provider where the existing local renderer can do so.

Purpose: Durable generated versions/artifacts must carry the spec that the web workbench and exports inspect.
Output: Worker metadata wiring, local provider overlay support, and focused worker tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-itasha-and-template-intelligence/06-CONTEXT.md
@.planning/phases/06-itasha-and-template-intelligence/06-RESEARCH.md
@services/worker/src/caragent_worker/tasks/jobs.py
@services/worker/src/caragent_worker/providers/local.py
@services/worker/tests/test_generation_tasks.py
@services/worker/tests/test_image_providers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify PreviewSpec persistence and overlay rendering behavior</name>
  <files>services/worker/tests/test_generation_tasks.py, services/worker/tests/test_image_providers.py</files>
  <behavior>
    - Successful generation stores `preview_spec` inside generated version parameters per D-13/D-15.
    - Generated artifact metadata includes `preview_spec`, warning summary, and `concept_label` per D-05/D-14.
    - Local deterministic provider draws exact requested text overlay labels on the concept preview image when text exists per D-04.
    - Any asset id used for reference or logo overlay must pass confirmed-rights checks per D-06/D-08.
  </behavior>
  <action>Add failing tests around `run_generate_2d_concept_job()` and `LocalDeterministicImageProvider.generate()` using the Phase 6 prompt payload from Plan 06-01. Assert that version parameters and artifact metadata contain PreviewSpec data, local provider metadata mirrors overlay/warning counts, the PNG output changes when overlay text changes, and missing-rights asset ids still fail before provider generation.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because worker artifacts do not yet persist `preview_spec`.
    - Rights-blocking expectations reuse the existing worker rights path.
  </acceptance_criteria>
  <done>Worker behavior is specified before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PreviewSpec through worker and local provider</name>
  <files>services/worker/src/caragent_worker/tasks/jobs.py, services/worker/src/caragent_worker/providers/local.py</files>
  <action>In `tasks/jobs.py`, read `preview_spec` from `prompt_plan.prompt_payload` and include it in `create_design_version(parameters=...)` and `create_artifact(metadata=...)` along with a warning summary and overlay count per D-05/D-13/D-15. Extend the rights check to cover both `reference_asset_ids` and `overlay_logo_asset_ids` from the brief/prompt payload per D-06/D-08. In `providers/local.py`, render deterministic text overlays from `preview_spec.overlay_layers` onto the existing side-car concept image and include metadata keys for `preview_spec`, `overlay_layer_count`, `safe_zone_count`, and `warning_count`; keep all labels as concept-preview metadata per D-04/D-05.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py tests/test_image_providers.py</automated>
    <automated>cd services/worker && uv run ruff check .</automated>
    <automated>cd services/worker && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Version parameters expose `preview_spec`.
    - Artifact metadata exposes `preview_spec` and warning/overlay counts.
    - Local rendering remains deterministic for the same prompt payload.
    - Missing-rights logo/reference asset ids are rejected before provider output.
  </acceptance_criteria>
  <done>Worker output carries durable Phase 6 spec data and deterministic overlay evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker to provider | Prompt payload becomes rendered image and metadata |
| Worker to database/storage | Provider output is persisted as artifact/version metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | Artifact metadata | mitigate | Persist PreviewSpec descriptors and ids only; never include local object storage roots or provider secrets. |
| T-06-05 | Repudiation | Overlay metadata | mitigate | Store overlay counts and source spec in version/artifact metadata for auditability. |
| T-06-06 | Elevation of Privilege | Logo/reference rights | mitigate | Enforce confirmed rights for all asset ids used by reference or overlay paths before generation. |
</threat_model>

<verification>
Run focused worker provider/task tests plus ruff and mypy for the worker service.
</verification>

<success_criteria>
- Worker generation persists PreviewSpec in existing JSON metadata.
- Local deterministic output reflects requested text overlays where possible.
- Rights enforcement remains blocking for unconfirmed assets.
</success_criteria>

<output>
After completion, create `.planning/phases/06-itasha-and-template-intelligence/06-02-SUMMARY.md`.
</output>

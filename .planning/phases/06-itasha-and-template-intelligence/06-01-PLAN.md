---
phase: 06-itasha-and-template-intelligence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "services/core/src/caragent_core/generation/briefs.py"
  - "services/core/src/caragent_core/generation/prompts.py"
  - "services/core/src/caragent_core/generation/templates.py"
  - "services/core/tests/test_generation_briefs.py"
  - "services/core/tests/test_prompt_plans.py"
autonomous: true
requirements: ["QUAL-01", "QUAL-03", "QUAL-04", "QUAL-05"]
must_haves:
  truths:
    - "Structured briefs can carry itasha controls without breaking existing brief defaults."
    - "Supported template safe zones and warnings are generated from core data, not browser-only state."
    - "Prompt payloads include a renderer-neutral PreviewSpec readable by worker/API consumers."
  artifacts:
    - "services/core/src/caragent_core/generation/briefs.py"
    - "services/core/src/caragent_core/generation/templates.py"
    - "services/core/src/caragent_core/generation/prompts.py"
  key_links:
    - "GenerationBriefPayload -> build_prompt_plan via PreviewSpec in prompt_payload"
    - "resolve_vehicle_template -> warnings and safe_zones in brief/template data"
---

<objective>
Extend the core generation contract with Phase 6 itasha controls, template safe-zone data, quality warnings, and PreviewSpec payload assembly.

Purpose: Core remains the single typed source for brief, template, warning, and renderer-neutral preview data.
Output: Updated core schemas/helpers and focused core tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-itasha-and-template-intelligence/06-CONTEXT.md
@.planning/phases/06-itasha-and-template-intelligence/06-UI-SPEC.md
@.planning/phases/06-itasha-and-template-intelligence/06-RESEARCH.md
@services/core/src/caragent_core/generation/briefs.py
@services/core/src/caragent_core/generation/templates.py
@services/core/src/caragent_core/generation/prompts.py
@services/core/tests/test_generation_briefs.py
@services/core/tests/test_prompt_plans.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify itasha controls, safe zones, warnings, and PreviewSpec</name>
  <files>services/core/tests/test_generation_briefs.py, services/core/tests/test_prompt_plans.py</files>
  <behavior>
    - Creating a brief with no Phase 6 fields keeps current defaults per D-03.
    - Creating a brief with character focus, supporting graphics, racing cues, typography intent, and color harmony preserves those values per D-01/D-02.
    - Unsupported template/view requests normalize to `generic-side-coupe` / `side` and preserve warning copy per D-10/D-12.
    - Prompt payload contains `preview_spec.canvas`, `preview_spec.template`, `preview_spec.safe_zones`, `preview_spec.overlay_layers`, and `preview_spec.warnings` per D-13/D-15.
  </behavior>
  <action>Add failing tests in `test_generation_briefs.py` and `test_prompt_plans.py` for the behavior above. Assert that existing Phase 3-5 payload fields remain present, new Phase 6 fields default to empty strings/lists as appropriate, safe-zone data is JSON-serializable, and warning strings are concise enough for the UI contract.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_generation_briefs.py tests/test_prompt_plans.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because Phase 6 fields and PreviewSpec are absent.
    - Tests reference QUAL-01, QUAL-03, QUAL-04, and QUAL-05 behaviors directly.
  </acceptance_criteria>
  <done>Core Phase 6 behavior is specified before production code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement core Phase 6 payload and template helpers</name>
  <files>services/core/src/caragent_core/generation/briefs.py, services/core/src/caragent_core/generation/templates.py, services/core/src/caragent_core/generation/prompts.py</files>
  <action>Extend `GenerationBriefPayload` with Phase 6 fields for `character_focus`, `supporting_graphics`, `racing_cues`, `typography_intent`, `color_harmony`, and `overlay_logo_asset_ids` per D-01/D-06. Update `create_generation_brief()` to accept these optional values while preserving existing defaults per D-03. In `templates.py`, add data-backed safe zones for `generic-side-coupe` / `side` with stable ids, labels, kinds, and normalized points/rectangles per D-10/D-11. Keep unsupported template/view normalization warnings per D-12. In `prompts.py`, add a `preview_spec` object to `prompt_payload` with canvas, template/view, safe zones, overlay descriptors derived from text/logo ids, warning strings, and source brief fields per D-13 through D-15.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_generation_briefs.py tests/test_prompt_plans.py</automated>
    <automated>cd services/core && uv run ruff check .</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Existing brief creation without Phase 6 fields still succeeds.
    - Safe-zone data is deterministic and tied only to the supported template/view.
    - PreviewSpec is present in prompt payload and includes warnings.
    - No new route or persistence table is introduced in this plan.
  </acceptance_criteria>
  <done>Core can produce Phase 6 PreviewSpec data for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/API to core schema | User-controlled text and asset ids become structured generation payload |
| Core prompt payload to worker/provider | Prompt metadata may be persisted and displayed later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `GenerationBriefPayload` | mitigate | Strip and validate string/list inputs with Pydantic validators; default optional Phase 6 fields safely. |
| T-06-02 | Spoofing | `resolve_vehicle_template()` | mitigate | Keep a single supported template/view and emit warnings when unsupported input is normalized. |
| T-06-03 | Information Disclosure | `preview_spec` | mitigate | Store asset ids and descriptors only; do not include local filesystem paths, API keys, or secrets. |
</threat_model>

<verification>
Run focused core tests, ruff, and mypy. Confirm tests cover QUAL-01, QUAL-03, QUAL-04, and QUAL-05.
</verification>

<success_criteria>
- Itasha fields are typed and default-compatible.
- Safe-zone and warning data come from core helpers.
- Prompt payload includes renderer-neutral PreviewSpec.
</success_criteria>

<output>
After completion, create `.planning/phases/06-itasha-and-template-intelligence/06-01-SUMMARY.md`.
</output>

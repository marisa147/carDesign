# Phase 33 Plan 01: Construction Package Export

## Tasks

1. Add an explicit `construction_package_zip` export format to the core supported export formats.
2. Extend the version export API to build an immutable ZIP package artifact containing:
   - `manifest.json`
   - `construction/layered.svg`
   - `construction/package.pdf`
   - `preview/source.png`
   - `warnings.md`
3. Preserve true source image artifact ids/object keys in package manifests even when the export artifact is the ZIP itself.
4. Add Web export controls for `施工包` with a clear quasi-construction disclaimer and readiness evidence.
5. Cover API package creation and existing export behavior with focused regression tests.

## Verification

- `uv run pytest -q tests/test_jobs.py -k "construction_package or concept_exports_can_be_created or enhanced_handoff_export"`
- `uv run ruff check src tests/test_jobs.py` in API and core services.
- `uv run mypy src` for API.
- `uv run pytest -q tests/test_jobs.py` for core export service coverage.
- `apps\web\node_modules\.bin\tsc.CMD --noEmit -p apps\web\tsconfig.json`

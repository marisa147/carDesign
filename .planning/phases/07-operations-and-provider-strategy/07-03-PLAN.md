---
phase: 07-operations-and-provider-strategy
plan: "03"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/src/caragent_worker/providers/base.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/worker/tests/test_image_providers.py"
  - "services/api/tests/test_operations.py"
autonomous: true
requirements: ["OPS-02", "OPS-04"]
must_haves:
  truths:
    - "Worker failures are categorized before being persisted."
    - "Structured events identify the failing stage and category."
    - "Secret redaction still applies to latest_error, model_run.error_message, and event/job metadata."
  artifacts:
    - "services/worker/src/caragent_worker/tasks/jobs.py"
    - "services/worker/tests/test_generation_tasks.py"
  key_links:
    - "provider exceptions -> FailureCategory"
    - "rights/storage/job validation exceptions -> durable failure metadata"
    - "operations recent failures -> classified worker events"
---

<objective>
Classify generation failures and emit structured worker events for provider, provider configuration, validation/rights, storage, timeout, queue/worker, canceled, and unknown failures.

Purpose: Operators and users need actionable failures instead of one generic failed state.
Output: Worker classification helpers, structured events, and tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-operations-and-provider-strategy/07-CONTEXT.md
@.planning/phases/07-operations-and-provider-strategy/07-RESEARCH.md
@.planning/phases/07-operations-and-provider-strategy/07-01-SUMMARY.md
@services/worker/src/caragent_worker/tasks/jobs.py
@services/worker/src/caragent_worker/providers/base.py
@services/worker/tests/test_generation_tasks.py
@services/worker/tests/test_image_providers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify worker failure categories</name>
  <files>services/worker/tests/test_generation_tasks.py, services/worker/tests/test_image_providers.py</files>
  <behavior>
    - `ImageProviderTimeoutError` becomes `timeout`.
    - `ImageProviderConfigurationError` becomes `provider_configuration`.
    - `ImageProviderError` becomes `provider`.
    - Missing rights and permission errors become `validation_rights`.
    - Object storage write failures become `storage`.
    - Unknown exceptions become `unknown`.
    - Failure metadata includes `stage`, `failure_category`, `provider`, `model`, and sanitized `error`.
  </behavior>
  <action>Add failing tests using existing fake providers and a failing object storage test double. Assert job status, latest error, model run error, final event metadata, and `job.metadata_json["operations"]` category for each failure class. Include a secret string in provider failure input and assert it is redacted everywhere.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py tests/test_image_providers.py -k "failure or timeout or rights or storage"</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because failures are generic.
    - Each category maps to OPS-02.
  </acceptance_criteria>
  <done>Failure classification behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement classification and structured failure events</name>
  <files>services/worker/src/caragent_worker/tasks/jobs.py, services/worker/src/caragent_worker/providers/base.py</files>
  <action>Add a small classification helper in the worker task module or provider base module. Use it in `_run_generate_2d_concept_job()` exception handling. Pass metadata to `jobs.transition_job_status()` with the failure category, stage, provider/model when known, current worker version, and sanitized error. Ensure `jobs.fail_model_run()` receives the same sanitized error. Add stage metadata before provider call, rights check, storage write, and terminal failure.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py tests/test_image_providers.py</automated>
    <automated>cd services/worker && uv run ruff check .</automated>
    <automated>cd services/worker && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Existing successful generation tests still pass.
    - Failure events include categories and stages.
    - Secrets are redacted consistently.
  </acceptance_criteria>
  <done>Worker writes classified operational failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify classified failures through operations API</name>
  <files>services/api/tests/test_operations.py</files>
  <behavior>
    - Recent failures returned by `/operations/provider-status` include `failure_category`, `message`, `job_id`, `provider`, `model`, `created_at`, and `stage` when available.
    - Missing metadata falls back to `unknown` without breaking older records.
  </behavior>
  <action>Extend operations API tests from Plan 07-02 by seeding failed jobs/events with metadata and without metadata. Assert categorized failures sort newest first and hide secret-like values.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if recent failures ignore `metadata`.
    - Older failures without metadata remain visible as `unknown`.
  </acceptance_criteria>
  <done>Operations API consumers can see failure categories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider/storage exceptions to durable ledger | External and local runtime errors become persisted metadata |
| Durable failures to operations API | Failure details become visible to operators and users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | worker failure metadata | mitigate | Redact provider secrets in all error sinks and test event/job/model-run payloads. |
| T-07-07 | Repudiation | failure events | mitigate | Store stage, category, provider, model, and worker version in terminal events. |
| T-07-08 | Tampering | category mapping | mitigate | Use core `FailureCategory` enum values and fallback to `unknown` only for unmapped exceptions. |
</threat_model>

<verification>
Run focused worker/API tests, ruff, and mypy.
</verification>

<success_criteria>
- Failures are classified durably.
- Recent failure summaries can use those categories.
- Secret redaction remains enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operations-and-provider-strategy/07-03-SUMMARY.md`.
</output>

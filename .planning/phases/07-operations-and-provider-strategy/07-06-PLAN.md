---
phase: 07-operations-and-provider-strategy
plan: "06"
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - "services/worker/src/caragent_worker/config.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/tests/test_config.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/operations.py"
  - "services/api/tests/test_operations.py"
autonomous: true
requirements: ["OPS-06", "OPS-03"]
must_haves:
  truths:
    - "Hosted provider calls are blocked before expensive execution when quota/rate controls reject them."
    - "Local deterministic generation bypasses monetary quota and records zero cost."
    - "Operations API exposes quota/rate guard configuration without secrets."
  artifacts:
    - "services/worker/src/caragent_worker/config.py"
    - "services/worker/src/caragent_worker/tasks/jobs.py"
    - "services/api/src/caragent_api/routes/operations.py"
  key_links:
    - "WorkerSettings quota fields -> preflight guard"
    - "ModelRun history -> hosted call counts"
    - "operations provider status -> quota summary"
---

<objective>
Add hosted-call quota and rate-limit preflight so non-local provider calls cannot run without explicit operational controls.

Purpose: Phase 7 must prevent accidental expensive calls while preserving local deterministic verification.
Output: Worker quota settings, preflight guard, operations summary, tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-operations-and-provider-strategy/07-CONTEXT.md
@.planning/phases/07-operations-and-provider-strategy/07-RESEARCH.md
@.planning/phases/07-operations-and-provider-strategy/07-05-SUMMARY.md
@services/worker/src/caragent_worker/config.py
@services/worker/src/caragent_worker/tasks/jobs.py
@services/api/src/caragent_api/routes/operations.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify quota and rate-limit settings</name>
  <files>services/worker/tests/test_config.py, services/api/tests/test_operations.py</files>
  <behavior>
    - Settings include hosted daily call limit, hosted per-minute rate limit, and max estimated cost per job.
    - Local defaults keep hosted provider calls guarded; local deterministic mode remains runnable.
    - Operations status reports guard values and whether hosted calls are blocked by missing guards.
  </behavior>
  <action>Add failing worker config tests for defaults and explicit env values. Extend operations API tests to assert quota summary fields such as `hosted_daily_call_limit`, `hosted_rate_limit_per_minute`, `max_estimated_cost_per_job`, and `hosted_calls_blocked_reason`.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_config.py -k "quota or rate"</automated>
    <automated>cd services/api && uv run pytest -q tests/test_operations.py -k quota</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before settings/API changes.
    - No secret or account identifier is included in quota summaries.
  </acceptance_criteria>
  <done>Quota/rate config expectations are specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement quota/rate settings and operations summary</name>
  <files>services/worker/src/caragent_worker/config.py, services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/routes/operations.py</files>
  <action>Add worker settings for hosted daily call limit, hosted per-minute rate limit, and max estimated cost per job with conservative defaults. Mirror safe summary fields in API settings or operations route as needed. Update `/operations/provider-status` to expose guard status without secrets.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_config.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_operations.py tests/test_openapi_export.py</automated>
    <automated>cd services/worker && uv run ruff check .</automated>
    <automated>cd services/api && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - Local deterministic mode reports quota as bypassed/zero-cost.
    - Hosted mode reports blocked when calls are enabled but limits are not set.
    - OpenAPI includes quota summary fields.
  </acceptance_criteria>
  <done>Operators can inspect quota/rate guard state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify hosted preflight guard</name>
  <files>services/worker/tests/test_generation_tasks.py</files>
  <behavior>
    - Hosted provider call is blocked before provider execution when hosted calls are enabled but quota/rate guards reject the request.
    - Daily call limit counts prior non-local successful or attempted model runs from durable state.
    - Per-minute rate limit counts recent non-local attempts.
    - Local deterministic generation bypasses hosted quota and records zero estimated/actual cost.
  </behavior>
  <action>Add failing worker tests with seeded prior model runs and a recording hosted provider. Assert provider is not called when guards reject, failure category is `provider_configuration` or a dedicated guard category if added, and local deterministic generation still succeeds.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py -k "quota or rate or hosted"</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before preflight guard exists.
    - Guard runs before expensive provider execution.
  </acceptance_criteria>
  <done>Hosted preflight behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 4: Implement hosted quota/rate preflight</name>
  <files>services/worker/src/caragent_worker/tasks/jobs.py</files>
  <action>Before each hosted provider attempt, check configured daily/rate/cost guards using durable model-run history. Treat local deterministic providers as quota-exempt and cost-zero. If the guard rejects, fail fast with structured metadata and do not call the provider. Keep the guard deterministic in tests by using database timestamps and existing model-run rows.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py tests/test_config.py</automated>
    <automated>cd services/worker && uv run ruff check .</automated>
    <automated>cd services/worker && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Hosted provider test double is not called when over quota/rate.
    - Guard failures are classified and visible in events/model runs.
    - Local deterministic smoke path remains green.
  </acceptance_criteria>
  <done>Hosted provider calls have preflight operational controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment quota config to worker provider execution | Operator settings gate expensive hosted calls |
| Durable model-run history to quota decisions | Historical usage influences whether new hosted calls run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Elevation of Privilege | hosted provider calls | mitigate | Require explicit hosted-call enablement plus quota/rate guards before external calls. |
| T-07-16 | Tampering | usage counting | mitigate | Count durable model runs from PostgreSQL instead of Redis/transient task state. |
| T-07-17 | Denial of Service | quota guard | mitigate | Keep local deterministic path quota-exempt so validation does not depend on hosted budget. |
</threat_model>

<verification>
Run focused worker/API tests, OpenAPI export, ruff, and mypy.
</verification>

<success_criteria>
- Hosted calls are guarded before execution.
- Local deterministic generation remains cost-zero and runnable.
- Operations API exposes guard state truthfully.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operations-and-provider-strategy/07-06-SUMMARY.md`.
</output>

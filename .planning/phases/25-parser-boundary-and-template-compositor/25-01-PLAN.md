# Phase 25 Plan: Parser Boundary And Template Compositor

## Scope

1. Add failing core tests for `BriefDraft`, deterministic parser fallback, and strict structured parser output.
2. Add failing worker tests for template-mask composition and local-provider compositor metadata.
3. Implement core `BriefParser`/`BriefDraft` and keep `create_generation_brief()` backed by the deterministic fallback path.
4. Add core `TemplateCompositor` protocol plus request/result dataclasses that do not require Pillow in core.
5. Implement a Pillow-based worker compositor that loads template `base`, `body_mask`, `window_mask`, `wheel_mask`, `handle_mask`, and `panel_lines` assets.
6. Route the local deterministic provider and deterministic recomposition helper through the template compositor.
7. Run focused core/worker tests, type checks, lint, and aggregate validation.

## Verification

- `cd services/core && uv run pytest -q tests/test_generation_briefs.py`
- `cd services/worker && uv run pytest -q tests/test_image_providers.py`
- `cd services/core && uv run ruff check src tests/test_generation_briefs.py && uv run mypy src`
- `cd services/worker && uv run ruff check src tests/test_image_providers.py && uv run mypy src`
- `corepack pnpm validate`
---
phase: 15
plan: 04
title: Legacy generic-side-coupe compatibility bridge
type: implementation
wave: 1
depends_on: [15-01, 15-02]
files_modified:
  - services/core/src/caragent_core/generation/templates.py
  - services/core/src/caragent_core/generation/briefs.py
  - services/core/src/caragent_core/generation/prompts.py
  - services/core/tests/test_generation_briefs.py
  - services/core/tests/test_prompt_plans.py
requirements:
  - V3-TEMPLATE-04
autonomous: true
---

<objective>
Preserve archived v1/v2 template behavior while adding v3 source/readiness metadata to new briefs and prompt payloads.
</objective>

<tasks>
1. Keep `SUPPORTED_TEMPLATE_ID` as `generic-side-coupe`.
2. Add `generic_coupe_side_v1` as an alias bridge to the legacy resolver.
3. Carry `template_source` and `template_readiness` in `GenerationBriefPayload`.
4. Include source/readiness in `vehicle_template` and `preview_spec.template`.
5. Update tests for brief and prompt payload compatibility.
</tasks>

<verification>
- `uv run pytest tests/test_generation_briefs.py tests/test_prompt_plans.py -q`
- Contract generation and drift check.
</verification>

<success_criteria>
- Existing unsupported-template fallback still resolves to `generic-side-coupe`.
- `generic_coupe_side_v1` resolves without warning through the compatibility bridge.
- New payloads contain source and readiness metadata.
</success_criteria>

<threat_model>
Compatibility must not silently reinterpret old payloads as production-ready assets. The bridge preserves concept-only behavior and reports readiness separately.
</threat_model>

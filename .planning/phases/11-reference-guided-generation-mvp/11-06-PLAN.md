---
phase: 11-reference-guided-generation-mvp
plan: "06"
type: execute
wave: 4
depends_on: ["11-02", "11-03", "11-04", "11-05"]
files_modified:
  - "apps/web/src/components/workbench/asset-panel.tsx"
  - "apps/web/src/components/workbench/parameter-panel.tsx"
  - "apps/web/src/components/workbench/progress-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/components/workbench/comparison-panel.tsx"
  - "apps/web/src/lib/api/generation.ts"
  - "apps/web/src/lib/api/operations.ts"
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/lib/workbench/store.test.ts"
  - "apps/web/src/lib/api/generation.test.ts"
  - "apps/web/src/lib/api/iteration.test.ts"
autonomous: true
requirements: ["V2-REF-01", "V2-REF-02", "V2-REF-03", "V2-REF-04", "V2-REF-05"]
must_haves:
  truths:
    - "The workbench shows what reference roles are selected, eligible, unsupported, and used."
    - "Generation and child iteration reuse current reference usage unless the user changes it."
    - "Reference failures and warnings are visible without exposing secrets."
  artifacts:
    - "Generation submit path includes structured reference usage."
    - "Child iteration path preserves current brief reference usage."
    - "Progress/history surfaces show durable reference warnings and usage evidence."
  key_links:
    - "11-UI-SPEC -> Interaction Contract and Progress And History"
    - "11-VALIDATION -> 11-06 web/unit checks"
---

<objective>
Complete the reference-guided generation UX, failure states, and child-iteration reuse path.

Purpose: The user should understand and reuse reference guidance across initial generation and follow-up iterations, with clear warning/failure states.
Output: Tested web workflow for role assignment, provider warnings, generation submission, child iteration reuse, progress diagnostics, and version reference trace display.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-reference-guided-generation-mvp/11-CONTEXT.md
@.planning/phases/11-reference-guided-generation-mvp/11-UI-SPEC.md
@.planning/phases/11-reference-guided-generation-mvp/11-PATTERNS.md
@apps/web/src/components/workbench/asset-panel.tsx
@apps/web/src/components/workbench/parameter-panel.tsx
@apps/web/src/components/workbench/progress-panel.tsx
@apps/web/src/components/workbench/workbench-app.tsx
@apps/web/src/components/workbench/comparison-panel.tsx
@apps/web/src/lib/api/generation.ts
@apps/web/src/lib/api/operations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red web tests for end-to-end reference UX</name>
  <read_first>
    - `apps/web/src/app/page.test.tsx`
    - `apps/web/src/lib/api/generation.test.ts`
    - `apps/web/src/lib/api/iteration.test.ts`
  </read_first>
  <files>apps/web/src/app/page.test.tsx, apps/web/src/lib/api/generation.test.ts, apps/web/src/lib/api/iteration.test.ts, apps/web/src/lib/workbench/store.test.ts</files>
  <action>Add failing tests for: selecting a confirmed asset as `角色`, seeing `可用于生成`; selecting an unsupported role with BFL status and seeing `引用受限`; submitting generation with `reference_usage`; submitting a child iteration that reuses the current brief's reference usage; and viewing a generated version with `reference_usage` metadata in a compact trace row.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/workbench/store.test.ts src/lib/api/generation.test.ts src/lib/api/iteration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation for missing warning/trace UI.
    - Tests do not require a live browser or hosted provider.
    - Tests assert request bodies include `reference_usage` exactly once.
  </acceptance_criteria>
  <done>End-to-end reference workbench behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Submit structured reference usage for generation and iteration</name>
  <read_first>
    - `apps/web/src/components/workbench/workbench-app.tsx`
    - `apps/web/src/lib/api/generation.ts`
    - `apps/web/src/lib/api/iteration.ts`
  </read_first>
  <files>apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/lib/api/generation.ts, apps/web/src/lib/api/iteration.ts, apps/web/src/lib/api/generation.test.ts, apps/web/src/lib/api/iteration.test.ts</files>
  <action>Ensure the initial generation brief save path and child iteration path use the current structured reference assignments from the active brief/workbench state. Do not add a separate reference-only submission endpoint. Preserve provider intent payload behavior from Phase 9 and targeted edit behavior from Phase 10.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/lib/api/generation.test.ts src/lib/api/iteration.test.ts src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Generation submit request includes `reference_usage` when references are selected.
    - Child iteration does not clear or mutate reference usage unless the brief was changed.
    - Provider intent and targeted edit fields remain present when applicable.
  </acceptance_criteria>
  <done>Generation and iteration reuse structured references consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Display reference warnings and trace evidence</name>
  <read_first>
    - `apps/web/src/components/workbench/progress-panel.tsx`
    - `apps/web/src/components/workbench/comparison-panel.tsx`
    - `apps/web/src/lib/api/operations.ts`
  </read_first>
  <files>apps/web/src/components/workbench/progress-panel.tsx, apps/web/src/components/workbench/comparison-panel.tsx, apps/web/src/components/workbench/parameter-panel.tsx, apps/web/src/lib/api/operations.ts, apps/web/src/app/page.test.tsx</files>
  <action>Add compact UI rows for reference warning categories and version reference trace. Use sanitized fields from job events, provider status, and selected version parameters. Display role counts and warning counts, not raw long JSON. Keep existing targeted comparison UI intact.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - UI shows `引用受限` when unsupported reference warnings exist.
    - Version trace row shows role counts or included reference count when `reference_usage` metadata exists.
    - No visible text overflows buttons or compact panels in test-rendered strings.
  </acceptance_criteria>
  <done>Users can inspect reference warnings and generated-version trace evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Polish failure/empty/loading states for references</name>
  <read_first>
    - `apps/web/src/components/workbench/asset-panel.tsx`
    - `apps/web/src/components/workbench/parameter-panel.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/asset-panel.tsx, apps/web/src/components/workbench/parameter-panel.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Finalize empty asset copy, loading state, rights save error state, and unsupported provider reference state using the UI-SPEC labels. Ensure role controls and warning badges wrap on narrow layouts without hidden role labels.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Empty state mentions reference roles without becoming tutorial prose.
    - Rights save error remains inline and destructive.
    - Role labels remain accessible on mobile-sized rendered layouts.
  </acceptance_criteria>
  <done>Reference UX states are polished and test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workbench state to API request | Client state may be stale, duplicated, or in conflict with provider status. |
| Job event metadata to UI | Diagnostic data must be sanitized and presented without secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Tampering | generation/iteration payload | mitigate | Build payload from structured assignments and backend-generated contracts, with focused tests. |
| T-11-17 | Repudiation | reference usage display | mitigate | Show durable version/job reference trace evidence after generation. |
| T-11-18 | Information Disclosure | warning diagnostics | mitigate | Use sanitized provider/worker warning fields and never render secrets or local paths. |
</threat_model>

<verification>
Run focused web workbench, store, generation API, and iteration API Vitest files.
</verification>

<success_criteria>
- Users can assign, submit, and reuse reference roles through the workbench.
- Unsupported and ineligible reference states are visible before and after submission.
- Generated versions expose reference usage trace without breaking targeted edit comparison.
</success_criteria>

<output>
After completion, create `.planning/phases/11-reference-guided-generation-mvp/11-06-SUMMARY.md`.
</output>

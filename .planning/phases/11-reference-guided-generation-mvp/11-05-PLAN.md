---
phase: 11-reference-guided-generation-mvp
plan: "05"
type: execute
wave: 3
depends_on: ["11-03", "11-04"]
files_modified:
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/core/src/caragent_core/references.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "apps/web/src/components/workbench/export-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/api/tests/test_jobs.py"
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/lib/api/iteration.test.ts"
autonomous: true
requirements: ["V2-REF-05"]
must_haves:
  truths:
    - "Reference usage evidence is durable across model runs, artifacts, versions, jobs, and export manifest source data."
    - "The same snapshot shape is reused across persistence surfaces."
    - "No binary upload data, secrets, or mutable rights assumptions are embedded in metadata."
  artifacts:
    - "Reference trace helper for model-run/artifact/version/job metadata."
    - "Worker tests asserting matching reference usage keys across durable rows."
    - "Export manifest source trace that Phase 13 can package later."
  key_links:
    - "11-CONTEXT -> Persistence And Traceability"
    - "11-RESEARCH -> Persistence Surfaces"
    - "ROADMAP -> V2 data additions ReferenceUsageSnapshot"
---

<objective>
Persist reference usage trace across generation records and export manifest source data.

Purpose: Phase 11 must prove exactly which reference assets, roles, provider decisions, and rights/source snapshots were used when a concept was generated.
Output: Durable, test-covered `reference_usage` metadata on model runs, artifacts, design versions, job events/metadata, and concept export manifests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-reference-guided-generation-mvp/11-CONTEXT.md
@.planning/phases/11-reference-guided-generation-mvp/11-RESEARCH.md
@.planning/phases/11-reference-guided-generation-mvp/11-VALIDATION.md
@services/worker/src/caragent_worker/tasks/jobs.py
@services/core/src/caragent_core/models.py
@services/core/src/caragent_core/services/jobs.py
@services/api/src/caragent_api/routes/jobs.py
@apps/web/src/components/workbench/export-panel.tsx
@apps/web/src/components/workbench/workbench-app.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for durable reference trace metadata</name>
  <read_first>
    - `services/worker/tests/test_generation_tasks.py`
    - `services/api/tests/test_jobs.py`
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>services/worker/tests/test_generation_tasks.py, services/api/tests/test_jobs.py, apps/web/src/app/page.test.tsx, apps/web/src/lib/api/iteration.test.ts</files>
  <action>Add failing tests that execute a local provider-off generation with confirmed reference assets and assert `reference_usage`, `reference_roles`, `rights_snapshot`, `included_reference_asset_ids`, `omitted_reference_asset_ids`, `unsupported_reference_roles`, and `reference_warning_count` appear on `ModelRun.parameters`, `ModelRun.prompt_payload`, generated `Artifact.metadata_json`, `DesignVersion.parameters`, and job completion metadata. Add a web export test asserting the submitted manifest includes `reference_usage` from the selected version.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/api/iteration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because trace metadata is missing or inconsistent.
    - Tests compare exact keys, not only generic metadata presence.
    - Tests assert no `image_bytes`, `api_key`, or `secret` string appears in reference metadata.
  </acceptance_criteria>
  <done>Reference trace persistence behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create a single reference trace metadata helper</name>
  <read_first>
    - `services/core/src/caragent_core/references.py`
    - `services/worker/src/caragent_worker/tasks/jobs.py`
  </read_first>
  <files>services/core/src/caragent_core/references.py, services/worker/src/caragent_worker/tasks/jobs.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Add a helper that converts a `ReferenceUsageSnapshot` into flat metadata keys: `reference_usage`, `reference_roles`, `rights_snapshot`, `included_reference_asset_ids`, `omitted_reference_asset_ids`, `unsupported_reference_roles`, and `reference_warning_count`. Use this helper everywhere persistence metadata is assembled.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Worker code does not hand-build different reference metadata shapes in multiple places.
    - Metadata values are JSON serializable.
    - Rights snapshot fields are copied, not linked to mutable asset ORM objects.
  </acceptance_criteria>
  <done>A single helper emits durable reference trace metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Persist reference trace on generation outputs</name>
  <read_first>
    - `services/worker/src/caragent_worker/tasks/jobs.py`
    - `services/core/src/caragent_core/services/jobs.py`
  </read_first>
  <files>services/worker/src/caragent_worker/tasks/jobs.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Merge reference trace metadata into model-run parameters, model-run prompt payload, generated artifact metadata, design version parameters, job events, and job completion metadata. Preserve existing provider, preview, targeted-edit, cost, fallback, and lineage fields.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Existing targeted edit and provider trace tests still pass.
    - Reference metadata appears on parent generation and child iteration outputs.
    - Parent version/artifact data remains immutable.
  </acceptance_criteria>
  <done>Generation outputs carry durable reference evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Include reference trace in concept export manifest source</name>
  <read_first>
    - `apps/web/src/components/workbench/workbench-app.tsx`
    - `apps/web/src/components/workbench/export-panel.tsx`
    - `apps/web/src/lib/api/iteration.ts`
  </read_first>
  <files>apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/components/workbench/export-panel.tsx, apps/web/src/app/page.test.tsx, apps/web/src/lib/api/iteration.test.ts</files>
  <action>When creating a concept export for a selected version, include `reference_usage` and `reference_warning_count` from `selectedVersion.parameters` in the submitted manifest if present. Keep the existing concept-only disclaimer and source artifact fields. Do not implement the Phase 13 ZIP package yet.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/api/iteration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Export manifest includes selected version id, source artifact object key, disclaimer, and reference usage trace.
    - Export still works when selected version has no reference metadata.
    - Test asserts the manifest does not include binary image data.
  </acceptance_criteria>
  <done>Phase 13 can later package reference trace from export manifest data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generation output metadata | Evidence must be accurate and not mutable by later asset edits. |
| Export manifest source data | User-facing export records must not leak secrets or binary payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Repudiation | reference evidence | mitigate | Store the same snapshot on model run, artifact, version, and job metadata with tests. |
| T-11-14 | Information Disclosure | export manifest | mitigate | Include reference IDs/roles/rights snapshot only; exclude binary bytes, local paths, and secrets. |
| T-11-15 | Tampering | mutable asset rights | mitigate | Snapshot rights/source values at generation time instead of reading current asset state later. |
</threat_model>

<verification>
Run focused worker generation tests, API job tests, and web export/iteration tests.
</verification>

<success_criteria>
- V2-REF-05 is represented across generation and export-facing trace surfaces.
- Reference trace metadata is stable, versioned, and JSON serializable.
- No binary data or secrets are persisted in reference trace metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/11-reference-guided-generation-mvp/11-05-SUMMARY.md`.
</output>

---
phase: 11-reference-guided-generation-mvp
plan: "03"
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - "services/core/src/caragent_core/references.py"
  - "services/core/src/caragent_core/generation/briefs.py"
  - "services/core/src/caragent_core/generation/prompts.py"
  - "services/api/src/caragent_api/routes/generation.py"
  - "services/core/tests/test_prompt_plans.py"
  - "services/core/tests/test_generation_jobs.py"
  - "services/api/tests/test_generation.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
autonomous: true
requirements: ["V2-REF-03", "V2-REF-04", "V2-REF-05"]
must_haves:
  truths:
    - "Prompt planning decides which requested references are included, omitted, or warning-only."
    - "Unsupported provider/model reference roles become clear warnings instead of silent drops."
    - "Prompt payload and input artifact IDs remain deterministic and JSON serializable."
  artifacts:
    - "Reference planner helper that normalizes legacy and structured reference usage."
    - "Prompt payload fields for `reference_usage`, included IDs, omitted IDs, and warnings."
    - "API tests for unsupported reference role warnings."
  key_links:
    - "11-RESEARCH -> Prompt And Request Planning"
    - "11-VALIDATION -> 11-03 core/API checks"
---

<objective>
Integrate reference usage into prompt planning and unsupported-capability warnings.

Purpose: Role assignments should become deterministic prompt/request metadata, and unsupported roles must be visible before a provider call can silently ignore them.
Output: Core prompt planning and API behavior that produce role-aware `reference_usage` payloads, included reference IDs, omitted reference IDs, and warning metadata.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-reference-guided-generation-mvp/11-CONTEXT.md
@.planning/phases/11-reference-guided-generation-mvp/11-RESEARCH.md
@.planning/phases/11-reference-guided-generation-mvp/11-PATTERNS.md
@services/core/src/caragent_core/generation/briefs.py
@services/core/src/caragent_core/generation/prompts.py
@services/core/src/caragent_core/provider_capabilities.py
@services/api/src/caragent_api/routes/generation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for role-aware prompt planning</name>
  <read_first>
    - `services/core/tests/test_prompt_plans.py`
    - `services/api/tests/test_generation.py`
  </read_first>
  <files>services/core/tests/test_prompt_plans.py, services/api/tests/test_generation.py</files>
  <action>Add failing tests that build a brief with `reference_usage` containing `character`, `palette`, and `inspiration` roles. Assert `build_prompt_plan()` writes `prompt_payload[\"reference_usage\"]`, role-specific prompt text, `included_reference_asset_ids`, `omitted_reference_asset_ids`, `unsupported_reference_roles`, and `reference_warning_count`. Assert legacy `reference_asset_ids` produces a deterministic default `reference_usage` entry.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_prompt_plans.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because prompt payload lacks `reference_usage`.
    - Tests assert unsupported roles are warnings, not silent omissions.
    - Tests assert prompt plan output is deterministic across repeated calls.
  </acceptance_criteria>
  <done>Prompt planner reference behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build reference planner normalization helper</name>
  <read_first>
    - `services/core/src/caragent_core/references.py`
    - `services/core/src/caragent_core/generation/briefs.py`
  </read_first>
  <files>services/core/src/caragent_core/references.py, services/core/src/caragent_core/generation/briefs.py, services/core/tests/test_prompt_plans.py</files>
  <action>Add a helper that converts structured `reference_usage` plus legacy `reference_asset_ids` into a normalized list of requested reference items. Use a single deterministic legacy default role and document it in code/test names. Deduplicate by `(asset_id, role)` while preserving stable ordering.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_prompt_plans.py</automated>
  </verify>
  <acceptance_criteria>
    - Duplicate assignments do not duplicate `input_artifact_ids`.
    - Disabled assignments do not become included generation references.
    - Legacy IDs remain accepted and visible in prompt payload.
  </acceptance_criteria>
  <done>Reference assignments are normalized before prompt planning.</done>
</task>

<task type="auto">
  <name>Task 3: Add role-aware reference usage to prompt plans</name>
  <read_first>
    - `services/core/src/caragent_core/generation/prompts.py`
    - `services/core/src/caragent_core/provider_capabilities.py`
  </read_first>
  <files>services/core/src/caragent_core/generation/prompts.py, services/core/src/caragent_core/references.py, services/core/tests/test_prompt_plans.py</files>
  <action>Update `build_prompt_plan()` so `prompt_text` names selected reference roles, `prompt_payload` includes a versioned `reference_usage` object, `input_artifact_ids` contains only included generation reference assets, and unsupported/prompt-only roles are recorded in warning fields. Keep `preview_spec.sources.reference_asset_ids` backward-compatible.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_prompt_plans.py tests/test_generation_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - `PromptPlan.input_artifact_ids` is deterministic and deduplicated.
    - Prompt text includes role labels without embedding binary data.
    - `prompt_payload.reference_usage.schema_version` equals `1`.
  </acceptance_criteria>
  <done>Prompt planning carries structured reference usage.</done>
</task>

<task type="auto">
  <name>Task 4: Surface unsupported-role warnings through API contracts</name>
  <read_first>
    - `services/api/src/caragent_api/routes/generation.py`
    - `services/api/src/caragent_api/schemas.py`
  </read_first>
  <files>services/api/src/caragent_api/routes/generation.py, services/api/src/caragent_api/schemas.py, services/api/tests/test_generation.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Ensure brief create/update and generation submission can surface reference warnings generated from provider capability metadata. Use stable warning strings or structured warning objects containing `role`, `asset_id`, and `reason`. Regenerate/check contracts after schema changes.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - API tests can assert unsupported-role warnings by exact key or exact warning text.
    - Existing provider intent behavior from Phase 9 remains intact.
    - Generated TypeScript contracts remain current.
  </acceptance_criteria>
  <done>Unsupported reference usage is visible to API and web clients.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brief payload to prompt plan | User-selected references may be stale, duplicated, disabled, or unsupported. |
| Prompt plan to provider request | Unsupported roles must not be sent as if supported by a provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | prompt reference payload | mitigate | Normalize and deduplicate assignments with typed helpers and deterministic tests. |
| T-11-08 | Repudiation | unsupported reference omission | mitigate | Persist omitted IDs, unsupported roles, and warning counts in prompt payload. |
| T-11-09 | Information Disclosure | prompt text | mitigate | Include asset IDs/roles only; do not embed binary data, local file paths, or provider secrets. |
</threat_model>

<verification>
Run focused core prompt tests, API generation tests, and `corepack pnpm contracts:check`.
</verification>

<success_criteria>
- V2-REF-03 provider-supported reference filtering is represented in prompt/request metadata.
- V2-REF-04 unsupported role warnings are visible and testable.
- V2-REF-05 prompt payload includes exact reference usage trace fields.
</success_criteria>

<output>
After completion, create `.planning/phases/11-reference-guided-generation-mvp/11-03-SUMMARY.md`.
</output>

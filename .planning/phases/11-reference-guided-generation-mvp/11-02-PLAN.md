---
phase: 11-reference-guided-generation-mvp
plan: "02"
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - "apps/web/src/components/workbench/asset-panel.tsx"
  - "apps/web/src/components/workbench/parameter-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/lib/api/generation.ts"
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/lib/api/generation.test.ts"
  - "apps/web/src/lib/api/assets.test.ts"
autonomous: true
requirements: ["V2-REF-01", "V2-REF-02", "V2-REF-04"]
must_haves:
  truths:
    - "Users assign a generation role to each selected asset instead of editing raw IDs as the primary flow."
    - "Rights-missing assets are visible but not generation-eligible."
    - "Provider unsupported-role warnings are visible before submit."
  artifacts:
    - "Asset-panel role controls for the six allowed roles."
    - "Workbench state that submits structured `reference_usage`."
    - "Parameter/provider warning summary for selected references."
  key_links:
    - "11-UI-SPEC -> Asset Tab, Parameters Tab, Provider Selector"
    - "11-CONTEXT -> Workbench UX"
    - "11-PATTERNS -> Web Patterns"
---

<objective>
Upgrade the workbench asset and parameter UI for role-based reference selection.

Purpose: Users need to mark uploaded assets as character, style, vehicle, logo, palette, or inspiration references and understand rights/provider eligibility before generation.
Output: Tested workbench UI controls and payload construction for structured `reference_usage`.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-reference-guided-generation-mvp/11-CONTEXT.md
@.planning/phases/11-reference-guided-generation-mvp/11-UI-SPEC.md
@.planning/phases/11-reference-guided-generation-mvp/11-PATTERNS.md
@apps/web/src/components/workbench/asset-panel.tsx
@apps/web/src/components/workbench/parameter-panel.tsx
@apps/web/src/components/workbench/workbench-app.tsx
@apps/web/src/lib/api/operations.ts
@apps/web/src/app/page.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red workbench tests for role assignment and eligibility</name>
  <read_first>
    - `apps/web/src/app/page.test.tsx`
    - `apps/web/src/lib/api/generation.test.ts`
    - `apps/web/src/lib/api/assets.test.ts`
  </read_first>
  <files>apps/web/src/app/page.test.tsx, apps/web/src/lib/api/generation.test.ts, apps/web/src/lib/api/assets.test.ts</files>
  <action>Add failing tests that load one missing-rights asset and one confirmed-rights asset. Assert the UI renders role labels `角色`, `风格`, `车辆`, `Logo`, `配色`, `仅灵感`, disables generation eligibility for the missing-rights asset, shows `引用素材需要权利确认`, and submits `reference_usage` with `{asset_id, role, enabled}` for the confirmed asset after role selection.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/api/generation.test.ts src/lib/api/assets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because role controls and `reference_usage` payload are absent.
    - Tests do not depend on a live API server.
    - Tests assert missing-rights assets cannot become enabled generation references.
  </acceptance_criteria>
  <done>Role assignment and eligibility behavior is specified by failing web tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add structured reference state to WorkbenchApp</name>
  <read_first>
    - `apps/web/src/components/workbench/workbench-app.tsx`
    - `apps/web/src/lib/api/generation.ts`
  </read_first>
  <files>apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/lib/api/generation.ts</files>
  <action>Replace flat selected reference state as the primary source with structured assignments shaped as `{asset_id: string, role: string, enabled: boolean}`. Derive assignments from `currentBrief.payload.reference_usage` when present, otherwise from legacy `reference_asset_ids` using a documented default role. Keep legacy `selectedReferenceAssetIds` derivation available for compatibility until all panels use structured assignments.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/api/generation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `WorkbenchApp` can initialize role assignments from a resumed brief.
    - Existing briefs with only `reference_asset_ids` still show selected references.
    - Submit/update payloads include `reference_usage` when structured assignments exist.
  </acceptance_criteria>
  <done>Workbench has structured reference assignment state.</done>
</task>

<task type="auto">
  <name>Task 3: Implement asset role controls and rights eligibility badges</name>
  <read_first>
    - `apps/web/src/components/workbench/asset-panel.tsx`
    - `.planning/phases/11-reference-guided-generation-mvp/11-UI-SPEC.md`
  </read_first>
  <files>apps/web/src/components/workbench/asset-panel.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Update `AssetPanel` so each asset row has a role select/control with the six UI labels, an include checkbox/toggle that is disabled when `asset.rights_status !== \"confirmed\"`, and an eligibility badge using the UI-SPEC labels `可用于生成`, `需确认权利`, `供应商不支持`, or `仅记录`. Keep rights/source save behavior intact.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Every role control has an accessible label containing the asset filename.
    - Missing-rights assets render `需确认权利` and cannot be enabled.
    - Confirmed assets can be assigned any of the six roles.
  </acceptance_criteria>
  <done>Users can assign roles and see rights eligibility in the asset panel.</done>
</task>

<task type="auto">
  <name>Task 4: Add parameter/provider reference warning summary</name>
  <read_first>
    - `apps/web/src/components/workbench/parameter-panel.tsx`
    - `apps/web/src/lib/api/operations.ts`
  </read_first>
  <files>apps/web/src/components/workbench/parameter-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Add a compact reference summary in `ParameterPanel` that lists selected role counts, displays `引用受限` when selected provider capability reports unsupported reference roles, and displays `引用素材需要权利确认` when enabled assignments are not eligible. Keep provider selector layout stable and retain the raw ID textarea only as compatibility/debug UI if needed.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Provider change recomputes selected-reference warnings.
    - Warning copy matches `11-UI-SPEC.md`.
    - No role warning relies on color alone.
  </acceptance_criteria>
  <done>Reference eligibility and provider support are visible before generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset UI to generation payload | UI state is user-controlled and may include stale or ineligible assets. |
| Provider status to warning copy | Capability metadata can be absent or stale and must fail safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Spoofing | enabled reference controls | mitigate | Disable and test generation eligibility for assets without confirmed rights. |
| T-11-05 | Repudiation | UI reference selection | mitigate | Submit structured `reference_usage` with role and enabled fields, not a hidden raw ID list only. |
| T-11-06 | Information Disclosure | provider warning display | mitigate | Reuse sanitized provider capability strings and do not surface secrets or local file paths. |
</threat_model>

<verification>
Run focused workbench Vitest files for asset, generation payload, and page flows.
</verification>

<success_criteria>
- V2-REF-01 role assignment is available in the asset library.
- V2-REF-02 rights-missing references are visibly ineligible.
- V2-REF-04 unsupported/ineligible reference warnings are visible before submit.
</success_criteria>

<output>
After completion, create `.planning/phases/11-reference-guided-generation-mvp/11-02-SUMMARY.md`.
</output>

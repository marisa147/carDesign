---
phase: 12-lightweight-3d-preview-mvp
plan: "04"
type: execute
wave: 2
depends_on: ["12-03-PLAN.md"]
files_modified:
  - "apps/web/src/lib/preview3d/spec.ts"
  - "apps/web/src/lib/preview3d/materials.ts"
  - "apps/web/src/components/workbench/preview-3d-viewer.tsx"
  - "apps/web/src/components/workbench/preview-3d-panel.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["V2-3D-01", "V2-3D-02", "V2-3D-03"]
must_haves:
  truths:
    - "Texture/material mapping is PreviewSpec-driven and does not claim verified UV accuracy."
    - "Safe-zone and overlay projection uses bounded normalized coordinates."
    - "Generated artifact metadata is linked as source evidence even if binary image serving is absent."
  artifacts:
    - "Material/decal plan helper."
    - "3D viewer uses PreviewSpec overlays/safe zones on the lightweight shell."
    - "Tests for material plan and visible warning copy."
  key_links:
    - "12-RESEARCH -> Preview3DSpec Shape and Shell Fixture Strategy"
    - "12-UI-SPEC -> Persistent Labels"
---

<objective>
Map existing PreviewSpec canvas, safe zones, overlay layers, and selected artifact metadata onto the lightweight shell material plan.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-lightweight-3d-preview-mvp/12-CONTEXT.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-RESEARCH.md
@apps/web/src/components/workbench/preview-panel.tsx
@apps/web/src/lib/preview3d/spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for material/decal projection</name>
  <read_first>
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>apps/web/src/app/page.test.tsx, apps/web/src/lib/preview3d/materials.ts</files>
  <action>Add failing tests for converting PreviewSpec safe zones and overlay layers into normalized material/decal entries. Assert source artifact id/object key appears as evidence and warning text prevents UV-accuracy claims.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover at least one safe zone and one overlay layer.
    - Tests assert coordinates are bounded from 0 to 1.
    - Tests assert `uv_not_verified` warning remains present.
  </acceptance_criteria>
  <done>Material mapping expectations are specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PreviewSpec material plan helper</name>
  <read_first>
    - `apps/web/src/lib/preview3d/spec.ts`
    - `apps/web/src/components/workbench/preview-panel.tsx`
  </read_first>
  <files>apps/web/src/lib/preview3d/materials.ts, apps/web/src/lib/preview3d/spec.ts</files>
  <action>Create a pure helper that maps PreviewSpec canvas coordinates to shell side material coordinates. Include overlay labels, safe-zone metadata, source artifact metadata, and warning ids. Clamp or omit invalid shapes safely.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Mapping is deterministic for current fixtures.
    - Invalid or missing canvas data falls back without throwing.
    - No binary image content is created in metadata.
  </acceptance_criteria>
  <done>PreviewSpec can drive a 3D material/decal plan.</done>
</task>

<task type="auto">
  <name>Task 3: Render material plan in the 3D viewer</name>
  <read_first>
    - `apps/web/src/components/workbench/preview-3d-viewer.tsx`
    - `apps/web/src/components/workbench/preview-3d-panel.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/preview-3d-viewer.tsx, apps/web/src/components/workbench/preview-3d-panel.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Draw deterministic shell material/decal markers from the material plan. Keep labels readable and bounded. Preserve non-production label and fallback state if material plan cannot be built.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Viewer uses selected version's PreviewSpec-derived material plan.
    - Safe-zone overlay can be toggled or remains clearly represented according to UI implementation.
    - No UI text overlaps controls in tested fixtures.
  </acceptance_criteria>
  <done>3D preview visually reflects PreviewSpec safe zones and overlays.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | material coordinates | mitigate | Normalize and clamp coordinates; ignore invalid PreviewSpec items. |
| T-12-06 | Misrepresentation | 3D material mapping | mitigate | Keep UV-not-verified warning visible and metadata-backed. |
</threat_model>

<verification>
Run focused web tests and typecheck.
</verification>

<success_criteria>
- The 3D shell reflects selected PreviewSpec safe zones and overlay layers.
- Source artifact metadata is linked as evidence.
- Viewer still avoids production UV claims.
</success_criteria>

<output>
After completion, create `.planning/phases/12-lightweight-3d-preview-mvp/12-04-SUMMARY.md`.
</output>

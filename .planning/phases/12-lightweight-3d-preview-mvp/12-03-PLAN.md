---
phase: 12-lightweight-3d-preview-mvp
plan: "03"
type: execute
wave: 2
depends_on: ["12-01-PLAN.md", "12-02-PLAN.md"]
files_modified:
  - "apps/web/package.json"
  - "pnpm-lock.yaml"
  - "apps/web/src/lib/workbench/store.ts"
  - "apps/web/src/lib/workbench/store.test.ts"
  - "apps/web/src/components/workbench/preview-panel.tsx"
  - "apps/web/src/components/workbench/preview-3d-panel.tsx"
  - "apps/web/src/components/workbench/preview-3d-viewer.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["V2-3D-02", "V2-3D-03", "V2-3D-05"]
must_haves:
  truths:
    - "Three.js loads only in the browser and only for the 3D preview surface."
    - "2D preview remains usable when 3D is disabled or incompatible."
    - "Camera state has resettable, bounded controls."
  artifacts:
    - "3D mode switch and viewer scaffold."
    - "Store state for preview mode/camera reset or equivalent component state."
    - "Web tests for 3D open/fallback/control labels."
  key_links:
    - "12-UI-SPEC -> Layout Contract and Controls"
    - "12-RESEARCH -> Frontend Rendering Strategy"
    - "12-PATTERNS -> Web Workbench"
---

<objective>
Add the frontend 3D preview surface, client-only Three.js viewer scaffold, camera controls, and fallback states.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-lightweight-3d-preview-mvp/12-UI-SPEC.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-PATTERNS.md
@apps/web/src/components/workbench/preview-panel.tsx
@apps/web/src/lib/workbench/store.ts
@apps/web/src/app/page.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red web tests for opening 3D preview and fallback</name>
  <read_first>
    - `apps/web/src/app/page.test.tsx`
    - `apps/web/src/lib/workbench/store.test.ts`
  </read_first>
  <files>apps/web/src/app/page.test.tsx, apps/web/src/lib/workbench/store.test.ts</files>
  <action>Add failing tests that select a generated version, switch to 3D mode, find `概念 3D 预览` and `非生产贴膜参考`, use reset/zoom/capture control labels, and verify unsupported template fallback copy while the 2D preview remains available.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/workbench/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before 3D mode exists.
    - Tests do not require a real WebGL context in jsdom.
    - Tests assert selected version state is preserved across mode switches.
  </acceptance_criteria>
  <done>Workbench 3D viewer behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add Three.js dependency and client-only viewer scaffold</name>
  <read_first>
    - `apps/web/package.json`
    - `apps/web/src/components/workbench/preview-panel.tsx`
  </read_first>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/src/components/workbench/preview-3d-panel.tsx, apps/web/src/components/workbench/preview-3d-viewer.tsx</files>
  <action>Add `three` as the minimal runtime dependency. Implement a client-only viewer component that creates a renderer, scene, camera, simple shell geometry, and cleanup lifecycle. Provide a no-WebGL/test fallback branch so Vitest remains deterministic.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Three.js is not imported by server-rendered code paths.
    - Viewer cleanup disposes renderer/listeners/animation frame.
    - The 2D panel still renders when feature flag is false or no selected version exists.
  </acceptance_criteria>
  <done>The workbench has a safe client-only 3D viewer scaffold.</done>
</task>

<task type="auto">
  <name>Task 3: Wire preview mode and camera controls</name>
  <read_first>
    - `apps/web/src/lib/workbench/store.ts`
    - `apps/web/src/components/workbench/preview-panel.tsx`
  </read_first>
  <files>apps/web/src/lib/workbench/store.ts, apps/web/src/lib/workbench/store.test.ts, apps/web/src/components/workbench/preview-panel.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Add preview mode/camera controls through the existing store or a tightly scoped component state. Wire rotate, zoom, reset, and capture placeholder actions with accessible labels. Keep 2D zoom/reset behavior separate from 3D camera reset.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/workbench/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `重置相机` restores default camera values.
    - Zoom and rotate controls do not resize surrounding layout.
    - Incompatible shell shows fallback instead of a blank canvas.
  </acceptance_criteria>
  <done>Users can open the 3D preview surface and operate basic camera controls.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Denial of Service | browser renderer | mitigate | Lazy-load viewer, cleanup resources, and provide fallback for WebGL failure. |
| T-12-06 | Spoofing | 3D label | mitigate | Persistent concept-only label appears in every 3D state. |
</threat_model>

<verification>
Run focused web Vitest, typecheck, and lint when dependency changes are stable.
</verification>

<success_criteria>
- V2-3D-02 user can open the 3D panel for a selected version.
- V2-3D-03 rotate, zoom, reset, and capture controls are visible.
- V2-3D-05 fallback state is visible for incompatible shell.
</success_criteria>

<output>
After completion, create `.planning/phases/12-lightweight-3d-preview-mvp/12-03-SUMMARY.md`.
</output>

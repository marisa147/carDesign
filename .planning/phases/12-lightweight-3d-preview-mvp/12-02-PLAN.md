---
phase: 12-lightweight-3d-preview-mvp
plan: "02"
type: execute
wave: 1
depends_on: ["12-01-PLAN.md"]
files_modified:
  - "services/core/src/caragent_core/preview3d.py"
  - "services/core/src/caragent_core/generation/templates.py"
  - "services/core/tests/test_generation_jobs.py"
  - "apps/web/src/lib/preview3d/spec.ts"
  - "apps/web/src/lib/preview3d/shells.ts"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["V2-3D-01", "V2-3D-05"]
must_haves:
  truths:
    - "At least one shell is registered and linked to the existing template id."
    - "Unsupported templates produce an explicit 2D fallback, not an exception."
    - "Shell fixtures are inspectable and testable without binary assets."
  artifacts:
    - "Generic side coupe shell fixture and compatibility resolver."
    - "Web shell/spec helpers for selected version PreviewSpec data."
    - "Focused core/web tests for compatible and incompatible versions."
  key_links:
    - "12-CONTEXT -> Compatibility And Fallback"
    - "12-RESEARCH -> Shell Fixture Strategy"
    - "12-PATTERNS -> Template compatibility"
---

<objective>
Register the first lightweight preview shell and build deterministic compatibility mapping from existing PreviewSpec/template data.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-lightweight-3d-preview-mvp/12-CONTEXT.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-RESEARCH.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-PATTERNS.md
@services/core/src/caragent_core/generation/templates.py
@apps/web/src/components/workbench/preview-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red compatibility tests</name>
  <read_first>
    - `services/core/tests/test_generation_jobs.py`
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>services/core/tests/test_generation_jobs.py, apps/web/src/app/page.test.tsx</files>
  <action>Add failing tests for resolving `generic-side-coupe-lightweight-v1` from `generic-side-coupe` PreviewSpec data, and for returning an explicit incompatible result for an unknown template without breaking 2D preview fixtures.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_generation_jobs.py</automated>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Compatible test expects shell id, material slots, and default camera preset.
    - Incompatible test expects fallback reason and no thrown error.
    - Web test keeps current 2D PreviewSpec assertions passing.
  </acceptance_criteria>
  <done>Shell compatibility expectations are specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement core shell registry</name>
  <read_first>
    - `services/core/src/caragent_core/preview3d.py`
    - `services/core/src/caragent_core/generation/templates.py`
  </read_first>
  <files>services/core/src/caragent_core/preview3d.py, services/core/src/caragent_core/generation/templates.py</files>
  <action>Add a lightweight shell registry with `generic-side-coupe-lightweight-v1`, material slots, dimensions, safe-zone projection strategy, and a resolver that builds a compatible or incompatible `Preview3DSpec` from PreviewSpec/source artifact metadata.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_generation_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Resolver links shell to `generic-side-coupe`.
    - Resolver preserves source version/artifact/template ids.
    - Resolver adds default non-production warnings.
  </acceptance_criteria>
  <done>Core can resolve a 3D preview shell for supported PreviewSpec data.</done>
</task>

<task type="auto">
  <name>Task 3: Add web-side spec and shell helpers</name>
  <read_first>
    - `apps/web/src/components/workbench/preview-panel.tsx`
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>apps/web/src/lib/preview3d/spec.ts, apps/web/src/lib/preview3d/shells.ts, apps/web/src/app/page.test.tsx</files>
  <action>Add pure TypeScript helpers that read generated contract data or version parameters, derive a Preview3D compatibility result for fixtures, and keep fallback strings stable for tests. Do not render Three.js yet.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Web helpers return compatible shell data for current page fixtures.
    - Unsupported template fixtures return fallback state.
    - Helpers are pure and safe in jsdom tests.
  </acceptance_criteria>
  <done>Frontend can compute whether selected versions can open 3D preview.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | shell compatibility | mitigate | Treat template id/view as data and validate through a small allowlist registry. |
| T-12-04 | Denial of Service | unsupported template | mitigate | Return fallback state instead of throwing in render path. |
</threat_model>

<verification>
Run focused core and web tests.
</verification>

<success_criteria>
- V2-3D-01 has a registered shell linked to existing template data.
- V2-3D-05 has a tested incompatible-shell fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/12-lightweight-3d-preview-mvp/12-02-SUMMARY.md`.
</output>

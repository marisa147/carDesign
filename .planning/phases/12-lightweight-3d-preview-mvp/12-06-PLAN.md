---
phase: 12-lightweight-3d-preview-mvp
plan: "06"
type: execute
wave: 3
depends_on: ["12-04-PLAN.md", "12-05-PLAN.md"]
files_modified:
  - "services/core/src/caragent_core/preview3d.py"
  - "services/api/tests/test_jobs.py"
  - "apps/web/src/components/workbench/preview-3d-panel.tsx"
  - "apps/web/src/components/workbench/preview-3d-viewer.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["V2-3D-03", "V2-3D-04", "V2-3D-05"]
must_haves:
  truths:
    - "Non-production labels are persistent across all 3D states."
    - "Warning metadata travels with specs and screenshot artifacts."
    - "Safe-zone overlays remain compatible with the lightweight shell without implying print accuracy."
  artifacts:
    - "Warning helper constants and metadata assertions."
    - "Persistent UI labels and fallback copy."
    - "Safe-zone overlay compatibility tests."
  key_links:
    - "12-CONTEXT -> Warning Posture"
    - "12-UI-SPEC -> Persistent Labels and Fallback State"
---

<objective>
Harden 3D preview warnings, non-production labels, and safe-zone overlay compatibility.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-lightweight-3d-preview-mvp/12-CONTEXT.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-UI-SPEC.md
@services/core/src/caragent_core/preview3d.py
@apps/web/src/components/workbench/preview-3d-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red warning and label tests</name>
  <read_first>
    - `services/api/tests/test_jobs.py`
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>services/api/tests/test_jobs.py, apps/web/src/app/page.test.tsx</files>
  <action>Add failing tests that every Preview3D spec/screenshot carries `non_production_preview` and `uv_not_verified` warnings, and every 3D UI state renders `概念 3D 预览` plus `非生产贴膜参考`.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover compatible, fallback, loading/error, and screenshot saved states where practical.
    - Tests assert warning metadata is not optional for screenshots.
  </acceptance_criteria>
  <done>Warning requirements are specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement warning metadata helpers</name>
  <read_first>
    - `services/core/src/caragent_core/preview3d.py`
  </read_first>
  <files>services/core/src/caragent_core/preview3d.py, services/api/tests/test_jobs.py</files>
  <action>Centralize default warning construction for Preview3DSpec and screenshot metadata. Ensure callers cannot accidentally omit concept-only and UV-not-verified warnings.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Warning count is deterministic.
    - Warning ids are stable and easy to package in Phase 13.
    - Metadata remains secret-free and binary-free.
  </acceptance_criteria>
  <done>3D preview warning metadata is durable and consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Harden UI labels, safe-zone overlay, and fallback states</name>
  <read_first>
    - `apps/web/src/components/workbench/preview-3d-panel.tsx`
    - `apps/web/src/components/workbench/preview-3d-viewer.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/preview-3d-panel.tsx, apps/web/src/components/workbench/preview-3d-viewer.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Keep concept-only labels visible while rotating, zooming, resetting, and capturing. Add or harden safe-zone overlay compatibility for the side shell. Ensure fallback copy appears for incompatible shell without disabling 2D mode.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
    <automated>corepack pnpm --filter @caragent/web lint</automated>
  </verify>
  <acceptance_criteria>
    - Labels are findable by text in all 3D mode states.
    - Safe-zone overlays are bounded and do not overlap controls.
    - Fallback copy matches UI-SPEC.
  </acceptance_criteria>
  <done>3D preview communicates concept-only limitations consistently.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Spoofing | production-readiness perception | mitigate | Persistent UI labels and durable warning metadata. |
| T-12-04 | Tampering | safe-zone overlay data | mitigate | Keep overlays bounded and derived from PreviewSpec helper output. |
</threat_model>

<verification>
Run focused API/web tests and web lint.
</verification>

<success_criteria>
- V2-3D-03 persistent non-production labeling is verified.
- V2-3D-04 warning metadata is stored with screenshots/specs.
- V2-3D-05 fallback keeps 2D preview available.
</success_criteria>

<output>
After completion, create `.planning/phases/12-lightweight-3d-preview-mvp/12-06-SUMMARY.md`.
</output>

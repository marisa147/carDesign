---
phase: 12-lightweight-3d-preview-mvp
plan: "05"
type: execute
wave: 3
depends_on: ["12-01-PLAN.md", "12-03-PLAN.md", "12-04-PLAN.md"]
files_modified:
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/tests/test_jobs.py"
  - "services/api/tests/test_generation.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
  - "apps/web/src/lib/api/iteration.ts"
  - "apps/web/src/lib/api/iteration.test.ts"
  - "apps/web/src/components/workbench/preview-3d-panel.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["V2-3D-03", "V2-3D-04"]
must_haves:
  truths:
    - "Screenshot capture persists an immutable artifact linked to the selected version."
    - "API validates screenshot bytes, content type, size, and version ownership."
    - "Screenshot metadata records camera, shell, warnings, and source artifact evidence."
  artifacts:
    - "Screenshot create request/response API contract."
    - "Web API wrapper and capture UI integration."
    - "Generated contracts updated."
  key_links:
    - "12-CONTEXT -> Screenshot Persistence"
    - "12-RESEARCH -> Screenshot Persistence Strategy"
    - "12-VALIDATION -> 12-05 focused checks"
---

<objective>
Persist 3D preview screenshots as durable version-linked artifacts from the workbench viewer.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-lightweight-3d-preview-mvp/12-CONTEXT.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-RESEARCH.md
@services/api/src/caragent_api/routes/jobs.py
@services/api/src/caragent_api/schemas.py
@services/core/src/caragent_core/storage.py
@services/core/src/caragent_core/services/jobs.py
@apps/web/src/components/workbench/preview-3d-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red API tests for screenshot persistence</name>
  <read_first>
    - `services/api/tests/test_jobs.py`
    - `services/api/tests/test_generation.py`
  </read_first>
  <files>services/api/tests/test_jobs.py, services/api/tests/test_generation.py</files>
  <action>Add failing tests for a version-scoped 3D screenshot endpoint. Assert feature flag gate, workspace/version ownership, PNG/WebP validation, bounded payload size, artifact kind/object key, and metadata containing `preview_3d_screenshot` without raw base64.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_generation.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before endpoint exists.
    - Tests prove screenshots are linked to the selected version.
    - Tests reject wrong workspace/version and unsupported content types.
  </acceptance_criteria>
  <done>Screenshot API behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement screenshot API and generated contracts</name>
  <read_first>
    - `services/api/src/caragent_api/routes/jobs.py`
    - `services/api/src/caragent_api/schemas.py`
    - `services/core/src/caragent_core/storage.py`
  </read_first>
  <files>services/api/src/caragent_api/routes/jobs.py, services/api/src/caragent_api/schemas.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Add request/response schemas and a version-scoped route that stores screenshot bytes through object storage and creates a linked artifact. Reuse `build_object_key` and `jobs.create_artifact`. Regenerate/check contracts.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_generation.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Route is guarded by `V2_LIGHTWEIGHT_3D_PREVIEW_ENABLED`.
    - Artifact metadata includes camera/shell/source/warnings.
    - Contract check passes.
  </acceptance_criteria>
  <done>The API can persist 3D preview screenshot artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire web capture action and API wrapper</name>
  <read_first>
    - `apps/web/src/lib/api/iteration.ts`
    - `apps/web/src/components/workbench/preview-3d-panel.tsx`
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>apps/web/src/lib/api/iteration.ts, apps/web/src/lib/api/iteration.test.ts, apps/web/src/components/workbench/preview-3d-panel.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Add a generated-client wrapper for screenshot creation. Wire viewer capture to submit canvas PNG plus current Preview3D/camera metadata. Show transient saved/error state and refresh artifact/version data after save.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/api/iteration.test.ts</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Capture button submits selected version id and Preview3D metadata.
    - Saved state does not permanently clutter the panel.
    - API wrapper tests assert request path and payload shape.
  </acceptance_criteria>
  <done>Users can capture and save a 3D preview screenshot artifact.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | screenshot upload | mitigate | Validate content type, size, workspace/version ownership, and metadata schema. |
| T-12-03 | Information Disclosure | screenshot payload metadata | mitigate | Store bytes only in object storage and remove data URLs/base64 from metadata. |
</threat_model>

<verification>
Run API tests, contracts check, web API tests, and web typecheck.
</verification>

<success_criteria>
- V2-3D-03 screenshot capture is functional.
- V2-3D-04 stores screenshot artifacts, camera preset, and warning metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/12-lightweight-3d-preview-mvp/12-05-SUMMARY.md`.
</output>

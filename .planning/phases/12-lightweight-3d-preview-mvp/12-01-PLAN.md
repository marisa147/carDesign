---
phase: 12-lightweight-3d-preview-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "services/core/src/caragent_core/enums.py"
  - "services/core/src/caragent_core/preview3d.py"
  - "services/core/tests/test_models.py"
  - "services/core/tests/test_generation_jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/tests/test_jobs.py"
  - "services/api/tests/test_generation.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
autonomous: true
requirements: ["V2-3D-01", "V2-3D-04", "V2-3D-05"]
must_haves:
  truths:
    - "Preview3DSpec is typed before frontend or screenshot persistence relies on it."
    - "3D screenshots are represented as artifacts plus metadata, not binary JSON."
    - "Compatibility and non-production warnings are explicit in the contract."
  artifacts:
    - "Preview3DSpec and screenshot metadata helpers with schema_version: 1."
    - "Artifact kind or metadata convention for 3D preview screenshots."
    - "Generated OpenAPI and TypeScript contracts are current."
  key_links:
    - "12-CONTEXT -> Preview3DSpec Contract and Warning Posture"
    - "12-RESEARCH -> Preview3DSpec Shape"
    - "12-VALIDATION -> 12-01 focused schema/contract checks"
---

<objective>
Create the typed Preview3DSpec, compatibility, camera, material, warning, and screenshot metadata foundation for Phase 12.

Purpose: API, web, screenshot persistence, and later handoff packaging need one stable JSON contract before the viewer is implemented.
Output: Tested core/API schemas and generated contracts for lightweight 3D preview specs and screenshot metadata.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-lightweight-3d-preview-mvp/12-CONTEXT.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-RESEARCH.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-VALIDATION.md
@.planning/phases/12-lightweight-3d-preview-mvp/12-PATTERNS.md
@services/core/src/caragent_core/enums.py
@services/core/src/caragent_core/models.py
@services/core/src/caragent_core/services/jobs.py
@services/api/src/caragent_api/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for Preview3DSpec and screenshot metadata</name>
  <read_first>
    - `services/core/tests/test_models.py`
    - `services/core/tests/test_generation_jobs.py`
    - `services/api/tests/test_jobs.py`
  </read_first>
  <files>services/core/tests/test_models.py, services/core/tests/test_generation_jobs.py, services/api/tests/test_jobs.py</files>
  <action>Add failing tests that require a `Preview3DSpec` contract with `schema_version: 1`, shell compatibility status, source version/artifact references, camera preset, material mapping, and warning ids. Add artifact metadata assertions for `preview_3d_screenshot` without binary bytes in JSON.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_models.py tests/test_generation_jobs.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because `Preview3DSpec` helpers do not exist.
    - Tests assert invalid compatibility status or missing version/artifact ids fail validation.
    - Tests assert screenshot metadata contains camera/shell/warnings but no base64 image data.
  </acceptance_criteria>
  <done>Preview3D contract behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared Preview3D schema helpers</name>
  <read_first>
    - `services/core/src/caragent_core/editing.py`
    - `services/core/src/caragent_core/references.py`
    - `services/core/src/caragent_core/enums.py`
  </read_first>
  <files>services/core/src/caragent_core/preview3d.py, services/core/src/caragent_core/enums.py, services/core/tests/test_models.py</files>
  <action>Create `caragent_core.preview3d` with Pydantic helpers for `Preview3DSpec`, source references, compatibility, shell, camera preset, material plan, warning, and screenshot metadata. Add a specific `ArtifactKind.PREVIEW_3D_SCREENSHOT` if tests show `preview` is too ambiguous for Phase 13 handoff.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_models.py tests/test_generation_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Helpers round-trip with `model_dump(mode="json")`.
    - `schema_version` defaults to `1`.
    - Compatibility statuses are constrained to explicit values.
    - Warning ids include non-production and UV-not-verified defaults.
  </acceptance_criteria>
  <done>Core 3D preview contracts are available to API and web-facing schemas.</done>
</task>

<task type="auto">
  <name>Task 3: Expose API schema contracts and regenerate TypeScript</name>
  <read_first>
    - `services/api/src/caragent_api/schemas.py`
    - `packages/contracts/openapi/openapi.json`
    - `packages/contracts/src/generated/client.ts`
  </read_first>
  <files>services/api/src/caragent_api/schemas.py, services/api/tests/test_generation.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Expose Preview3D schema types through API schemas so generated contracts can reference them from later screenshot and preview routes. Regenerate or check contracts through the repo contract flow.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py tests/test_jobs.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Generated TypeScript contracts include Preview3D schema types.
    - Existing job/generation response schemas remain backward compatible.
    - Contract check exits successfully without unrelated schema churn.
  </acceptance_criteria>
  <done>Preview3D contracts are visible to the web package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/API to metadata | Preview3D metadata may be user-controlled later and must be typed before storage. |
| Metadata to handoff | Phase 13 may package screenshots, so disclaimers and source linkage must be durable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | Preview3DSpec metadata | mitigate | Validate schema version, compatibility status, source ids, camera fields, and warning ids with Pydantic tests. |
| T-12-02 | Repudiation | screenshot provenance | mitigate | Store source version/artifact, shell id, camera preset, and warning ids in screenshot metadata. |
| T-12-03 | Information Disclosure | screenshot metadata | mitigate | Forbid binary bytes, local paths, secrets, and raw object storage internals beyond safe object keys. |
</threat_model>

<verification>
Run focused core/API tests and `corepack pnpm contracts:check`.
</verification>

<success_criteria>
- V2-3D-01 has a typed shell/spec foundation.
- V2-3D-04 has screenshot metadata and artifact contract foundations.
- V2-3D-05 has explicit compatibility/fallback status in the spec.
</success_criteria>

<output>
After completion, create `.planning/phases/12-lightweight-3d-preview-mvp/12-01-SUMMARY.md`.
</output>

# Phase 26 Plan: Security And Operations Hardening

## Implementation Steps

1. Add API auth/ownership tests for server-derived workspace owners and
   cross-owner workspace/asset rejection.
2. Add upload validation tests for spoofed image bytes and update fixtures to
   use valid PNGs.
3. Add BFL provider download tests for unsafe result URLs and invalid image
   responses.
4. Add hosted quota reserve/settle unit tests and worker trace metadata
   regression coverage.
5. Implement request user dependency, ownership helpers, image-byte validators,
   provider download validation, hosted quota ledger, and trace metadata.
6. Run targeted API/core/worker tests, then the repo validation command.

## Verification

- `uv run pytest -q` in affected Python services.
- `uv run ruff check ...`
- `uv run mypy src`
- `corepack pnpm validate`


---
phase: 03-first-text-to-2d-generation-slice
plan: "02"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - "services/core/src/caragent_core/generation/prompts.py"
  - "services/core/src/caragent_core/generation/__init__.py"
  - "services/core/tests/test_prompt_plans.py"
autonomous: true
requirements: ["GEN-04"]
must_haves:
  truths:
    - "Prompt planning is deterministic and auditable."
    - "Prompt text, structured payload, provider/model parameters, template/view, and input references are explicit."
    - "The planner does not call hosted AI providers."
---

<objective>
Build the deterministic prompt-planning layer that transforms a structured brief into stored prompt text and provider parameters.
</objective>

<context>
@.planning/phases/03-first-text-to-2d-generation-slice/03-CONTEXT.md
@.planning/phases/03-first-text-to-2d-generation-slice/03-AI-SPEC.md
@services/core/src/caragent_core/generation/briefs.py
@services/core/src/caragent_core/generation/templates.py
@services/core/src/caragent_core/models.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02A | Repudiation | Prompt construction | mitigate | Store exact prompt text and structured payload produced by deterministic code. |
| T-03-02B | Information Disclosure | Prompt payload | mitigate | Include only sanitized metadata and asset ids; no secrets or raw binary data. |
| T-03-02C | Tampering | Provider parameters | mitigate | Validate provider/model/size/quality fields before use. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add failing prompt plan tests</name>
  <files>services/core/tests/test_prompt_plans.py</files>
  <action>Write tests that build a prompt plan from a `GenerationBriefPayload` and assert final prompt text includes vehicle/template/view, character/theme, style, palette, text intent, coverage, and concept-preview wording. Assert prompt payload contains original request, normalized fields, warnings, provider label, model, parameters, and input asset ids.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_prompt_plans.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail until `prompts.py` exists.
    - Tests verify exact prompt text is deterministic for the same input.
    - Tests verify unsupported template warnings are carried into prompt payload.
  </acceptance_criteria>
  <done>Prompt trace requirements are specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement prompt plan models</name>
  <files>services/core/src/caragent_core/generation/prompts.py</files>
  <action>Add Pydantic models for `PromptProviderSettings` and `PromptPlan`. Include fields for provider, model, parameters, prompt_text, prompt_payload, input_artifact_ids, estimated_cost, and concept label. Keep model/provider defaults suitable for local deterministic generation.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_prompt_plans.py</automated>
  </verify>
  <acceptance_criteria>
    - Prompt plan can serialize with `model_dump(mode="json")`.
    - Provider/model names are fields, not constants hard-coded into job services.
    - Estimated cost can be null.
  </acceptance_criteria>
  <done>Prompt plan data contract exists.</done>
</task>

<task type="auto">
  <name>Task 3: Implement deterministic prompt builder</name>
  <files>services/core/src/caragent_core/generation/prompts.py, services/core/src/caragent_core/generation/__init__.py</files>
  <action>Implement `build_prompt_plan(brief, provider_settings)` that creates concise prompt text for a 2D pain-car concept render and structured payload for model-run storage. Export the helper from the generation package.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_prompt_plans.py tests/test_generation_briefs.py</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Builder performs no network calls and imports no provider SDK.
    - Prompt text includes concept-preview wording and does not imply production-ready wrap output.
    - Reference assets are included by id/metadata only; no binary payload is embedded.
  </acceptance_criteria>
  <done>Prompt planning is ready for model-run trace storage.</done>
</task>

</tasks>

<verification>
Run core prompt/brief tests, core Ruff, and core mypy.
</verification>

<success_criteria>
- Prompt plans are deterministic and serializable.
- `GEN-04` trace fields are represented before provider work begins.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-text-to-2d-generation-slice/03-02-SUMMARY.md`.
</output>

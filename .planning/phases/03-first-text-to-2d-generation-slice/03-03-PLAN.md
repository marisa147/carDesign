---
phase: 03-first-text-to-2d-generation-slice
plan: "03"
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - "services/worker/pyproject.toml"
  - "services/worker/src/caragent_worker/config.py"
  - "services/worker/src/caragent_worker/providers/__init__.py"
  - "services/worker/src/caragent_worker/providers/base.py"
  - "services/worker/src/caragent_worker/providers/local.py"
  - "services/worker/src/caragent_worker/providers/bfl.py"
  - "services/worker/tests/test_image_providers.py"
autonomous: true
requirements: ["GEN-04", "GEN-05", "GEN-06", "GEN-07"]
must_haves:
  truths:
    - "Provider calls are isolated behind adapter interfaces."
    - "Local deterministic generation creates valid image bytes without external calls."
    - "Hosted provider code is mocked in tests and gated by explicit settings."
---

<objective>
Implement the provider adapter boundary, deterministic local image provider, and a mocked hosted-provider adapter suitable for later worker execution.
</objective>

<context>
@.planning/phases/03-first-text-to-2d-generation-slice/03-CONTEXT.md
@.planning/phases/03-first-text-to-2d-generation-slice/03-AI-SPEC.md
@.planning/phases/03-first-text-to-2d-generation-slice/03-RESEARCH.md
@services/worker/src/caragent_worker/config.py
@services/worker/src/caragent_worker/tasks/jobs.py
@services/core/src/caragent_core/generation/prompts.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03A | Information Disclosure | Provider adapter | mitigate | Read API keys from settings only and never include them in results/events. |
| T-03-03B | Denial of Service | Hosted polling | mitigate | Add timeout, max poll attempts, and clear failure results. |
| T-03-03C | Supply Chain | Dependencies | mitigate | Add only worker-scoped `httpx` and `Pillow`; do not spread provider deps across API/core. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add failing provider adapter tests</name>
  <files>services/worker/tests/test_image_providers.py</files>
  <action>Write tests for a local deterministic provider returning PNG bytes, provider result metadata, zero cost, and no external calls. Add mocked HTTP tests for BFL-style submit/poll/result flow, signed URL download, timeout/failure handling, and missing-key configuration errors.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests do not require live internet or provider credentials.
    - Local provider test verifies PNG signature and non-empty dimensions/metadata.
    - Hosted adapter tests assert Authorization is used but the token is never present in normalized errors.
  </acceptance_criteria>
  <done>Provider boundary behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Add worker dependencies and provider settings</name>
  <files>services/worker/pyproject.toml, services/worker/src/caragent_worker/config.py</files>
  <action>Add worker-scoped dependencies for `httpx` and `Pillow`. Extend worker settings with provider model, generation timeout, poll interval, max poll attempts, and local output image defaults while preserving `AI_PROVIDER_CALLS_ENABLED=false` by default.</action>
  <verify>
    <automated>cd services/worker && uv run python -c "from caragent_worker.config import get_settings; print(get_settings().ai_provider_calls_enabled)"</automated>
  </verify>
  <acceptance_criteria>
    - Provider calls remain disabled by default.
    - New settings have safe defaults for local mode.
    - Existing worker config tests remain passing.
  </acceptance_criteria>
  <done>Worker has provider runtime settings.</done>
</task>

<task type="auto">
  <name>Task 3: Implement provider protocols and local adapter</name>
  <files>services/worker/src/caragent_worker/providers/base.py, services/worker/src/caragent_worker/providers/local.py, services/worker/src/caragent_worker/providers/__init__.py</files>
  <action>Add normalized provider request/result/error dataclasses or Pydantic models and implement `LocalDeterministicImageProvider` using Pillow. Render a simple concept-preview PNG that includes deterministic color blocks and normalized brief labels from the prompt payload.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py</automated>
    <automated>cd services/worker && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Local adapter returns bytes, content type, width, height, checksum-friendly metadata, provider/model labels, and zero cost.
    - Local adapter makes no HTTP calls.
    - Output is deterministic for the same prompt plan.
  </acceptance_criteria>
  <done>Local generation provider exists.</done>
</task>

<task type="auto">
  <name>Task 4: Implement BFL-style hosted adapter with mocked HTTP coverage</name>
  <files>services/worker/src/caragent_worker/providers/bfl.py, services/worker/src/caragent_worker/providers/__init__.py</files>
  <action>Implement a hosted adapter for BFL-style async generation using `httpx`: submit request, poll bounded status, retrieve result URL, download image bytes, and return normalized metadata. Keep endpoint/model configurable and test all behavior with mocked `httpx` transports.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_image_providers.py</automated>
    <automated>cd services/worker && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - Hosted adapter is not selected unless provider calls are enabled.
    - Provider URLs/results are copied to bytes for project storage.
    - Errors are sanitized and bounded.
  </acceptance_criteria>
  <done>Hosted adapter scaffold is ready without live provider dependency.</done>
</task>

</tasks>

<verification>
Run worker provider tests, Ruff, and mypy. Do not run live hosted provider calls.
</verification>

<success_criteria>
- Provider adapter interface is isolated.
- Local provider can generate actual image bytes.
- Hosted provider path is covered by mocks and gated by settings.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-text-to-2d-generation-slice/03-03-SUMMARY.md`.
</output>

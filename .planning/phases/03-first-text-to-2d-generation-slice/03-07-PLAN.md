---
phase: 03-first-text-to-2d-generation-slice
plan: "07"
type: execute
wave: 6
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06"]
files_modified:
  - "scripts/smoke-local.mjs"
  - "scripts/validate-all.mjs"
  - "scripts/check-env-examples.mjs"
  - ".env.example"
  - "services/api/.env.example"
  - "services/worker/.env.example"
  - "services/api/src/caragent_api/scripts/phase3_generation_smoke.py"
  - "README.md"
  - "docs/development.md"
  - ".planning/phases/03-first-text-to-2d-generation-slice/03-VERIFICATION.md"
  - ".planning/phases/03-first-text-to-2d-generation-slice/03-HUMAN-UAT.md"
autonomous: true
requirements: ["GEN-01", "GEN-02", "GEN-03", "GEN-04", "GEN-05", "GEN-06", "GEN-07"]
must_haves:
  truths:
    - "Root validation and local smoke cover Phase 3 without hosted provider keys."
    - "Docs explain local deterministic generation and optional hosted-provider enablement truthfully."
    - "Verification records exact command outcomes and requirement coverage."
---

<objective>
Finalize Phase 3 smoke checks, docs, env guards, verification report, and UAT checklist.
</objective>

<context>
@.planning/phases/03-first-text-to-2d-generation-slice/03-VALIDATION.md
@scripts/smoke-local.mjs
@scripts/validate-all.mjs
@scripts/check-env-examples.mjs
@README.md
@docs/development.md
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07A | Repudiation | Verification report | mitigate | Record exact commands, dates, outcomes, and gaps in `03-VERIFICATION.md`. |
| T-03-07B | Information Disclosure | Env/docs | mitigate | Document env names with placeholders only and keep provider calls disabled by default. |
| T-03-07C | Denial of Service | Smoke script | mitigate | Keep Docker/provider checks bounded and clean up services after smoke/UAT. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 3 generation smoke script</name>
  <files>services/api/src/caragent_api/scripts/phase3_generation_smoke.py, scripts/smoke-local.mjs</files>
  <action>Add a smoke path that creates a workspace, creates a structured brief from natural language, creates/enqueues or directly runs the local deterministic generation path in-process, verifies job events/model run/artifact/design version, and cleans up test data. Wire it into `pnpm smoke:local` after Phase 2 smoke.</action>
  <verify>
    <automated>corepack pnpm smoke:local --allow-docker-unavailable</automated>
  </verify>
  <acceptance_criteria>
    - Smoke requires no hosted provider keys.
    - Smoke proves generated artifact and design version records exist.
    - Docker-unavailable mode remains truthful and does not claim the smoke ran.
  </acceptance_criteria>
  <done>Local smoke covers Phase 3 generation.</done>
</task>

<task type="auto">
  <name>Task 2: Update validation and env guards</name>
  <files>scripts/validate-all.mjs, scripts/check-env-examples.mjs, .env.example, services/api/.env.example, services/worker/.env.example</files>
  <action>Update aggregate validation and env examples for Phase 3 provider settings, timeout/poll settings, and local deterministic generation defaults. Keep hosted provider calls disabled by default and fail env guard if real-looking keys are committed.</action>
  <verify>
    <automated>node scripts/check-env-examples.mjs</automated>
    <automated>corepack pnpm validate</automated>
  </verify>
  <acceptance_criteria>
    - Example env files mention Phase 3 generation and hosted provider opt-in.
    - Provider keys remain empty placeholders.
    - Validate still runs web/contracts/core/API/worker checks.
  </acceptance_criteria>
  <done>Configuration and aggregate validation match Phase 3.</done>
</task>

<task type="auto">
  <name>Task 3: Update developer docs</name>
  <files>README.md, docs/development.md</files>
  <action>Document the local deterministic generation workflow, structured brief flow, prompt/model-run traceability, optional hosted provider enablement, retry behavior, validation commands, smoke commands, and exact Phase 3 boundaries.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=fs.readFileSync('docs/development.md','utf8'); for (const t of ['Phase 3','structured brief','local deterministic','prompt trace','retry']) if (!d.includes(t)) throw new Error('missing docs token '+t)"</automated>
  </verify>
  <acceptance_criteria>
    - Docs do not claim production-ready wrap output.
    - Docs clearly state hosted provider smoke is optional and explicitly enabled.
    - Docs keep Phase 4/5/6/7 features deferred.
  </acceptance_criteria>
  <done>Developers know how to run and verify Phase 3.</done>
</task>

<task type="auto">
  <name>Task 4: Run final verification and write reports</name>
  <files>.planning/phases/03-first-text-to-2d-generation-slice/03-VERIFICATION.md, .planning/phases/03-first-text-to-2d-generation-slice/03-HUMAN-UAT.md</files>
  <action>Run final commands: service-specific tests, `corepack pnpm contracts:check`, `corepack pnpm lint`, `corepack pnpm typecheck`, `corepack pnpm test`, `corepack pnpm validate`, Docker-backed `corepack pnpm infra:up`, `corepack pnpm smoke:local`, and `corepack pnpm infra:down`. Use Browser to verify the web proof if it exists. Write verification and UAT artifacts with exact outcomes and gaps.</action>
  <verify>
    <automated>Test `03-VERIFICATION.md` exists and records every `GEN-*` requirement as satisfied, partial, or gap with evidence.</automated>
  </verify>
  <acceptance_criteria>
    - No command is marked passed unless it returned exit 0.
    - Docker cleanup is recorded.
    - Browser UAT records workspace/job/artifact evidence if web proof exists.
  </acceptance_criteria>
  <done>Phase 3 is ready for `$gsd-verify-work 3`.</done>
</task>

</tasks>

<verification>
Run full root validation, Docker smoke, and browser UAT. Record all evidence in `03-VERIFICATION.md`.
</verification>

<success_criteria>
- Phase 3 can be verified locally without hosted keys.
- Docs and env examples are truthful.
- GEN-01 through GEN-07 have explicit evidence or gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-text-to-2d-generation-slice/03-07-SUMMARY.md`.
</output>

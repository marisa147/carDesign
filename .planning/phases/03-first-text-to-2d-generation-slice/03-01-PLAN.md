---
phase: 03-first-text-to-2d-generation-slice
plan: "01"
type: execute
wave: 1
depends_on: ["02-03", "02-05"]
files_modified:
  - "services/core/pyproject.toml"
  - "services/core/src/caragent_core/generation/__init__.py"
  - "services/core/src/caragent_core/generation/briefs.py"
  - "services/core/src/caragent_core/generation/templates.py"
  - "services/core/tests/test_generation_briefs.py"
autonomous: true
requirements: ["GEN-01", "GEN-02", "GEN-03"]
must_haves:
  truths:
    - "Natural-language generation input becomes a typed, reusable structured brief."
    - "One supported vehicle template and view are explicit and test-covered."
    - "Unsupported template/view requests are normalized with warnings, not silently accepted."
---

<objective>
Create the typed Phase 3 generation brief and minimal vehicle-template catalog used by prompt planning, API routes, and worker execution.
</objective>

<context>
@.planning/phases/03-first-text-to-2d-generation-slice/03-CONTEXT.md
@.planning/phases/03-first-text-to-2d-generation-slice/03-AI-SPEC.md
@.planning/phases/03-first-text-to-2d-generation-slice/03-RESEARCH.md
@services/core/src/caragent_core/services/workspaces.py
@services/core/src/caragent_core/models.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01A | Tampering | Brief payload | mitigate | Validate required fields through typed schemas before persistence. |
| T-03-01B | Repudiation | Unsupported template handling | mitigate | Preserve normalization warnings in the structured brief payload. |
| T-03-01C | Misrepresentation | Vehicle support | mitigate | Expose one supported template/view explicitly and warn on unsupported inputs. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add failing core brief tests</name>
  <files>services/core/tests/test_generation_briefs.py</files>
  <action>Write tests for creating a `GenerationBriefPayload` from natural-language input, preserving original request text, extracting or defaulting vehicle/template/view/style/palette/text/coverage fields, validating empty input, normalizing unsupported template/view to the supported option, and preserving warning strings.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_generation_briefs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests initially fail because generation brief modules do not exist.
    - Tests cover `GEN-01`, `GEN-02`, and `GEN-03` at service/schema level.
    - Tests do not require API, worker, browser, Redis, or provider credentials.
  </acceptance_criteria>
  <done>Brief behavior is specified before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add Pydantic dependency and generation package</name>
  <files>services/core/pyproject.toml, services/core/src/caragent_core/generation/__init__.py</files>
  <action>Add `pydantic>=2.13,<3` to `caragent-core` because typed brief/prompt payloads are shared by API and worker. Create the `caragent_core.generation` package and export the public brief/template helpers.</action>
  <verify>
    <automated>cd services/core && uv run python -c "from caragent_core.generation import GenerationBriefPayload"</automated>
  </verify>
  <acceptance_criteria>
    - Core remains importable by API and worker.
    - Dependency versions resolve through uv without changing runtime Python bounds.
  </acceptance_criteria>
  <done>Core can host shared generation schemas.</done>
</task>

<task type="auto">
  <name>Task 3: Implement supported template catalog</name>
  <files>services/core/src/caragent_core/generation/templates.py</files>
  <action>Add a small catalog with one supported template id, label, default view, allowed views, and canvas hints. Implement helpers to resolve template/view requests and return normalized values plus warnings when unsupported input is requested.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_generation_briefs.py</automated>
  </verify>
  <acceptance_criteria>
    - Supported template id and side view are deterministic constants.
    - Unsupported vehicle or view returns a warning and the supported default.
    - Broad vehicle library or safe-zone data is not introduced in this plan.
  </acceptance_criteria>
  <done>Minimum template/view scope is explicit.</done>
</task>

<task type="auto">
  <name>Task 4: Implement typed brief payload and parser</name>
  <files>services/core/src/caragent_core/generation/briefs.py</files>
  <action>Implement Pydantic models and deterministic parser helpers that convert original request text plus optional user-provided fields into a `GenerationBriefPayload`. Include original request, template id, view, character/theme, style, palette, text, coverage, reference asset ids, and warnings.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_generation_briefs.py</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Empty natural-language input fails validation.
    - Payload can round-trip through `model_dump(mode="json")`.
    - Reference asset ids are strings/UUID-safe but rights enforcement is left to later plans.
  </acceptance_criteria>
  <done>Structured brief creation works in core.</done>
</task>

</tasks>

<verification>
Run `cd services/core && uv run ruff check .`, `cd services/core && uv run mypy src`, and `cd services/core && uv run pytest -q tests/test_generation_briefs.py`.
</verification>

<success_criteria>
- Phase 3 has typed, test-covered brief payloads.
- One supported template/view is explicit.
- Unsupported input is normalized with warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-text-to-2d-generation-slice/03-01-SUMMARY.md`.
</output>

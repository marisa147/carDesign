---
phase: 02-durable-data-jobs-and-assets
plan: "06"
type: execute
wave: 4
depends_on: ["02-03", "02-04", "02-05"]
files_modified:
  - "services/api/src/caragent_api/main.py"
  - "services/api/src/caragent_api/routes/*.py"
  - "services/api/tests/test_openapi_export.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
  - "packages/contracts/src/index.ts"
  - "apps/web/src/lib/api/workspaces.ts"
  - "apps/web/src/lib/api/jobs.ts"
autonomous: true
requirements: ["DATA-01", "DATA-02", "DATA-03", "DATA-04", "DATA-05", "DATA-06", "DATA-07"]
must_haves:
  truths:
    - "All Phase 2 product routes appear in deterministic OpenAPI."
    - "Generated TypeScript contracts are current."
    - "Frontend API wrappers consume generated contract helpers instead of duplicated hand-written types."
---

<objective>
Refresh and harden the typed API contract surface for Phase 2 routes.
</objective>

<context>
@.planning/phases/02-durable-data-jobs-and-assets/02-CONTEXT.md
@packages/contracts/orval.config.ts
@scripts/check-contracts.mjs
@apps/web/src/lib/api/health.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06A | Tampering | API contracts | mitigate | Contract drift check must fail when generated files are stale. |
| T-02-06B | Information Disclosure | Schemas | mitigate | Response schemas must omit secrets and raw object storage credentials. |
| T-02-06C | Reliability | Frontend wrappers | mitigate | Use generated URLs/types so route changes fail contract checks. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify OpenAPI includes product routes</name>
  <files>services/api/tests/test_openapi_export.py</files>
  <action>Extend OpenAPI export tests to assert Phase 2 routes and core schemas are present: workspaces, messages, briefs, assets, rights update, jobs, events, versions/artifacts, and cost fields. Keep deterministic formatting tests.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_openapi_export.py</automated>
  </verify>
  <acceptance_criteria>
    - Removing a Phase 2 route fails OpenAPI tests.
    - Cost and rights schemas are represented.
  </acceptance_criteria>
  <done>OpenAPI route coverage is test-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and check contracts</name>
  <files>packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts, packages/contracts/src/index.ts</files>
  <action>Run the existing OpenAPI export and Orval generation path. Commit refreshed artifacts only after reviewing that generated client names are stable and no Phase 1 health types disappeared. Update `packages/contracts/src/index.ts` only if exports require it.</action>
  <verify>
    <automated>pnpm contracts:generate</automated>
    <automated>pnpm contracts:check</automated>
    <automated>pnpm --filter @caragent/contracts typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm contracts:check` passes from a clean generated state.
    - Health contract remains present.
    - New Phase 2 route helpers/types are generated.
  </acceptance_criteria>
  <done>Contracts are current.</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend API wrappers</name>
  <files>apps/web/src/lib/api/workspaces.ts, apps/web/src/lib/api/jobs.ts</files>
  <action>Add small wrapper functions around generated contract helpers for create/resume workspace, list/create messages, create/list jobs, and list events. Match the `health.ts` pattern: accept optional API base URL and fetch implementation, throw useful errors on non-2xx responses, and return generated response types.</action>
  <verify>
    <automated>pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Wrappers import generated types/functions from `@caragent/contracts`.
    - Wrappers do not duplicate request/response interfaces by hand.
    - Wrappers do not expose object storage secrets.
  </acceptance_criteria>
  <done>Frontend has typed service wrappers for Phase 2 proof UI.</done>
</task>

<task type="auto">
  <name>Task 4: Add wrapper tests if needed</name>
  <files>apps/web/src/lib/api/*.test.ts</files>
  <action>If wrapper logic includes nontrivial error handling or mapping, add Vitest tests with mocked fetch to prove generated URLs, methods, JSON bodies, and error paths. If wrappers stay trivial, ensure page tests in plan 02-08 cover them through UI behavior.</action>
  <verify>
    <automated>pnpm --filter @caragent/web test</automated>
  </verify>
  <acceptance_criteria>
    - Either direct wrapper tests exist or plan 02-08 page tests exercise wrappers.
    - Typecheck catches generated-contract drift.
  </acceptance_criteria>
  <done>Frontend contract usage is covered.</done>
</task>

</tasks>

<verification>
Run API OpenAPI tests, `pnpm contracts:generate`, `pnpm contracts:check`, web typecheck, and contracts typecheck.
</verification>

<success_criteria>
- New API schemas and endpoints are contract-generated.
- Frontend code consumes generated contracts for Phase 2 APIs.
- Contract drift check remains a required gate.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-06-SUMMARY.md`.
</output>

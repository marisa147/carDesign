---
phase: 02-durable-data-jobs-and-assets
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "services/core/pyproject.toml"
  - "services/core/src/caragent_core/__init__.py"
  - "services/core/src/caragent_core/database.py"
  - "services/core/tests/test_database.py"
  - "services/api/pyproject.toml"
  - "services/worker/pyproject.toml"
  - "services/api/alembic.ini"
  - "services/api/alembic/env.py"
  - "package.json"
  - "docs/development.md"
autonomous: true
requirements: ["DATA-01", "DATA-02", "DATA-05"]
user_setup:
  - tool: "uv dependency resolution"
    why: "Adds SQLAlchemy/Alembic/async database dependencies and local path dependencies."
must_haves:
  truths:
    - "API and worker can import shared durable-data primitives without worker importing FastAPI routers."
    - "Alembic can import shared SQLAlchemy metadata."
    - "Root validation includes the new shared core package."
  artifacts:
    - path: "services/core/pyproject.toml"
      provides: "Shared Python package definition"
      contains: "caragent-core"
    - path: "services/core/src/caragent_core/database.py"
      provides: "Async database engine/session helpers"
      contains: "create_async_engine"
    - path: "services/api/alembic/env.py"
      provides: "Migration metadata bridge"
      contains: "caragent_core"
  key_links:
    - from: "services/api/pyproject.toml"
      to: "services/core"
      via: "local path dependency"
      pattern: "caragent-core"
    - from: "services/worker/pyproject.toml"
      to: "services/core"
      via: "local path dependency"
      pattern: "caragent-core"
---

<objective>
Create the shared Python data-core package and migration foundation used by Phase 2.

Purpose: Give API and worker a common durable-data layer while preserving the Phase 1 boundary that workers must not import FastAPI routers.
Output: `services/core`, API/worker local path dependencies, Alembic configuration, and validation wiring.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-durable-data-jobs-and-assets/02-CONTEXT.md
@.planning/phases/02-durable-data-jobs-and-assets/02-RESEARCH.md
@.planning/phases/02-durable-data-jobs-and-assets/02-PATTERNS.md
@.planning/phases/02-durable-data-jobs-and-assets/02-VALIDATION.md

<interfaces>
Create `caragent_core.database` with typed async SQLAlchemy helpers. Do not create product tables in this plan beyond empty metadata wiring.
</interfaces>
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01A | Tampering | Migration metadata | mitigate | Alembic imports one shared metadata object so API and worker do not drift on table definitions. |
| T-02-01B | Information Disclosure | Database URL handling | mitigate | Database helpers must not log raw URLs or secrets; tests should cover repr/log-safe behavior when applicable. |
| T-02-01C | Elevation of Privilege | Worker boundary | mitigate | Keep worker imports limited to `caragent_core`; preserve tests forbidding `caragent_api` imports. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add shared core package</name>
  <files>services/core/pyproject.toml, services/core/src/caragent_core/__init__.py, services/core/tests/test_database.py</files>
  <read_first>Read `02-CONTEXT.md` D-02-04 through D-02-08 and `02-PATTERNS.md` Shared Core Package.</read_first>
  <action>Create a `services/core` uv package named `caragent-core` with Python `>=3.13,<3.14`, strict ruff/mypy/pytest settings matching API/worker style, and dependencies for SQLAlchemy async database work. Add `caragent_core/__init__.py` exporting a version string. Add a smoke test that imports `caragent_core` and the database module.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q</automated>
    <automated>cd services/core && uv run ruff check .</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Core package imports without depending on FastAPI or Celery.
    - Core test suite runs independently through uv.
    - No provider SDK dependencies are added.
  </acceptance_criteria>
  <done>Shared package shell exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement async database helpers</name>
  <files>services/core/src/caragent_core/database.py, services/core/tests/test_database.py</files>
  <read_first>Read `services/api/src/caragent_api/config.py` for current database URL defaults before editing.</read_first>
  <action>Add typed helpers for creating an async SQLAlchemy engine, creating an async sessionmaker, and yielding sessions for service/repository tests. Include URL normalization only if needed for SQLite test URLs; do not hard-code production credentials. Add tests using a temporary SQLite async URL or SQLAlchemy mockable constructs to prove helpers create sessionmakers and do not expose password-bearing URLs in assertions/loggable reprs.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_database.py</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Database helper functions are typed.
    - Tests do not require Docker.
    - Secret-bearing URLs are not printed by helper return values or errors introduced in this task.
  </acceptance_criteria>
  <done>Core database utilities are ready for models and repositories.</done>
</task>

<task type="auto">
  <name>Task 3: Wire API and worker local dependencies</name>
  <files>services/api/pyproject.toml, services/worker/pyproject.toml, services/api/uv.lock, services/worker/uv.lock</files>
  <read_first>Read both existing service pyproject files and preserve current package pins unless dependency resolution requires compatible updates.</read_first>
  <action>Add the local `caragent-core` dependency to API and worker using uv path dependency syntax. Run uv sync for both services so lockfiles reflect the dependency. Do not remove existing FastAPI/Celery/config dependencies.</action>
  <verify>
    <automated>cd services/api && uv run python -c "import caragent_core; print(caragent_core.__version__)"</automated>
    <automated>cd services/worker && uv run python -c "import caragent_core; print(caragent_core.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - API can import `caragent_core`.
    - Worker can import `caragent_core`.
    - Worker still does not import `caragent_api`.
  </acceptance_criteria>
  <done>Shared package is available to both Python services.</done>
</task>

<task type="auto">
  <name>Task 4: Add Alembic migration foundation</name>
  <files>services/api/alembic.ini, services/api/alembic/env.py, services/api/alembic/versions/.gitkeep</files>
  <read_first>Read `02-RESEARCH.md` Database And Migrations.</read_first>
  <action>Create Alembic configuration under `services/api`, with `env.py` importing shared metadata from `caragent_core.models` once plan 02-02 adds it. If the metadata module does not exist yet, create a minimal empty metadata object in core now so Alembic imports successfully. Configure the migration URL from `DATABASE_URL`/`ApiSettings` without embedding secrets in committed files.</action>
  <verify>
    <automated>cd services/api && uv run alembic current</automated>
  </verify>
  <acceptance_criteria>
    - Alembic command imports without crashing.
    - No real credentials are committed.
    - Version directory exists for plan 02-02 migration.
  </acceptance_criteria>
  <done>Migration command path is ready.</done>
</task>

<task type="auto">
  <name>Task 5: Include core in validation commands and docs</name>
  <files>package.json, docs/development.md</files>
  <read_first>Read current root scripts and development guide to preserve existing command names.</read_first>
  <action>Update root `lint`, `typecheck`, `test`, and `validate` orchestration to include `services/core`. Update docs to mention the core package, its role, and its commands. Do not rename Phase 1 command names.</action>
  <verify>
    <automated>pnpm lint</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - Existing root command names still exist.
    - Core checks run as part of aggregate commands.
    - Docs describe core as shared durable-data code, not an API service.
  </acceptance_criteria>
  <done>Foundation validation includes the shared package.</done>
</task>

</tasks>

<verification>
Run all task verification commands. Then run `git -c safe.directory=D:/python/carAgent status --short` and confirm changes are limited to this plan's scope plus intended planning files.
</verification>

<success_criteria>
- `services/core` exists and passes lint/type/test.
- API and worker can import `caragent_core`.
- Alembic imports shared metadata.
- Root validation includes the new package.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-01-SUMMARY.md`.
</output>

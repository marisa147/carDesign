---
phase: 02-durable-data-jobs-and-assets
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "services/core/src/caragent_core/models.py"
  - "services/core/src/caragent_core/enums.py"
  - "services/core/tests/test_models.py"
  - "services/api/alembic/versions/*_phase_02_initial_ledger.py"
autonomous: true
requirements: ["DATA-01", "DATA-02", "DATA-03", "DATA-04", "DATA-05", "DATA-06", "DATA-07"]
must_haves:
  truths:
    - "The initial durable ledger schema covers all DATA-02 record categories."
    - "Workspace, job, asset, artifact, and object-key uniqueness constraints support resume/idempotency/immutability."
    - "Migration upgrade creates the Phase 2 ledger tables."
  artifacts:
    - path: "services/core/src/caragent_core/models.py"
      provides: "SQLAlchemy table definitions"
      contains: "GenerationJob"
    - path: "services/api/alembic/versions/*_phase_02_initial_ledger.py"
      provides: "Initial ledger migration"
      contains: "op.create_table"
---

<objective>
Define the durable product ledger schema and initial migration for Phase 2.
</objective>

<context>
@.planning/phases/02-durable-data-jobs-and-assets/02-CONTEXT.md
@.planning/phases/02-durable-data-jobs-and-assets/02-RESEARCH.md
@.planning/phases/02-durable-data-jobs-and-assets/02-PATTERNS.md
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02A | Tampering | Artifact records | mitigate | Use immutable artifact rows and unique object keys; no update path should replace binary identity. |
| T-02-02B | Repudiation | Job events | mitigate | Use append-only events with timestamps and actor/source fields where practical. |
| T-02-02C | Denial of Service | Workspace queries | mitigate | Add indexes for workspace/job/event lookup paths used by API polling. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add domain enums</name>
  <files>services/core/src/caragent_core/enums.py, services/core/tests/test_models.py</files>
  <action>Create string enums or Literal-backed constants for workspace status, message role, asset kind, rights status, job status, job event type, artifact kind, design version status, model run status, feedback rating/approval state, and export status. Keep enum values stable and lowercase API-safe strings.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_models.py</automated>
  </verify>
  <acceptance_criteria>
    - Enums include at least job statuses `queued`, `running`, `succeeded`, `failed`, `canceled`.
    - Rights status supports a state that blocks generation/export until confirmed.
  </acceptance_criteria>
  <done>Domain enum vocabulary exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add SQLAlchemy models</name>
  <files>services/core/src/caragent_core/models.py, services/core/tests/test_models.py</files>
  <action>Define SQLAlchemy 2 typed declarative models for workspaces, messages, design briefs, assets, generation jobs, job events, design versions, artifacts, model runs, feedback, and exports. Include UUID primary keys, created/updated timestamps, foreign keys, JSON metadata/payload fields where needed, cost estimate/actual fields on jobs/model runs, and parent version lineage. Add tests that inspect metadata and assert all required table names and constraints exist.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_models.py</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - All DATA-02 record categories have tables.
    - `generation_jobs` has a workspace-scoped idempotency uniqueness constraint.
    - `artifacts` and `assets` do not store raw binary payload columns.
  </acceptance_criteria>
  <done>Shared metadata represents the Phase 2 ledger.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and harden initial migration</name>
  <files>services/api/alembic/versions/*_phase_02_initial_ledger.py</files>
  <action>Create an Alembic revision for the models. Review the autogenerated migration before committing: ensure enum/check constraints, indexes, foreign keys, unique constraints, nullable fields, and JSON columns match the model intent. Do not leave autogenerated noise unrelated to Phase 2.</action>
  <verify>
    <automated>cd services/api && uv run alembic upgrade head</automated>
    <automated>cd services/api && uv run alembic downgrade base && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - Migration upgrades and downgrades cleanly against the configured local/test database.
    - Migration contains no real data or credentials.
    - Migration table names match model table names.
  </acceptance_criteria>
  <done>Initial ledger migration is executable.</done>
</task>

<task type="auto">
  <name>Task 4: Add schema integrity tests</name>
  <files>services/core/tests/test_models.py, services/api/tests/test_migrations.py</files>
  <action>Add tests proving all required tables are present, job idempotency uniqueness is modelled, artifact object keys are unique, and asset rights/source columns exist. Add an API migration smoke test that imports Alembic config and metadata without requiring provider/network access.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_models.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_migrations.py</automated>
  </verify>
  <acceptance_criteria>
    - Missing any DATA-02 table fails a test.
    - Missing idempotency/object-key/rights constraints fails a test.
  </acceptance_criteria>
  <done>Schema coverage is test-backed.</done>
</task>

</tasks>

<verification>
Run `cd services/core && uv run pytest -q`, `cd services/api && uv run pytest -q tests/test_migrations.py`, and Alembic upgrade/downgrade checks.
</verification>

<success_criteria>
- Initial migration represents all required durable entities.
- Constraints support idempotency, rights metadata, and immutable object keys.
- Migration path is repeatable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-02-SUMMARY.md`.
</output>

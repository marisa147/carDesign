---
phase: 02-durable-data-jobs-and-assets
plan: "04"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "services/core/src/caragent_core/storage.py"
  - "services/core/src/caragent_core/services/assets.py"
  - "services/core/tests/test_assets.py"
  - "services/api/src/caragent_api/routes/assets.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/tests/test_assets.py"
autonomous: true
requirements: ["DATA-02", "DATA-03", "DATA-04"]
must_haves:
  truths:
    - "Asset uploads are validated before metadata and object records are committed."
    - "Object storage keys are immutable and workspace-scoped."
    - "Rights/source metadata is required before assets can be marked usable for generation/export."
---

<objective>
Implement durable uploaded assets, object-storage metadata, validation, and rights/source enforcement.
</objective>

<context>
@.planning/phases/02-durable-data-jobs-and-assets/02-CONTEXT.md
@.planning/phases/02-durable-data-jobs-and-assets/02-RESEARCH.md
@infra/compose.yml
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04A | Denial of Service | Upload endpoint | mitigate | Enforce max byte size before object storage writes where possible. |
| T-02-04B | Information Disclosure | Object storage credentials | mitigate | Keep S3 credentials server-side; API returns asset metadata, not credentials. |
| T-02-04C | Tampering | Rights metadata | mitigate | Block generation/export usability until rights/source fields are explicitly confirmed. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add object-key and validation tests</name>
  <files>services/core/tests/test_assets.py</files>
  <action>Write failing tests for safe workspace-scoped object key generation, supported image MIME/extension validation, byte-size rejection, immutable key uniqueness, and rights metadata state transitions.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_assets.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests reject unsupported executable/text payloads.
    - Tests prove two uploads of the same filename get distinct keys.
    - Tests prove unconfirmed rights cannot be used for generation/export.
  </acceptance_criteria>
  <done>Asset behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement storage helpers and asset service</name>
  <files>services/core/src/caragent_core/storage.py, services/core/src/caragent_core/services/assets.py</files>
  <action>Implement object-key generation, filename sanitization, upload validation, checksum calculation where practical, and asset create/update-rights/list operations. Add a storage client abstraction that can use S3/MinIO in integration paths and an in-memory/fake implementation in unit tests.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_assets.py</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Service stores metadata/object keys, not raw binary payloads in DB models.
    - Object keys include workspace ID and a record-specific UUID.
    - Rights/source updates are timestamped and auditable.
  </acceptance_criteria>
  <done>Core asset and storage logic is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add asset API routes</name>
  <files>services/api/src/caragent_api/routes/assets.py, services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/main.py</files>
  <action>Add multipart upload support, list workspace assets, read asset metadata, and update rights/source metadata routes. Use API-mediated upload. Validate workspace existence before storing. Map domain validation errors to 400/422 and missing records to 404 without leaking storage secrets.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_assets.py</automated>
  </verify>
  <acceptance_criteria>
    - Upload route accepts supported image/reference payloads only.
    - Rights update route records source/rights data.
    - Asset list route returns metadata and rights state, not object-storage credentials.
  </acceptance_criteria>
  <done>Asset APIs are exposed.</done>
</task>

<task type="auto">
  <name>Task 4: Add API asset tests</name>
  <files>services/api/tests/test_assets.py</files>
  <action>Add tests for successful upload, unsupported type rejection, too-large rejection if size limit is implemented, list assets by workspace, update rights metadata, and block use of assets without confirmed rights through the core service method intended for later job creation.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_assets.py</automated>
  </verify>
  <acceptance_criteria>
    - DATA-03 and DATA-04 are covered at API/service level.
    - Tests use fake storage unless explicitly marked Docker integration.
  </acceptance_criteria>
  <done>Asset upload and rights behavior is test-backed.</done>
</task>

</tasks>

<verification>
Run core asset tests and API asset tests. After contract generation, confirm asset schemas appear in OpenAPI.
</verification>

<success_criteria>
- Uploaded asset metadata persists.
- Object keys are immutable and scoped.
- Rights/source metadata gates later generation/export use.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-04-SUMMARY.md`.
</output>

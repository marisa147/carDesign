---
phase: 02-durable-data-jobs-and-assets
plan: "05"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/src/caragent_core/repositories/jobs.py"
  - "services/core/tests/test_jobs.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/tests/test_jobs.py"
autonomous: true
requirements: ["DATA-02", "DATA-05", "DATA-06", "DATA-07"]
must_haves:
  truths:
    - "Generation job state and events are durable PostgreSQL records."
    - "Duplicate workspace/idempotency-key job creation returns the original job."
    - "Cost and model-run fields exist and are nullable until real provider integration."
---

<objective>
Implement durable jobs, append-only events, idempotency, model-run/cost records, design versions, artifacts, feedback, and export service coverage.
</objective>

<context>
@.planning/phases/02-durable-data-jobs-and-assets/02-CONTEXT.md
@.planning/phases/02-durable-data-jobs-and-assets/02-RESEARCH.md
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05A | Repudiation | Job status changes | mitigate | Append job events for transitions and preserve event ordering. |
| T-02-05B | Tampering | Idempotency | mitigate | Enforce workspace-scoped uniqueness and return the original row on duplicate keys. |
| T-02-05C | Information Disclosure | Failure/cost payloads | mitigate | Store sanitized failure messages and do not expose secrets in model parameters. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add job service tests first</name>
  <files>services/core/tests/test_jobs.py</files>
  <action>Write failing tests for create job with idempotency key, duplicate key returning original job, append event ordering, status transition persistence across sessions, creating local simulation model-run records, creating immutable artifacts/design versions, and recording feedback/export rows.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Duplicate job test asserts row count remains one.
    - Event test asserts chronological order and append-only behavior.
    - Cost fields can be null and can be updated with numeric values.
  </acceptance_criteria>
  <done>Job ledger behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement job repositories and services</name>
  <files>services/core/src/caragent_core/repositories/jobs.py, services/core/src/caragent_core/services/jobs.py</files>
  <action>Implement typed functions for create idempotent job, get job, list workspace jobs, append event, transition status, create model run, create artifact, create design version, record feedback, and record export. Keep operation names generic enough for Phase 3 real generation to reuse.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Service functions do not import FastAPI, Celery, or provider SDKs.
    - Duplicate idempotency handling is atomic enough for the selected database strategy.
    - Events are append-only through public service APIs.
  </acceptance_criteria>
  <done>Core job ledger services exist.</done>
</task>

<task type="auto">
  <name>Task 3: Add job API routes</name>
  <files>services/api/src/caragent_api/routes/jobs.py, services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/main.py</files>
  <action>Add routes to create/list workspace jobs, read a job, list job events, and minimally list versions/artifacts/model-runs/feedback/exports needed to prove DATA-02 persistence. Require an idempotency key on job creation. Return the existing job for duplicate keys with a response field or status detail that makes idempotent reuse visible.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Job creation without idempotency key is rejected.
    - Duplicate creation returns the same job ID.
    - Job status reads come from durable storage.
  </acceptance_criteria>
  <done>Job APIs are exposed.</done>
</task>

<task type="auto">
  <name>Task 4: Add API job tests</name>
  <files>services/api/tests/test_jobs.py</files>
  <action>Add tests for job creation, duplicate idempotency key, listing jobs, reading events, preserving status after a new client/session, and exposing nullable estimated/actual cost fields. Include at least one test that creates a local simulation design version/artifact/model-run through service/API path and reads it back.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - DATA-05, DATA-06, and DATA-07 are covered by API-level tests.
    - DATA-02 supporting records are represented in tests.
  </acceptance_criteria>
  <done>Job API behavior is test-backed.</done>
</task>

</tasks>

<verification>
Run core job tests, API job tests, and then contracts generation/checking in plan 02-06.
</verification>

<success_criteria>
- Durable jobs/events are the source of truth.
- Idempotency prevents duplicate job rows.
- Cost/model-run/version/artifact/feedback/export records exist and are readable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-05-SUMMARY.md`.
</output>

---
phase: 02-durable-data-jobs-and-assets
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - "services/core/src/caragent_core/repositories/workspaces.py"
  - "services/core/src/caragent_core/services/workspaces.py"
  - "services/core/tests/test_workspaces.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/workspaces.py"
  - "services/api/src/caragent_api/main.py"
  - "services/api/tests/test_workspaces.py"
autonomous: true
requirements: ["DATA-01", "DATA-02"]
must_haves:
  truths:
    - "API can create and resume workspaces from durable storage."
    - "Conversation messages and structured briefs persist and list by workspace."
    - "Workspace APIs use typed Pydantic schemas and appear in OpenAPI."
---

<objective>
Implement durable workspace, conversation message, and structured brief services and API routes.
</objective>

<context>
@.planning/phases/02-durable-data-jobs-and-assets/02-CONTEXT.md
@services/api/src/caragent_api/main.py
@services/api/tests/test_health.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03A | Information Disclosure | Workspace read APIs | mitigate | Return only records scoped to the requested workspace ID; do not expose DB internals or secrets. |
| T-02-03B | Tampering | Message creation | mitigate | Validate role/content and reject empty or malformed payloads. |
| T-02-03C | Repudiation | Conversation history | mitigate | Persist timestamps and message role/source for every message. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace service tests first</name>
  <files>services/core/tests/test_workspaces.py</files>
  <action>Write failing tests for creating a workspace, resuming by ID, appending ordered messages, and storing/listing structured briefs. Use an isolated async test database fixture. Assert data survives a new session from the same database.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_workspaces.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before repository/service implementation.
    - Tests assert a second session can read rows written by the first session.
  </acceptance_criteria>
  <done>Workspace persistence behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement workspace repositories and services</name>
  <files>services/core/src/caragent_core/repositories/workspaces.py, services/core/src/caragent_core/services/workspaces.py</files>
  <action>Implement typed service functions for create workspace, get workspace, list workspace summary, create message, list messages, create design brief, and list briefs. Keep route-independent logic in core. Raise typed domain exceptions for not found and validation errors rather than FastAPI exceptions.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_workspaces.py</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Services return domain/model data without importing FastAPI.
    - Message ordering is deterministic by creation/order fields.
    - Missing workspace returns a typed domain error.
  </acceptance_criteria>
  <done>Core workspace services are implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add workspace API schemas and routes</name>
  <files>services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/routes/workspaces.py, services/api/src/caragent_api/main.py</files>
  <action>Create Pydantic request/response schemas for workspace, message, and brief operations. Add routes for creating a workspace, reading a workspace, listing/creating messages, and listing/creating briefs. Include the router from `create_app`. Expand CORS methods beyond GET for the new POST endpoints.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_workspaces.py</automated>
  </verify>
  <acceptance_criteria>
    - API responses use Pydantic schemas, not raw ORM objects.
    - Route handlers delegate to core services.
    - `/health` continues to pass existing tests.
  </acceptance_criteria>
  <done>Workspace APIs are exposed.</done>
</task>

<task type="auto">
  <name>Task 4: Add API route tests</name>
  <files>services/api/tests/test_workspaces.py</files>
  <action>Add route tests that create a workspace, add two messages, add a structured brief, and read them back through a new request/client context. Assert response schemas contain stable IDs, timestamps, and content fields. Test 404 for a missing workspace.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_workspaces.py tests/test_health.py</automated>
  </verify>
  <acceptance_criteria>
    - DATA-01 is covered by an API-level test.
    - Structured brief persistence is covered.
  </acceptance_criteria>
  <done>Workspace API behavior is test-backed.</done>
</task>

</tasks>

<verification>
Run core workspace tests, API workspace tests, existing API health tests, and `pnpm contracts:generate` after routes are added in plan 02-06.
</verification>

<success_criteria>
- Workspaces, messages, and briefs persist across sessions.
- API exposes typed create/resume/list operations.
- Existing health route behavior is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-03-SUMMARY.md`.
</output>

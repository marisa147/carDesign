---
phase: 02-durable-data-jobs-and-assets
plan: "09"
type: execute
wave: 6
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-06", "02-07", "02-08"]
files_modified:
  - "scripts/smoke-local.mjs"
  - "scripts/validate-all.mjs"
  - "scripts/check-env-examples.mjs"
  - ".env.example"
  - "services/api/.env.example"
  - "services/worker/.env.example"
  - "docs/development.md"
  - "README.md"
  - ".planning/phases/02-durable-data-jobs-and-assets/02-VERIFICATION.md"
autonomous: true
requirements: ["DATA-01", "DATA-02", "DATA-03", "DATA-04", "DATA-05", "DATA-06", "DATA-07"]
must_haves:
  truths:
    - "Aggregate validation covers Phase 2 persistence, contracts, worker, web, and smoke paths."
    - "Docker-backed local smoke exercises PostgreSQL/Redis/MinIO and the new durable data path where practical."
    - "Docs tell developers how to run migrations, data smoke, and Phase 2 UAT."
---

<objective>
Finalize Phase 2 validation, local smoke, documentation, and verification report.
</objective>

<context>
@.planning/phases/02-durable-data-jobs-and-assets/02-VALIDATION.md
@scripts/smoke-local.mjs
@scripts/validate-all.mjs
@docs/development.md
@.planning/phases/01-foundation-and-contracts/01-VERIFICATION.md
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09A | Repudiation | Verification evidence | mitigate | Record exact commands and outcomes in `02-VERIFICATION.md`. |
| T-02-09B | Denial of Service | Smoke script | mitigate | Keep Docker checks bounded with timeouts and clear cleanup instructions. |
| T-02-09C | Information Disclosure | Env/docs | mitigate | Env examples remain local-only and contain no real secrets. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend smoke and validation scripts</name>
  <files>scripts/smoke-local.mjs, scripts/validate-all.mjs</files>
  <action>Update smoke/validation scripts so Phase 2 checks can run from root. Preserve existing Docker daemon behavior and Phase 1 service checks. Add a bounded data smoke path that verifies API migration readiness and, where practical, a small DB/object-storage round trip using local PostgreSQL/MinIO. Keep non-Docker unavailable behavior truthful.</action>
  <verify>
    <automated>pnpm smoke:local --allow-docker-unavailable</automated>
    <automated>pnpm validate</automated>
  </verify>
  <acceptance_criteria>
    - Smoke still fails by default when Docker is unavailable.
    - Allow flag still states checks were not performed when Docker is missing.
    - Validation includes core/API/worker/web/contracts.
  </acceptance_criteria>
  <done>Root verification includes Phase 2 paths.</done>
</task>

<task type="auto">
  <name>Task 2: Update env examples and guards</name>
  <files>.env.example, services/api/.env.example, services/worker/.env.example, scripts/check-env-examples.mjs</files>
  <action>Update examples/guards for any new settings such as database migration URL, upload limits, or local storage toggles. Keep secrets as local-only example values. Do not introduce real provider keys or enable provider calls.</action>
  <verify>
    <automated>node scripts/check-env-examples.mjs</automated>
  </verify>
  <acceptance_criteria>
    - New settings are documented consistently across examples and guards.
    - Provider calls remain disabled by default.
    - No real secrets are committed.
  </acceptance_criteria>
  <done>Configuration examples match Phase 2 code.</done>
</task>

<task type="auto">
  <name>Task 3: Update developer docs</name>
  <files>README.md, docs/development.md</files>
  <action>Document the Phase 2 durable data commands: core validation, Alembic migration, API product routes, contract refresh/check, local smoke, and human UAT. Keep boundaries clear: no real generation, full workbench, auth, billing, or production export in Phase 2.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=fs.readFileSync('docs/development.md','utf8'); for (const t of ['Alembic','workspace','idempotency','pnpm validate','pnpm smoke:local']) if (!d.includes(t)) throw new Error('missing docs token '+t)"</automated>
  </verify>
  <acceptance_criteria>
    - Docs explain how to run migrations.
    - Docs explain how to perform durable refresh/status UAT.
    - Docs do not claim real image generation is implemented.
  </acceptance_criteria>
  <done>Developers can run and verify Phase 2 locally.</done>
</task>

<task type="auto">
  <name>Task 4: Run final verification and write report</name>
  <files>.planning/phases/02-durable-data-jobs-and-assets/02-VERIFICATION.md</files>
  <action>Run final commands: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm contracts:check`, `pnpm validate`, Docker-backed `pnpm infra:up`, `pnpm smoke:local`, and `pnpm infra:down` when Docker is available. Write `02-VERIFICATION.md` with exact outcomes, residual warnings, human UAT status, requirements coverage, and gaps if any.</action>
  <verify>
    <automated>Test file exists and records every DATA requirement as satisfied, partial, or gap with evidence.</automated>
  </verify>
  <acceptance_criteria>
    - Verification report does not mark a command passed unless it was run and returned exit 0.
    - Docker cleanup is recorded after smoke.
    - Any host-gated limitation is explicit.
  </acceptance_criteria>
  <done>Phase 2 verification report is ready for `$gsd-verify-work 2`.</done>
</task>

</tasks>

<verification>
Run full root validation, contract checks, and Docker-backed smoke. Then inspect `git status --short` and ensure only intended Phase 2 files changed.
</verification>

<success_criteria>
- Phase 2 can be verified from root commands.
- Docs and env examples match implementation.
- Verification report covers DATA-01 through DATA-07.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-09-SUMMARY.md`.
</output>

# Phase 32 Plan: GPT Sectioned Generation Pipeline

## Implementation Steps

1. Core section plan
   - Add helpers in prompt planning to resolve selected template sections.
   - Build `section_design_plan` with overall direction and one prompt per section.
   - Include selected/regeneration section id when targeted edit metadata points at a section.

2. Worker trace
   - Add section design summary to model_run parameters.
   - Ensure provider result metadata, artifact metadata, version parameters, job operations, and final event include section ids/template/provider trace.

3. Tests
   - Add core prompt plan tests for GR86/BRZ section prompts.
   - Add worker persistence test asserting section trace across durable records.

4. Planning state
   - Mark GPTD-01..04 complete after focused validation.

## Verification

- `cd services/core && uv run pytest -q tests/test_generation_prompts.py` or existing prompt tests.
- `cd services/worker && uv run pytest -q tests/test_generation_tasks.py -k section`
- `cd services/core && uv run ruff check src tests`
- `cd services/worker && uv run ruff check src tests/test_generation_tasks.py`

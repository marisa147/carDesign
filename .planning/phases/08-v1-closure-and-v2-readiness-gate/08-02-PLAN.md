---
phase: 08-v1-closure-and-v2-readiness-gate
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - ".env.example"
  - "services/api/.env.example"
  - "services/worker/.env.example"
  - "apps/web/.env.example"
  - "services/api/src/caragent_api/config.py"
  - "services/worker/src/caragent_worker/config.py"
  - "apps/web/src/lib/config/public-env.ts"
  - "services/api/tests/test_config.py"
  - "services/worker/tests/test_config.py"
  - "apps/web/src/lib/config/public-env.test.ts"
autonomous: true
requirements: ["V2-READY-03", "V2-READY-04"]
must_haves:
  truths:
    - "V2 feature flags are present but disabled by default."
    - "Local deterministic provider remains available without hosted credentials."
    - "Browser-readable flags never expose secrets or server-only provider config."
  artifacts:
    - ".env.example"
    - "services/api/src/caragent_api/config.py"
    - "services/worker/src/caragent_worker/config.py"
    - "apps/web/src/lib/config/public-env.ts"
  key_links:
    - "env examples -> typed API/worker settings"
    - "non-secret public env -> web future gates"
    - "local mode -> hosted provider credentials optional"
---

<objective>
Add inert, default-off V2 feature flag scaffolding across env examples and typed settings while proving V1 local-only behavior still works without hosted credentials.

Purpose: Later V2 phases need independent rollout switches, but Phase 8 must not activate new behavior.
Output: Documented defaults, typed settings, and focused config tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-CONTEXT.md
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-RESEARCH.md
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-VALIDATION.md
@.env.example
@services/api/src/caragent_api/config.py
@services/worker/src/caragent_worker/config.py
@apps/web/src/lib/config/public-env.ts
@services/api/tests/test_config.py
@services/worker/tests/test_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify default-off V2 config behavior</name>
  <files>services/api/tests/test_config.py, services/worker/tests/test_config.py, apps/web/src/lib/config/public-env.test.ts</files>
  <behavior>
    - API and worker expose V2 readiness flags with false defaults.
    - Local mode accepts empty hosted provider credentials when V2 hosted flags are false.
    - Public web env exposes only non-secret feature availability flags.
  </behavior>
  <action>Add or extend focused config tests for default-off V2 flags. If `apps/web/src/lib/config/public-env.test.ts` does not exist, create it using the app's Vitest conventions.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_config.py</automated>
    <automated>cd ../worker && uv run pytest -q tests/test_config.py</automated>
    <automated>cd ../.. && corepack pnpm --filter @caragent/web test -- --run apps/web/src/lib/config/public-env.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation if V2 flags are absent.
    - Tests assert false/default-off behavior.
    - Tests do not require real provider keys.
  </acceptance_criteria>
  <done>Default-off expectations are locked.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed V2 flag settings and env examples</name>
  <files>.env.example, services/api/.env.example, services/worker/.env.example, apps/web/.env.example, services/api/src/caragent_api/config.py, services/worker/src/caragent_worker/config.py, apps/web/src/lib/config/public-env.ts</files>
  <action>Add clearly named V2 flags for hosted provider rollout, targeted editing, reference guidance, lightweight 3D preview, and enhanced handoff package. Defaults must be false/off. Keep server-only/provider-cost flags in API/worker settings and expose only non-secret UI capability flags through web public env.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_config.py && uv run ruff check . && uv run mypy src</automated>
    <automated>cd ../worker && uv run pytest -q tests/test_config.py && uv run ruff check . && uv run mypy src</automated>
    <automated>cd ../.. && corepack pnpm --filter @caragent/web test -- --run apps/web/src/lib/config/public-env.test.ts && corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Local `.env.example` still shows hosted provider calls disabled.
    - New flags do not change current provider routing or workbench behavior.
    - No API keys or server-only values are read by browser code.
  </acceptance_criteria>
  <done>V2 flags exist and are inert by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env files to server settings | Local/operator config controls runtime behavior |
| Server settings to browser env | Only non-secret flags may cross into frontend code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | web public env | mitigate | Keep secrets and provider config server-side; test browser env does not include key-like names. |
| T-08-04 | Elevation of Privilege | feature flags | mitigate | Default V2 feature flags to false and require explicit enablement in later owning phases. |
| T-08-05 | Denial of Service | hosted provider config | mitigate | Preserve existing hosted quota/cost guards and do not enable hosted calls by default. |
</threat_model>

<verification>
Run API/worker config tests, web public env tests, ruff, mypy, and web typecheck.
</verification>

<success_criteria>
- V2-READY-03 is covered by default-off flags.
- V2-READY-04 is covered by local-only/no-secret config tests.
- Browser env remains non-secret.
</success_criteria>

<output>
After completion, create `.planning/phases/08-v1-closure-and-v2-readiness-gate/08-02-SUMMARY.md`.
</output>

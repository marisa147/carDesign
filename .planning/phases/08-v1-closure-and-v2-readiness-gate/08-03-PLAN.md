---
phase: 08-v1-closure-and-v2-readiness-gate
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - "scripts/check-v1-compatibility.mjs"
  - "package.json"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
  - "docs/development.md"
autonomous: true
requirements: ["V2-READY-02", "V2-READY-03"]
must_haves:
  truths:
    - "FastAPI/Pydantic OpenAPI remains the API contract source."
    - "Existing v1 route/schema/client surfaces are checked before V2 schemas expand."
    - "Compatibility checks are runnable from root commands."
  artifacts:
    - "scripts/check-v1-compatibility.mjs"
    - "package.json"
    - "docs/development.md"
  key_links:
    - "OpenAPI artifact -> generated TS client"
    - "v1 route/schema compatibility -> V2 extension safety"
    - "root script -> readiness report evidence"
---

<objective>
Add a V1 contract compatibility gate that verifies key v1 workbench, job, artifact, version, operations, and PreviewSpec contract surfaces remain available before V2 schema extensions.

Purpose: V2 must extend the v1 ledger and contract shape without silently breaking existing clients.
Output: A root-runnable compatibility check and documentation.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-CONTEXT.md
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-RESEARCH.md
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-VALIDATION.md
@packages/contracts/openapi/openapi.json
@packages/contracts/src/generated/client.ts
@scripts/check-contracts.mjs
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify V1 compatibility checker expectations</name>
  <files>scripts/check-v1-compatibility.mjs</files>
  <behavior>
    - Check committed OpenAPI includes key v1 routes such as generation, jobs, cancel, operations status, feedback/export/iteration, and health.
    - Check generated client includes corresponding helpers/types.
    - Check `DesignVersion`/artifact parameter surfaces still allow `preview_spec` metadata.
  </behavior>
  <action>Create `scripts/check-v1-compatibility.mjs` with clear assertions and actionable error messages. Start with dry static checks against committed generated artifacts; do not require running the API server.</action>
  <verify>
    <automated>node scripts/check-v1-compatibility.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The script fails clearly if a required v1 route/helper/schema disappears.
    - The script does not require network access or provider credentials.
  </acceptance_criteria>
  <done>Compatibility expectations are executable.</done>
</task>

<task type="auto">
  <name>Task 2: Wire compatibility gate into root scripts and docs</name>
  <files>package.json, docs/development.md</files>
  <action>Add a root script such as `compat:v1` that runs `node scripts/check-v1-compatibility.mjs`. Document when to run it and how it relates to `pnpm contracts:check`. Do not replace `contracts:check`; compatibility is an additional V2 readiness gate.</action>
  <verify>
    <automated>corepack pnpm compat:v1</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Root package scripts expose the compatibility check.
    - Development docs name both `pnpm compat:v1` and `pnpm contracts:check`.
    - Generated artifacts remain in sync.
  </acceptance_criteria>
  <done>Compatibility check is part of the readiness command surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated contracts to web callers | Compatibility script guards client-visible API shape |
| Static generated artifacts to readiness evidence | Static checks influence whether V2 work proceeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | generated contract artifacts | mitigate | Pair compatibility script with `pnpm contracts:check` so stale generated files are detected. |
| T-08-07 | Repudiation | readiness evidence | mitigate | Compatibility check prints explicit missing surface names and is recorded in verification. |
</threat_model>

<verification>
Run `corepack pnpm compat:v1` and `corepack pnpm contracts:check`.
</verification>

<success_criteria>
- V1 contract compatibility has a root-runnable gate.
- V2-READY-02 includes contract drift and compatibility evidence.
- V2-READY-03 feature flag scaffolding does not break existing generated clients.
</success_criteria>

<output>
After completion, create `.planning/phases/08-v1-closure-and-v2-readiness-gate/08-03-SUMMARY.md`.
</output>

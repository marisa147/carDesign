---
phase: 08-v1-closure-and-v2-readiness-gate
plan: "04"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - ".planning/phases/08-v1-closure-and-v2-readiness-gate/08-MIGRATION-SAFETY.md"
  - "scripts/check-migration-safety.mjs"
  - "package.json"
  - "docs/development.md"
autonomous: true
requirements: ["V2-READY-02", "V2-READY-03"]
must_haves:
  truths:
    - "Phase 8 either proves no schema migration is needed or documents a safe migration boundary."
    - "Existing v1 durable ledger tables remain canonical."
    - "Migration checks are runnable without hosted provider credentials."
  artifacts:
    - ".planning/phases/08-v1-closure-and-v2-readiness-gate/08-MIGRATION-SAFETY.md"
    - "scripts/check-migration-safety.mjs"
    - "package.json"
  key_links:
    - "Alembic versions -> migration safety report"
    - "core models -> v1 durable ledger compatibility"
    - "root script -> readiness verification"
---

<objective>
Create a migration safety review and no-op migration proof for V2 readiness.

Purpose: V2 schema extensions must not start from an unknown database state or accidentally reinterpret v1 durable records.
Output: Migration safety artifact, optional static checker, and root script/docs.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-CONTEXT.md
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-RESEARCH.md
@.planning/phases/08-v1-closure-and-v2-readiness-gate/08-VALIDATION.md
@services/api/alembic/versions/
@services/core/src/caragent_core/models.py
@services/api/pyproject.toml
@docs/development.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration safety review</name>
  <files>.planning/phases/08-v1-closure-and-v2-readiness-gate/08-MIGRATION-SAFETY.md</files>
  <action>Create a migration safety document that identifies current Alembic heads, v1 canonical ledger tables/records, whether Phase 8 introduces any schema migration, and what future V2 migrations must preserve. State explicitly if Phase 8 is no-op for schema.</action>
  <verify>
    <automated>Test-Path .planning/phases/08-v1-closure-and-v2-readiness-gate/08-MIGRATION-SAFETY.md</automated>
    <automated>Select-String -Path .planning/phases/08-v1-closure-and-v2-readiness-gate/08-MIGRATION-SAFETY.md -Pattern "no-op","Alembic","V2-READY-02"</automated>
  </verify>
  <acceptance_criteria>
    - The report distinguishes schema changes from config/docs/script changes.
    - The report names v1 ledger concepts that must stay readable.
    - Future V2 migration constraints are explicit.
  </acceptance_criteria>
  <done>Migration safety is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Add migration safety checker and root script</name>
  <files>scripts/check-migration-safety.mjs, package.json, docs/development.md</files>
  <action>Add a lightweight static checker that validates expected Alembic directory/head files exist and reports the current migration chain. Add a root script such as `migration:safety` and document the live host command `cd services/api && uv run alembic upgrade head && uv run alembic current` for full verification.</action>
  <verify>
    <automated>corepack pnpm migration:safety</automated>
    <manual>cd services/api && uv run alembic upgrade head && uv run alembic current</manual>
  </verify>
  <acceptance_criteria>
    - Static checker runs without Docker or database access.
    - Docs distinguish static safety check from live Alembic verification.
    - No Phase 8 schema migration is added unless explicitly justified.
  </acceptance_criteria>
  <done>Migration safety has static and live verification paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migrations to durable ledger | Schema changes affect stored v1 jobs, artifacts, versions, exports, and model runs |
| Static checker to live database state | Static checks are helpful but cannot replace live migration verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | migrations | mitigate | Document current Alembic chain and require live `upgrade head` before sign-off. |
| T-08-09 | Denial of Service | database startup | mitigate | Keep static checker available for sandbox, but do not count it as live DB proof. |
</threat_model>

<verification>
Run `corepack pnpm migration:safety` and, on a prepared host, `cd services/api && uv run alembic upgrade head && uv run alembic current`.
</verification>

<success_criteria>
- V2-READY-02 includes migration-safety evidence.
- No Phase 8 change silently mutates database schema.
- Future V2 migration rules are documented before feature phases begin.
</success_criteria>

<output>
After completion, create `.planning/phases/08-v1-closure-and-v2-readiness-gate/08-04-SUMMARY.md`.
</output>

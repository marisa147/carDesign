# Phase 30 Plan: Construction Brief And Smart Q&A

## Implementation Steps

1. Requirement completion helper
   - Add frontend helpers to derive required field state from `GenerationBriefPayload`.
   - Core required fields: vehicle template, character/theme, main color, wrap range, text/logo.
   - Derive first missing follow-up question and supplementation intent from the original request.

2. Chat panel controls and status
   - Add `onClearConversation` prop.
   - Render required completion status, first missing question, and GPT supplementation cue.
   - Add a clear button that removes local chat transcript/draft without clearing current brief.

3. Workbench state handler
   - Add `handleClearConversation` in `WorkbenchApp`.
   - Preserve workspace/current brief/assets/jobs/provider state; clear local messages and draft.

4. Construction brief panel
   - Add grouped construction-order summary to `ParameterPanel`.
   - Show missing values and warnings without blocking save/generate.
   - Reuse existing save/new/archive controls for draft save/discard behavior.

5. Tests and state
   - Extend `chat-panel.test.tsx` for missing-field question, GPT supplementation cue, and clear control.
   - Add focused ParameterPanel rendering coverage if existing test harness allows it.
   - Update GSD requirements and summaries after verification.

## Verification

- `apps\\web\\node_modules\\.bin\\tsc.CMD --noEmit -p apps\\web\\tsconfig.json`
- Targeted Vitest for chat/parameter tests if local jsdom runtime permits; otherwise document existing blocker.

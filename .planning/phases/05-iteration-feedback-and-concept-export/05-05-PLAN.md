---
phase: 05-iteration-feedback-and-concept-export
plan: "05"
type: execute
wave: 5
depends_on: ["05-03", "05-04"]
files_modified:
  - "apps/web/src/components/workbench/feedback-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["ITER-04"]
---

<objective>
Add durable feedback, rating, approval, rejection, and comment UI for selected versions.
</objective>

<context>
@.planning/phases/05-iteration-feedback-and-concept-export/05-CONTEXT.md
@.planning/phases/05-iteration-feedback-and-concept-export/05-UI-SPEC.md
@apps/web/src/components/workbench/workbench-app.tsx
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05A | Repudiation | feedback | mitigate | Persist durable feedback records with version id and timestamp. |
| T-05-05B | Tampering | selected version | mitigate | Submit feedback only for current selected version id. |
| T-05-05C | UX integrity | approval/rejection | mitigate | Approval/rejection must not delete artifacts or hide previous versions. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Add feedback UI tests</name>
  <files>apps/web/src/app/page.test.tsx</files>
  <action>Add failing tests for rating selection, approve/reject state, comment submission, exact feedback request payload, saved feedback history rendering, and disabled state without a selected version.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover rating 1-5 accessible names.
    - Tests prove feedback stays scoped to the selected version.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement feedback panel</name>
  <files>apps/web/src/components/workbench/feedback-panel.tsx</files>
  <action>Add rating, approve/reject/neutral controls, comment textarea, submit state, error/saved state, and feedback history list.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Control states are accessible and mobile-safe.
    - Existing saved feedback is visible without implying edit/delete support.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire feedback API and refresh behavior</name>
  <files>apps/web/src/components/workbench/workbench-app.tsx</files>
  <action>Load feedback for the workspace, submit new feedback through wrappers, update Query cache/local state, and preserve selected version context.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Feedback submit does not mutate browser storage.
    - Error state does not clear existing feedback history.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run focused page tests, web typecheck, and web lint.
</verification>

<success_criteria>
- Users can rate, approve, reject, or comment on a selected design version.
- Feedback is durable and visible after refresh.
</success_criteria>

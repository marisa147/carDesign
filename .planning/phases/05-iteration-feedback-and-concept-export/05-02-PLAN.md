---
phase: 05-iteration-feedback-and-concept-export
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/generation.py"
  - "services/api/tests/test_generation.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/tests/test_generation_tasks.py"
autonomous: true
requirements: ["ITER-01", "ITER-02", "ITER-03"]
---

<objective>
Add child iteration submission so regenerating from a selected version preserves parent lineage.
</objective>

<context>
@.planning/phases/05-iteration-feedback-and-concept-export/05-CONTEXT.md
@.planning/phases/05-iteration-feedback-and-concept-export/05-RESEARCH.md
@services/api/src/caragent_api/routes/generation.py
@services/worker/src/caragent_worker/tasks/jobs.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02A | Tampering | parent version metadata | mitigate | Validate parent version belongs to workspace before job creation. |
| T-05-02B | Repudiation | iteration lineage | mitigate | Store parent version id and change request in durable job metadata/model-run parameters. |
| T-05-02C | Data integrity | worker version creation | mitigate | Worker must pass parent id into `create_design_version` and tests assert parent/depth. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Add child iteration API and worker tests</name>
  <files>services/api/tests/test_generation.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Add failing tests for submitting an iteration from a parent version, rejecting missing/wrong-workspace parent versions, recording metadata, and worker-created child `DesignVersion` lineage.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py</automated>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove the parent version remains readable and unchanged.
    - Worker result includes a child version id whose row has `parent_version_id`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add iteration submission route and schema</name>
  <files>services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/routes/generation.py</files>
  <action>Add an iteration request schema with `brief_id`, `idempotency_key`, `requested_by`, `change_request`, and optional `parameter_overrides`. Add a route scoped to workspace + parent version that creates a generation job with parent metadata.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py</automated>
  </verify>
  <acceptance_criteria>
    - Route response reuses generation submission response shape where possible.
    - Job metadata contains parent version id and change request.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve lineage in worker generation</name>
  <files>services/worker/src/caragent_worker/tasks/jobs.py</files>
  <action>Read parent version id/change request from job metadata and pass parent id to `create_design_version`. Include change-request metadata in version/model-run parameters when available.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Normal non-iteration generation still creates root versions.
    - Iteration generation creates child versions with incremented lineage depth.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run focused API/worker generation tests and service lint/type checks for touched Python services.
</verification>

<success_criteria>
- Users can request regeneration from an existing version without overwriting it.
- Parent-child lineage is durable and visible through existing version list APIs.
</success_criteria>

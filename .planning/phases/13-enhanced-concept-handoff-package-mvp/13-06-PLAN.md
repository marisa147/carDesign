---
phase: 13-enhanced-concept-handoff-package-mvp
plan: "06"
type: execute
wave: 5
depends_on: ["13-04", "13-05"]
files_modified:
  - "services/core/src/caragent_core/handoff.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/tests/test_jobs.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/tests/test_jobs.py"
  - "apps/web/src/components/workbench/export-panel.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["V2-HANDOFF-05", "V2-HANDOFF-02", "V2-HANDOFF-03"]
must_haves:
  truths:
    - "Rights/source validation is server-authoritative."
    - "Missing required rights/source metadata blocks ZIP package creation."
    - "Optional missing 3D screenshot evidence remains a warning."
  artifacts:
    - "Core guardrail helper and tests."
    - "API failure-state tests."
    - "Workbench blocked-state UI tests."
  key_links:
    - "13-CONTEXT -> Rights And Source Guardrails"
    - "13-RESEARCH -> Rights And Safety Strategy"
    - "13-VALIDATION -> 13-06-01"
---

<objective>
Add rights/source guardrails and blocked-export failure states for enhanced handoff packages across core, API, and workbench UI.

Purpose: Phase 13 must not package a review handoff that lacks required rights/source evidence for included references.
Output: server-authoritative validation and visible blocked/warning states.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-CONTEXT.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-RESEARCH.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-UI-SPEC.md
@services/core/src/caragent_core/references.py
@services/core/src/caragent_core/handoff.py
@services/api/src/caragent_api/routes/jobs.py
@apps/web/src/components/workbench/export-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red guardrail tests</name>
  <read_first>
    - `services/core/tests/test_jobs.py`
    - `services/api/tests/test_jobs.py`
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>services/core/tests/test_jobs.py, services/api/tests/test_jobs.py, apps/web/src/app/page.test.tsx</files>
  <action>Add failing tests for missing/rejected reference rights/source metadata. Core/API tests should seed a version whose `included_reference_asset_ids` contains an asset id but `rights_snapshot` is missing, rejected, or lacks both `source_label` and `source_url`; enhanced ZIP export must fail with a deterministic validation message containing `rights/source metadata`. Web tests should render a selected version with missing rights/source and assert `缺少版权或来源信息` is visible and `生成交接包` is disabled.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because enhanced package path does not validate rights/source guardrails.
    - Tests prove optional no-screenshot state is warning-only, not blocked.
    - Existing PNG/JPG export tests remain unaffected.
  </acceptance_criteria>
  <done>Guardrail behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement core/API rights-source guardrails</name>
  <read_first>
    - `services/core/src/caragent_core/handoff.py`
    - `services/core/src/caragent_core/references.py`
    - `services/api/src/caragent_api/routes/jobs.py`
  </read_first>
  <files>services/core/src/caragent_core/handoff.py, services/api/src/caragent_api/routes/jobs.py, services/core/tests/test_jobs.py, services/api/tests/test_jobs.py</files>
  <action>Implement a helper such as `validate_handoff_rights_source(metadata)` that checks every id in `included_reference_asset_ids` has a matching `rights_snapshot` entry with `rights_status: "confirmed"` and at least one of `source_label` or `source_url`. Treat `omitted_reference_asset_ids` and unsupported roles as warnings. Have the API map this validation failure to 422 with a user-safe message. Include blocked reason in the failed export response only if a failed export row is intentionally recorded; otherwise no export row should be created on validation failure.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py && cd ../api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Missing rights snapshot blocks enhanced ZIP export.
    - `rights_status: "rejected"` blocks enhanced ZIP export.
    - Missing `source_label` and `source_url` blocks enhanced ZIP export.
    - Warning-only optional metadata does not block export.
  </acceptance_criteria>
  <done>Server-authoritative guardrails are enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Implement workbench blocked and warning states</name>
  <read_first>
    - `apps/web/src/components/workbench/export-panel.tsx`
    - `apps/web/src/components/workbench/workbench-app.tsx`
    - `apps/web/src/app/page.test.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/export-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Add UI readiness derivation for enhanced package mode. Mark ZIP blocked when selected version parameters include `included_reference_asset_ids` without confirmed rights/source evidence in `rights_snapshot`, or when selected concept artifact is missing. Mark no `preview_3d_screenshot` artifacts as warning-only. Display `缺少版权或来源信息` for rights/source blockers, visible warning copy for omitted screenshots, and keep PNG/JPG export controls available where the existing logic allows.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `生成交接包` is disabled when rights/source blocker exists.
    - `ZIP` mode still shows package preview rows.
    - Warning-only missing screenshot state does not disable package creation.
    - Existing reference assignment and concept export tests still pass.
  </acceptance_criteria>
  <done>Workbench clearly distinguishes blocked and warning states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reference metadata to package | Rights/source snapshots decide whether references may enter handoff packages. |
| Browser readiness to API validation | Browser state can be stale or manipulated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-16 | Repudiation | rights evidence | mitigate | Require confirmed rights/source snapshot for included references. |
| T-13-17 | Elevation of Privilege | browser guardrail bypass | mitigate | API repeats guardrails and returns 422 before storage write. |
| T-13-18 | Information Disclosure | reference manifest | mitigate | Package only source/rights metadata already intended for trace, no binary reference images unless explicitly included by future scope. |
</threat_model>

<verification>
Run core/API/web focused guardrail tests.
</verification>

<success_criteria>
- V2-HANDOFF-05 blocks incomplete rights/source metadata.
- V2-HANDOFF-02 warning report covers optional missing metadata.
- Existing non-ZIP concept export path remains backward compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/13-enhanced-concept-handoff-package-mvp/13-06-SUMMARY.md`.
</output>

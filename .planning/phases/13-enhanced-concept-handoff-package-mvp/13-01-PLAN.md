---
phase: 13-enhanced-concept-handoff-package-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "services/core/src/caragent_core/storage.py"
  - "services/core/src/caragent_core/handoff.py"
  - "services/core/src/caragent_core/enums.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/tests/test_models.py"
  - "services/core/tests/test_jobs.py"
autonomous: true
requirements: ["V2-HANDOFF-01", "V2-HANDOFF-03", "V2-HANDOFF-04"]
must_haves:
  truths:
    - "Enhanced package format is explicit and does not replace existing PNG/JPG exports."
    - "Package manifest is typed and schema-versioned before API or UI depends on it."
    - "Object storage reads are available before ZIP building includes source artifacts."
  artifacts:
    - "Handoff package Pydantic helpers with schema_version: 1."
    - "`ObjectStorage.get_object` implemented for in-memory and file storage."
    - "Core constants for `enhanced_concept_handoff_zip` and concept-only disclaimer."
  key_links:
    - "13-CONTEXT -> Package Taxonomy"
    - "13-RESEARCH -> Manifest Shape"
    - "13-VALIDATION -> 13-01-01"
---

<objective>
Create the typed handoff package schema foundation, package format taxonomy, and object storage read capability required by Phase 13.

Purpose: later plans need a stable manifest contract and a safe way to read source artifacts before building ZIP package bytes.
Output: core `handoff.py` helpers, storage read API, and focused tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-CONTEXT.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-RESEARCH.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-VALIDATION.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-PATTERNS.md
@services/core/src/caragent_core/storage.py
@services/core/src/caragent_core/enums.py
@services/core/src/caragent_core/services/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for handoff schema and storage reads</name>
  <read_first>
    - `services/core/tests/test_models.py`
    - `services/core/tests/test_jobs.py`
    - `services/core/src/caragent_core/storage.py`
  </read_first>
  <files>services/core/tests/test_models.py, services/core/tests/test_jobs.py</files>
  <action>Add failing tests that require `caragent_core.handoff` to expose `HANDOFF_PACKAGE_SCHEMA_VERSION = 1`, `ENHANCED_HANDOFF_PACKAGE_FORMAT = "enhanced_concept_handoff_zip"`, `HANDOFF_CONCEPT_ONLY_DISCLAIMER`, and a `HandoffPackageManifest` model that rejects missing required ids and forbids unknown top-level fields. Add a storage test proving `InMemoryObjectStorage.get_object("key")` returns a `StoredObject` after `put_object` and raises `FileNotFoundError` for a missing key.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_models.py tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because `caragent_core.handoff` and `ObjectStorage.get_object` do not exist.
    - Tests assert the package format string exactly equals `enhanced_concept_handoff_zip`.
    - Tests assert rendered manifest JSON does not contain `image_base64`, `api_key`, `secret`, or `C:\\`.
  </acceptance_criteria>
  <done>Core schema/storage behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement storage read support</name>
  <read_first>
    - `services/core/src/caragent_core/storage.py`
    - `services/api/src/caragent_api/routes/jobs.py`
    - `services/worker/src/caragent_worker/tasks/jobs.py`
  </read_first>
  <files>services/core/src/caragent_core/storage.py</files>
  <action>Extend `ObjectStorage` with `async def get_object(self, key: str) -> StoredObject`. Implement it in `InMemoryObjectStorage` by returning `self.objects[key]` or raising `FileNotFoundError(key)`. Implement it in `FileObjectStorage` by reading `self.root / key`, returning `StoredObject(content=target.read_bytes(), content_type="application/octet-stream")`, and raising `FileNotFoundError(key)` when the file does not exist. Keep existing `put_object` behavior unchanged.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py tests/test_assets.py</automated>
  </verify>
  <acceptance_criteria>
    - `ObjectStorage` protocol declares both `put_object` and `get_object`.
    - `InMemoryObjectStorage.get_object` returns the original content and content type.
    - `FileObjectStorage.get_object` reads bytes from disk and raises `FileNotFoundError` for missing objects.
    - Existing upload/generation tests that only call `put_object` still pass.
  </acceptance_criteria>
  <done>Object storage can read package source bytes.</done>
</task>

<task type="auto">
  <name>Task 3: Add handoff package schema helpers</name>
  <read_first>
    - `services/core/src/caragent_core/references.py`
    - `services/core/src/caragent_core/preview3d.py`
    - `services/core/src/caragent_core/services/jobs.py`
  </read_first>
  <files>services/core/src/caragent_core/handoff.py, services/core/src/caragent_core/enums.py, services/core/src/caragent_core/services/jobs.py, services/core/tests/test_models.py</files>
  <action>Create `services/core/src/caragent_core/handoff.py` with Pydantic models for `HandoffPackageFile`, `HandoffSourceArtifact`, `HandoffPackageArtifact`, `HandoffPromptTrace`, `HandoffProviderTrace`, `HandoffWarningReport`, `HandoffReferenceManifest`, and `HandoffPackageManifest`. Use `ConfigDict(extra="forbid")`, `schema_version: int = 1`, `package_type: Literal["enhanced_concept_handoff"]`, and `format: Literal["enhanced_concept_handoff_zip"]`. Export `HANDOFF_PACKAGE_SCHEMA_VERSION`, `ENHANCED_HANDOFF_PACKAGE_FORMAT`, and `HANDOFF_CONCEPT_ONLY_DISCLAIMER`. Add `SUPPORTED_CONCEPT_EXPORT_FORMATS` support for `enhanced_concept_handoff_zip` without removing `png` or `jpg`.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_models.py tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - `from caragent_core.handoff import HandoffPackageManifest` succeeds.
    - Manifest dumps include `schema_version: 1`, `package_type: "enhanced_concept_handoff"`, and `format: "enhanced_concept_handoff_zip"`.
    - Unknown manifest fields raise validation errors.
    - Existing `record_export(..., export_format="png")` tests still pass.
  </acceptance_criteria>
  <done>Typed handoff schema and format taxonomy are ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storage to package builder | Stored artifact bytes and object keys are trusted only after workspace/version validation. |
| Manifest to browser/reviewer | Manifest data may be displayed or packaged, so it must be typed and sanitized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | Handoff manifest | mitigate | Type manifest fields, forbid extra top-level keys, and test for no binary/base64/secrets/local paths. |
| T-13-02 | Tampering | Storage object reads | mitigate | Add exact object-key reads and keep ownership validation for later API/package tasks. |
| T-13-03 | Repudiation | Export format taxonomy | mitigate | Persist explicit `enhanced_concept_handoff_zip` format and schema version. |
</threat_model>

<verification>
Run focused core tests: `cd services/core && uv run pytest -q tests/test_models.py tests/test_jobs.py tests/test_assets.py`.
</verification>

<success_criteria>
- V2-HANDOFF-01 has an explicit package format.
- V2-HANDOFF-03 has a typed stable manifest schema.
- V2-HANDOFF-04 keeps export ledger compatibility and prepares immutable package artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/13-enhanced-concept-handoff-package-mvp/13-01-SUMMARY.md`.
</output>

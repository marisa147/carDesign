---
phase: 13-enhanced-concept-handoff-package-mvp
plan: "03"
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - "services/core/src/caragent_core/handoff.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/tests/test_jobs.py"
autonomous: true
requirements: ["V2-HANDOFF-02", "V2-HANDOFF-03", "V2-HANDOFF-04"]
must_haves:
  truths:
    - "ZIP content is built from object storage and typed report helpers."
    - "Required concept image bytes are required; optional screenshots are included when available."
    - "ZIP file names are stable enough for automated inspection."
  artifacts:
    - "Core ZIP package builder."
    - "Tests that inspect `manifest.json`, Markdown files, concept image, and optional screenshots."
    - "No provider calls or browser rendering required."
  key_links:
    - "13-RESEARCH -> ZIP Strategy"
    - "13-PATTERNS -> Core Package And Ledger"
    - "13-VALIDATION -> 13-03-01"
---

<objective>
Create the provider-off ZIP package builder that writes stable handoff package bytes from selected artifact storage, manifest data, and report renderers.

Purpose: API integration should only need to validate request/ownership and then call a tested package builder.
Output: core ZIP builder with package inspection tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-CONTEXT.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-RESEARCH.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-VALIDATION.md
@services/core/src/caragent_core/handoff.py
@services/core/src/caragent_core/storage.py
@services/core/src/caragent_core/services/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red ZIP inspection tests</name>
  <read_first>
    - `services/core/tests/test_jobs.py`
    - `services/core/src/caragent_core/handoff.py`
    - `services/core/src/caragent_core/storage.py`
  </read_first>
  <files>services/core/tests/test_jobs.py</files>
  <action>Add failing tests that seed `InMemoryObjectStorage` with a concept PNG object and one `preview_3d_screenshot` PNG object, call a builder such as `build_handoff_package_zip(...)`, and inspect the resulting bytes with `zipfile.ZipFile`. Assert `namelist()` includes `manifest.json`, `handoff-notes.md`, `warnings.md`, `prompt-trace.md`, `references.json`, `images/concept.png`, and `screenshots/<screenshot-artifact-id>.png`. Assert `manifest.json` parses and has `format: "enhanced_concept_handoff_zip"` and `schema_version: 1`.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because the ZIP builder is missing.
    - Tests assert `images/concept.png` bytes equal the stored concept bytes.
    - Tests assert optional screenshot bytes are included when the artifact exists.
    - Tests assert no ZIP member contains `api_key`, `secret`, `image_base64`, or `C:\\`.
  </acceptance_criteria>
  <done>ZIP builder behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement package file selection and object reads</name>
  <read_first>
    - `services/core/src/caragent_core/handoff.py`
    - `services/core/src/caragent_core/models.py`
    - `services/core/src/caragent_core/storage.py`
  </read_first>
  <files>services/core/src/caragent_core/handoff.py</files>
  <action>Implement package input helpers that resolve the required source concept image artifact and optional screenshot artifacts into package file descriptors. Use source artifact content type or object key suffix to select `images/concept.png`, `images/concept.jpg`, or `images/concept.webp`. Use screenshot artifact ids for screenshot filenames. Raise a handoff validation error when the required concept object cannot be read. Treat missing optional screenshot objects as warning records if metadata says they exist but bytes are missing.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Required concept object read failure raises a deterministic validation error containing `source concept image`.
    - Optional screenshot object read failure records a warning item and does not omit `manifest.json`.
    - File paths inside package use forward slashes and never include local filesystem paths.
  </acceptance_criteria>
  <done>Package builder can safely select and read package artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ZIP byte builder</name>
  <read_first>
    - `services/core/src/caragent_core/handoff.py`
    - `services/core/tests/test_jobs.py`
  </read_first>
  <files>services/core/src/caragent_core/handoff.py, services/core/tests/test_jobs.py</files>
  <action>Implement `build_handoff_package_zip` or equivalent with Python `zipfile` and `io.BytesIO`. Write JSON with sorted keys and 2-space indent for `manifest.json`; write Markdown strings from Plan 13-02 renderers; write `references.json`; write source image and optional screenshots. Return a result object containing `content`, `content_type: "application/zip"`, `byte_size`, `checksum_sha256`, `manifest`, and package file list.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py tests/test_models.py</automated>
  </verify>
  <acceptance_criteria>
    - ZIP bytes open with `zipfile.ZipFile`.
    - `manifest.json` and `handoff-notes.md` exist and are non-empty.
    - Result checksum is 64 hex characters.
    - No hosted provider, browser, or network call is used.
  </acceptance_criteria>
  <done>Core can build enhanced handoff ZIP package bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Object storage to ZIP | Stored objects become downloadable package contents. |
| Package file names | File names must not leak host paths or unsafe user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Information Disclosure | ZIP contents | mitigate | Inspect all text members for forbidden tokens; use controlled file names. |
| T-13-08 | Tampering | required concept image | mitigate | Fail package creation when required concept bytes cannot be read. |
| T-13-09 | Denial of Service | package builder | accept | MVP packages are small; tests use in-memory bytes and no recursive storage enumeration. |
</threat_model>

<verification>
Run package inspection tests: `cd services/core && uv run pytest -q tests/test_jobs.py tests/test_models.py`.
</verification>

<success_criteria>
- V2-HANDOFF-02 package sections are present in ZIP.
- V2-HANDOFF-03 stable manifest JSON and Markdown notes are packaged.
- V2-HANDOFF-04 package bytes can become immutable artifacts in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/13-enhanced-concept-handoff-package-mvp/13-03-SUMMARY.md`.
</output>

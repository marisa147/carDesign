---
phase: 13-enhanced-concept-handoff-package-mvp
plan: "05"
type: execute
wave: 4
depends_on: ["13-04"]
files_modified:
  - "apps/web/src/lib/api/iteration.ts"
  - "apps/web/src/lib/api/iteration.test.ts"
  - "apps/web/src/components/workbench/export-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["V2-HANDOFF-01", "V2-HANDOFF-02", "V2-HANDOFF-03", "V2-HANDOFF-04"]
must_haves:
  truths:
    - "Workbench exports ZIP through the same version-scoped route as the API."
    - "Package preview makes included/warning/blocked state visible before submit."
    - "Existing PNG/JPG concept export behavior remains available."
  artifacts:
    - "Export panel ZIP mode and readiness preview."
    - "Workbench submit payload with `enhanced_concept_handoff_zip`."
    - "Web API/page tests."
  key_links:
    - "13-UI-SPEC -> Export Mode Control and Package Preview"
    - "13-PATTERNS -> Web Workbench"
    - "13-VALIDATION -> 13-05-01"
---

<objective>
Add workbench ZIP handoff package UX: export mode, package readiness preview, enhanced export submission, history display, and focused web tests.

Purpose: users need to create and inspect the enhanced handoff package from the existing workbench without a separate page.
Output: web client wiring and UI tests for `enhanced_concept_handoff_zip`.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-CONTEXT.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-UI-SPEC.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-VALIDATION.md
@apps/web/src/components/workbench/export-panel.tsx
@apps/web/src/components/workbench/workbench-app.tsx
@apps/web/src/lib/api/iteration.ts
@apps/web/src/app/page.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red web API and page tests for ZIP package export</name>
  <read_first>
    - `apps/web/src/lib/api/iteration.test.ts`
    - `apps/web/src/app/page.test.tsx`
    - `apps/web/src/components/workbench/export-panel.tsx`
  </read_first>
  <files>apps/web/src/lib/api/iteration.test.ts, apps/web/src/app/page.test.tsx</files>
  <action>Add tests that require the export API wrapper/workbench to submit `format: "enhanced_concept_handoff_zip"` for ZIP mode. In `page.test.tsx`, stub `NEXT_PUBLIC_V2_ENHANCED_HANDOFF_PACKAGE_ENABLED=true`, load a selected version/artifact with reference trace and optional 3D screenshot artifact, click `ZIP`, verify visible text `交接包预览`, `概念交接包，仅供评审`, and `生成交接包`, submit, and assert request body contains `concept_label: "client-review"`, `format: "enhanced_concept_handoff_zip"`, selected artifact id, and safe manifest fields without secrets.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/lib/api/iteration.test.ts src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because ZIP mode and package preview are absent.
    - Tests find `ZIP`, `交接包`, and `生成交接包` visible.
    - Tests assert the web request body does not contain `api_key`, `secret`, `image_base64`, or `C:\\`.
  </acceptance_criteria>
  <done>Web ZIP export behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend export panel UI and types</name>
  <read_first>
    - `apps/web/src/components/workbench/export-panel.tsx`
    - `apps/web/src/lib/config/public-env.ts`
    - `apps/web/src/components/ui/button.tsx`
    - `apps/web/src/components/ui/badge.tsx`
  </read_first>
  <files>apps/web/src/components/workbench/export-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx</files>
  <action>Extend `ConceptExportFormat` to include `"enhanced_concept_handoff_zip"`. Render mode buttons for `PNG`, `JPG`, and `ZIP`. For ZIP mode, show `交接包预览`, readiness rows for concept image, 3D screenshots, safe-zone/warnings, prompt/provider, references, and notes. Use concise status text and lucide icons. Keep `manifestDisclaimer` or a new handoff disclaimer visible as `概念交接包，仅供评审`. Disable ZIP mode when the public enhanced handoff flag is off or selected version/artifact is missing.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Existing PNG/JPG buttons still render and work.
    - ZIP button exposes selected/disabled state.
    - Package preview text does not overflow in test DOM snapshots or assertions.
    - No nested card-in-card structure is introduced beyond existing compact rows.
  </acceptance_criteria>
  <done>Export panel can display package mode and readiness.</done>
</task>

<task type="auto">
  <name>Task 3: Wire workbench package submission and history</name>
  <read_first>
    - `apps/web/src/components/workbench/workbench-app.tsx`
    - `apps/web/src/lib/api/iteration.ts`
    - `apps/web/src/lib/workbench/query-keys.ts`
  </read_first>
  <files>apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/lib/api/iteration.ts, apps/web/src/lib/api/iteration.test.ts, apps/web/src/app/page.test.tsx</files>
  <action>Update `submitSelectedExport` so ZIP mode sends `format: "enhanced_concept_handoff_zip"`, `artifact_id` for the selected concept image, `concept_label: "client-review"`, and a safe manifest request summary with selected version id, source artifact key, source `web-workbench`, reference trace fields, and optional review notes. Update history rows to show `ZIP` and package file/status/disclaimer details when returned manifest contains package file data. Keep cache update with `upsertExport` unchanged.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web exec vitest --run src/lib/api/iteration.test.ts src/app/page.test.tsx src/lib/api/generation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Submitted ZIP payload contains `"format":"enhanced_concept_handoff_zip"`.
    - Successful response adds ZIP export to `导出历史`.
    - Existing concept export success notice still appears for PNG/JPG or is updated without breaking tests.
    - Fetch body contains no secrets or local paths.
  </acceptance_criteria>
  <done>Workbench can create and display enhanced package exports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser public env to UI | Browser feature flag controls availability but API remains authoritative. |
| UI manifest request to API | Browser may submit unsafe or incomplete manifest fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Spoofing | UI readiness | mitigate | UI previews readiness but API re-validates ownership/rights. |
| T-13-14 | Information Disclosure | web request body | mitigate | Tests assert no secrets/base64/local paths are sent in manifest body. |
| T-13-15 | Denial of Service | dense export UI | mitigate | Keep controls compact, stable, and testable without layout-shifting dynamic content. |
</threat_model>

<verification>
Run focused web tests plus static checks: `corepack pnpm --filter @caragent/web exec vitest --run src/app/page.test.tsx src/lib/api/iteration.test.ts src/lib/api/generation.test.ts`, then lint/typecheck.
</verification>

<success_criteria>
- V2-HANDOFF-01 can be initiated from workbench.
- V2-HANDOFF-02 package contents are previewed.
- V2-HANDOFF-03 ZIP format is visible and submitted.
- V2-HANDOFF-04 export history shows durable package rows.
</success_criteria>

<output>
After completion, create `.planning/phases/13-enhanced-concept-handoff-package-mvp/13-05-SUMMARY.md`.
</output>

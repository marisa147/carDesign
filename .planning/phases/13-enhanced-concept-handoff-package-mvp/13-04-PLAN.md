---
phase: 13-enhanced-concept-handoff-package-mvp
plan: "04"
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/tests/test_jobs.py"
  - "services/api/tests/test_openapi_export.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
autonomous: true
requirements: ["V2-HANDOFF-01", "V2-HANDOFF-03", "V2-HANDOFF-04"]
must_haves:
  truths:
    - "Enhanced ZIP export is feature-gated server-side."
    - "Export record links to immutable ZIP artifact and selected version."
    - "Existing PNG/JPG export API remains backward compatible."
  artifacts:
    - "API route integration for `enhanced_concept_handoff_zip`."
    - "Generated OpenAPI/TypeScript contracts are current."
    - "API tests for feature gate, artifact creation, immutable export history, and ZIP contents."
  key_links:
    - "13-VALIDATION -> 13-04-01"
    - "13-PATTERNS -> API And Contracts"
    - "13-RESEARCH -> Recommended Architecture"
---

<objective>
Wire the enhanced handoff ZIP builder into the version-scoped export API, export ledger, immutable package artifact creation, and generated contracts.

Purpose: users and the web client need one durable API path for creating and listing enhanced handoff package exports.
Output: feature-gated API support with package artifact rows and contract regeneration.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-CONTEXT.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-RESEARCH.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-VALIDATION.md
@services/api/src/caragent_api/routes/jobs.py
@services/api/src/caragent_api/schemas.py
@services/core/src/caragent_core/services/jobs.py
@services/core/src/caragent_core/handoff.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red API tests for enhanced export route</name>
  <read_first>
    - `services/api/tests/test_jobs.py`
    - `services/api/src/caragent_api/routes/jobs.py`
    - `services/api/src/caragent_api/config.py`
  </read_first>
  <files>services/api/tests/test_jobs.py</files>
  <action>Add tests that call `POST /workspaces/{workspace_id}/versions/{version_id}/exports` with `format: "enhanced_concept_handoff_zip"` and `V2_ENHANCED_HANDOFF_PACKAGE_ENABLED=true`. Seed object storage with source concept image bytes and optional 3D screenshot bytes. Assert response status 201, `format` is `enhanced_concept_handoff_zip`, `status` is `succeeded`, `artifact_id` is a new ZIP artifact id, manifest has `schema_version: 1`, and stored ZIP contains `manifest.json` and `handoff-notes.md`. Add a disabled-flag test expecting 403.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because route still treats enhanced format as unsupported or ungated.
    - Disabled flag response contains `V2_ENHANCED_HANDOFF_PACKAGE_ENABLED is disabled`.
    - Existing PNG export API tests still pass.
  </acceptance_criteria>
  <done>API behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement feature-gated API package creation</name>
  <read_first>
    - `services/api/src/caragent_api/routes/jobs.py`
    - `services/core/src/caragent_core/handoff.py`
    - `services/core/src/caragent_core/services/jobs.py`
    - `services/core/src/caragent_core/storage.py`
  </read_first>
  <files>services/api/src/caragent_api/routes/jobs.py, services/core/src/caragent_core/services/jobs.py, services/api/src/caragent_api/schemas.py</files>
  <action>When `ExportCreateRequest.format` equals `enhanced_concept_handoff_zip`, require `settings.v2_enhanced_handoff_package_enabled`. Resolve selected version, source artifact, optional same-version `preview_3d_screenshot` artifacts, job model runs, and feedback/export notes as needed. Call the core ZIP builder, store ZIP bytes with `ObjectStorage.put_object`, create an `ArtifactKind.EXPORT` artifact with `content_type: "application/zip"`, checksum, byte size, metadata containing safe handoff manifest summary, and selected version/job id. Record an export row with `artifact_id` set to the ZIP artifact, `format` set to `enhanced_concept_handoff_zip`, `status: "succeeded"`, and `completed_at` set. Keep PNG/JPG path using existing `record_export` behavior.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Enhanced package export creates a new artifact row on every request and does not overwrite existing exports.
    - ZIP artifact object key ends with `.zip`.
    - Export response manifest includes package file list and package artifact object key.
    - Disabled feature flag returns 403 before storage write.
  </acceptance_criteria>
  <done>API creates durable enhanced package exports.</done>
</task>

<task type="auto">
  <name>Task 3: Update OpenAPI and generated TypeScript contracts</name>
  <read_first>
    - `services/api/src/caragent_api/schemas.py`
    - `services/api/tests/test_openapi_export.py`
    - `packages/contracts/openapi/openapi.json`
    - `packages/contracts/src/generated/client.ts`
  </read_first>
  <files>services/api/src/caragent_api/schemas.py, services/api/tests/test_openapi_export.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Expose any new typed response/request fields needed by enhanced package manifests without breaking existing `ExportCreateRequest`/`ExportResponse`. Regenerate contracts through the repo flow and add OpenAPI tests that the export route and schemas still exist. Do not hand-maintain generated TypeScript beyond the standard contract generation command.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_openapi_export.py tests/test_jobs.py</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/src/generated/client.ts` still exports `ExportCreateRequest` and `ExportResponse`.
    - Generated contracts include the version-scoped export route.
    - `corepack pnpm contracts:check` exits 0 in a host/elevated run if sandbox child-process permissions block it.
  </acceptance_criteria>
  <done>API contracts are current for web integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to API export request | Users can request package creation for arbitrary ids. |
| API to object storage | API writes downloadable ZIP artifact bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Elevation of Privilege | package export route | mitigate | Validate workspace/version/artifact ownership before reading or packaging bytes. |
| T-13-11 | Tampering | export ledger | mitigate | Always create a new export artifact/record; never overwrite prior exports. |
| T-13-12 | Information Disclosure | package metadata | mitigate | Store only safe manifest summary and no binary/base64/secrets in export manifest. |
</threat_model>

<verification>
Run API and contract checks: `cd services/api && uv run pytest -q tests/test_jobs.py tests/test_openapi_export.py`, then `corepack pnpm contracts:check`.
</verification>

<success_criteria>
- V2-HANDOFF-01 can create an enhanced handoff package through API.
- V2-HANDOFF-03 returns ZIP manifest evidence.
- V2-HANDOFF-04 export ledger is durable, immutable, and version-linked.
</success_criteria>

<output>
After completion, create `.planning/phases/13-enhanced-concept-handoff-package-mvp/13-04-SUMMARY.md`.
</output>

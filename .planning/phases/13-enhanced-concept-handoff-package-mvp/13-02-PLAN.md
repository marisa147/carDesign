---
phase: 13-enhanced-concept-handoff-package-mvp
plan: "02"
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - "services/core/src/caragent_core/handoff.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/tests/test_jobs.py"
  - "services/core/tests/test_generation_jobs.py"
autonomous: true
requirements: ["V2-HANDOFF-02", "V2-HANDOFF-05"]
must_haves:
  truths:
    - "Package reports come from durable version/artifact/model-run metadata, not browser-only state."
    - "Concept-only disclaimers and warnings are included in both manifest data and readable notes."
    - "Safe-zone evidence can be deterministic metadata/Markdown in MVP; production overlay art remains optional."
  artifacts:
    - "Warning report helper."
    - "Safe-zone/template report helper."
    - "Prompt/provider/reference/review note summary helpers."
  key_links:
    - "13-CONTEXT -> Manifest And Notes"
    - "13-RESEARCH -> Rights And Safety Strategy"
    - "13-VALIDATION -> 13-02-01"
---

<objective>
Build deterministic report helpers for safe zones, warnings, prompt/provider trace, reference manifest, review notes, and concept-only handoff notes.

Purpose: the ZIP builder and API route need one sanitized source for manifest sections and human-readable Markdown files.
Output: tested report rendering helpers inside the core handoff package.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-CONTEXT.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-RESEARCH.md
@.planning/phases/13-enhanced-concept-handoff-package-mvp/13-VALIDATION.md
@services/core/src/caragent_core/handoff.py
@services/core/src/caragent_core/references.py
@services/core/src/caragent_core/preview3d.py
@services/core/src/caragent_core/generation/templates.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add red tests for deterministic reports</name>
  <read_first>
    - `services/core/tests/test_jobs.py`
    - `services/core/tests/test_generation_jobs.py`
    - `services/core/src/caragent_core/handoff.py`
  </read_first>
  <files>services/core/tests/test_jobs.py, services/core/tests/test_generation_jobs.py</files>
  <action>Add failing tests for helper functions such as `build_handoff_warning_report`, `build_handoff_reference_manifest`, `render_handoff_notes_markdown`, `render_handoff_warnings_markdown`, and `render_handoff_prompt_trace_markdown`. Tests must use a version with `preview_spec`, `preview_3d`, `rights_snapshot`, `reference_usage`, model-run prompt/provider fields, and feedback metadata. Assert the output contains `Concept handoff package for review only`, safe-zone ids like `door-main`, warning ids like `non_production_preview`, reference asset ids, and provider/model names, while excluding `api_key`, `secret`, `image_base64`, and `C:\\`.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py tests/test_generation_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because report helpers are missing.
    - Tests assert Markdown starts with deterministic headings: `# Concept Handoff Notes`, `# Warning Report`, and `# Prompt Trace`.
    - Tests assert safe-zone/template metadata is present without needing production print dimensions.
  </acceptance_criteria>
  <done>Report helper behavior is specified by failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement warning and safe-zone report builders</name>
  <read_first>
    - `services/core/src/caragent_core/handoff.py`
    - `services/core/src/caragent_core/preview3d.py`
    - `services/core/src/caragent_core/generation/templates.py`
  </read_first>
  <files>services/core/src/caragent_core/handoff.py</files>
  <action>Implement typed helper payloads for safe-zone/template and warning reports. Extract `preview_spec.template`, `preview_spec.safe_zones`, `preview_spec.warnings`, `preview_3d.warnings`, and optional `preview_3d_screenshot.warning_ids`. Normalize missing optional sections to warning items, not crashes. Include exact warning ids `non_production_preview` and `uv_not_verified` when present in Preview3D metadata.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py tests/test_generation_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Warning report includes `schema_version: 1`.
    - Safe-zone report includes template id/view and a list of safe zone records.
    - Missing screenshot artifacts produce optional warning text, not a blocked state.
    - No helper emits binary bytes or base64 fields.
  </acceptance_criteria>
  <done>Warnings and safe-zone evidence are deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Implement prompt/provider/reference/note renderers</name>
  <read_first>
    - `services/core/src/caragent_core/handoff.py`
    - `services/core/src/caragent_core/references.py`
    - `services/core/src/caragent_core/models.py`
  </read_first>
  <files>services/core/src/caragent_core/handoff.py, services/core/tests/test_jobs.py</files>
  <action>Implement Markdown/JSON renderers that accept typed manifest sections and produce `handoff-notes.md`, `warnings.md`, `prompt-trace.md`, and `references.json` contents. Prompt trace should summarize model-run id, provider, model, status, estimated/actual cost, prompt text summary, and input/output artifact ids. Reference manifest should include included/omitted ids, roles, rights snapshot, unsupported roles, and warning count. Notes must include concept-only disclaimer and review notes from feedback/export manifest when present.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Rendered notes include `# Concept Handoff Notes`.
    - Rendered warnings include `# Warning Report`.
    - Rendered prompt trace includes provider/model/status but not raw secrets.
    - `references.json` is parseable JSON and includes `schema_version`.
  </acceptance_criteria>
  <done>Readable package reports are ready for ZIP inclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable metadata to human-readable notes | Metadata may contain user text, warnings, or provider details. |
| Reference trace to package | Rights/source metadata controls whether handoff is review-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information Disclosure | report renderers | mitigate | Sanitize/omit forbidden fields and assert no secrets/base64/local paths in rendered text. |
| T-13-05 | Spoofing | concept-only disclaimer | mitigate | Centralize disclaimer from `HANDOFF_CONCEPT_ONLY_DISCLAIMER` in every rendered report. |
| T-13-06 | Repudiation | warning evidence | mitigate | Preserve warning ids/messages and source metadata in manifest and Markdown. |
</threat_model>

<verification>
Run focused core report tests: `cd services/core && uv run pytest -q tests/test_jobs.py tests/test_generation_jobs.py`.
</verification>

<success_criteria>
- V2-HANDOFF-02 has deterministic reports for required package sections.
- V2-HANDOFF-05 has warning/blocking evidence structures ready for guardrails.
</success_criteria>

<output>
After completion, create `.planning/phases/13-enhanced-concept-handoff-package-mvp/13-02-SUMMARY.md`.
</output>

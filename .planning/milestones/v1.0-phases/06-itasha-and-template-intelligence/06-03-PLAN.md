---
phase: 06-itasha-and-template-intelligence
plan: "03"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/generation.py"
  - "services/api/tests/test_generation.py"
  - "services/api/tests/test_openapi_export.py"
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
autonomous: true
requirements: ["QUAL-01", "QUAL-02", "QUAL-03", "QUAL-05"]
must_haves:
  truths:
    - "API create/update brief payloads accept Phase 6 itasha and overlay fields."
    - "OpenAPI and generated TypeScript contracts expose the new structured brief fields."
    - "Existing generation/iteration routes keep their current shape while carrying PreviewSpec metadata through existing responses."
  artifacts:
    - "services/api/src/caragent_api/schemas.py"
    - "services/api/src/caragent_api/routes/generation.py"
    - "packages/contracts/openapi/openapi.json"
    - "packages/contracts/src/generated/client.ts"
  key_links:
    - "GenerationBriefCreateRequest/UpdateRequest -> create_generation_brief/update_generation_brief_route"
    - "FastAPI OpenAPI export -> @caragent/contracts generated client"
---

<objective>
Expose Phase 6 brief and preview metadata through existing API contracts and regenerate the TypeScript contract package.

Purpose: The frontend must consume typed Phase 6 fields without hand-written API shapes or route changes.
Output: API schema/route updates, OpenAPI JSON, generated TypeScript client, and focused API/contract tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-itasha-and-template-intelligence/06-CONTEXT.md
@.planning/phases/06-itasha-and-template-intelligence/06-RESEARCH.md
@services/api/src/caragent_api/schemas.py
@services/api/src/caragent_api/routes/generation.py
@services/api/tests/test_generation.py
@services/api/tests/test_openapi_export.py
@packages/contracts/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify API Phase 6 create/update contract</name>
  <files>services/api/tests/test_generation.py, services/api/tests/test_openapi_export.py</files>
  <behavior>
    - Create generation brief accepts Phase 6 fields and persists them into `GenerationBriefResponse.payload` per D-01/D-02.
    - Patch generation brief accepts the same editable Phase 6 fields without requiring unrelated fields per D-02/D-03.
    - Invalid empty update payload remains rejected.
    - OpenAPI schema includes Phase 6 request/response fields so generated contracts can type them per D-15.
  </behavior>
  <action>Add failing tests in `test_generation.py` for create and patch payloads containing `character_focus`, `supporting_graphics`, `racing_cues`, `typography_intent`, `color_harmony`, and `overlay_logo_asset_ids`. Add an OpenAPI export test assertion that the request/response schemas include these properties and still include existing Phase 3-5 properties.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py tests/test_openapi_export.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because API schemas do not yet expose Phase 6 fields.
    - Tests do not introduce new generation job routes.
  </acceptance_criteria>
  <done>API contract behavior is specified before route/schema changes.</done>
</task>

<task type="auto">
  <name>Task 2: Update API schemas/routes and regenerate contracts</name>
  <files>services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/routes/generation.py, packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Add Phase 6 fields to `GenerationBriefCreateRequest` and `GenerationBriefUpdateRequest`, pass them through `create_generation_brief()` and update merge logic in `update_generation_brief_route()` per D-01/D-03. Regenerate OpenAPI with `uv run python -m caragent_api.scripts.export_openapi --out ../../packages/contracts/openapi/openapi.json`, then run `corepack pnpm --filter @caragent/contracts generate` and `corepack pnpm contracts:check`. Do not hand-edit generated TypeScript contracts.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py tests/test_openapi_export.py</automated>
    <automated>cd services/api && uv run ruff check .</automated>
    <automated>cd services/api && uv run mypy src</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - API create/update routes persist Phase 6 fields.
    - Generated TypeScript includes Phase 6 fields on brief payload/request types.
    - Contract drift check passes after generation.
    - Core generation/job API route shape remains unchanged except typed payload fields.
  </acceptance_criteria>
  <done>Frontend can import generated Phase 6 contract types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to FastAPI | User-controlled Phase 6 brief fields enter API request models |
| FastAPI to contracts package | OpenAPI schemas become generated frontend types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | `GenerationBriefCreateRequest` | mitigate | Use Pydantic typed fields and core schema validation before persistence. |
| T-06-08 | Repudiation | OpenAPI/contracts | mitigate | Regenerate and check contracts so frontend cannot silently use stale shapes. |
| T-06-09 | Denial of Service | Brief update payload | mitigate | Preserve existing empty-update rejection and list normalization. |
</threat_model>

<verification>
Run focused API tests, API ruff/mypy, regenerate contracts, and run contracts drift check.
</verification>

<success_criteria>
- FastAPI accepts and returns typed Phase 6 brief fields.
- Generated contracts expose those fields.
- No new core job API route shape is required for PreviewSpec.
</success_criteria>

<output>
After completion, create `.planning/phases/06-itasha-and-template-intelligence/06-03-SUMMARY.md`.
</output>

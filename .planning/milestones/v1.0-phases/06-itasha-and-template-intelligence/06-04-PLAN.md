---
phase: 06-itasha-and-template-intelligence
plan: "04"
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/components/workbench/parameter-panel.tsx"
  - "apps/web/src/components/workbench/preview-panel.tsx"
  - "apps/web/src/components/workbench/export-panel.tsx"
  - "apps/web/src/lib/workbench/store.ts"
  - "apps/web/src/lib/workbench/store.test.ts"
autonomous: true
requirements: ["QUAL-01", "QUAL-02", "QUAL-03", "QUAL-04", "QUAL-05"]
must_haves:
  truths:
    - "User can edit Phase 6 itasha controls from the existing parameter panel."
    - "User can toggle deterministic text/logo layer visibility and safe-zone visibility in the existing preview panel."
    - "Workbench displays warning and PreviewSpec summaries without horizontal overflow or production-ready claims."
  artifacts:
    - "apps/web/src/components/workbench/parameter-panel.tsx"
    - "apps/web/src/components/workbench/preview-panel.tsx"
    - "apps/web/src/components/workbench/export-panel.tsx"
    - "apps/web/src/lib/workbench/store.ts"
  key_links:
    - "@caragent/contracts GenerationBriefUpdateRequest -> ParameterPanel onSave"
    - "DesignVersion.parameters.preview_spec -> PreviewPanel overlay/spec summary"
    - "ExportResponse.manifest.parameters.preview_spec -> ExportPanel manifest preview"
---

<objective>
Add Phase 6 itasha controls, overlay/safe-zone toggles, warnings, and PreviewSpec visibility to the existing workbench UI.

Purpose: Users should operate Phase 6 from the same chat/preview/parameter/export surface verified in Phases 4-5.
Output: Updated web components, local UI state, and focused web tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-itasha-and-template-intelligence/06-CONTEXT.md
@.planning/phases/06-itasha-and-template-intelligence/06-UI-SPEC.md
@.planning/phases/06-itasha-and-template-intelligence/06-RESEARCH.md
@apps/web/src/components/workbench/parameter-panel.tsx
@apps/web/src/components/workbench/preview-panel.tsx
@apps/web/src/components/workbench/export-panel.tsx
@apps/web/src/lib/workbench/store.ts
@apps/web/src/app/page.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify workbench Phase 6 interactions</name>
  <files>apps/web/src/app/page.test.tsx, apps/web/src/lib/workbench/store.test.ts</files>
  <behavior>
    - Parameter panel renders `痛车设计控制`, `角色焦点`, `辅助图形`, `赛车/JDM 元素`, `字体意图`, and `配色协调` per D-01/D-16.
    - Saving Phase 6 controls patches the existing brief route and does not submit generation per D-02/D-03.
    - Preview panel renders `文字/Logo 图层`, `安全区`, `模板参考区`, and PreviewSpec summary for selected versions per D-04/D-11/D-15.
    - Missing-rights logo assets are not presented as enabled overlay candidates per D-06/D-08.
    - Store state toggles overlay and safe-zone visibility locally without mutating selected version data per D-16.
  </behavior>
  <action>Add failing Vitest/Testing Library coverage to `page.test.tsx` for resumed workbench fixtures containing Phase 6 brief fields, warnings, preview spec, and export manifest parameters. Add `store.test.ts` assertions for overlay/safe-zone visibility toggles and reset behavior.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- --run apps/web/src/app/page.test.tsx apps/web/src/lib/workbench/store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before UI/store implementation because Phase 6 controls and toggles are absent.
    - Tests assert no generation job is submitted by parameter saves.
    - Tests assert concept-preview wording remains present.
  </acceptance_criteria>
  <done>Workbench Phase 6 behavior is specified before component changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement workbench controls, overlays, and spec display</name>
  <files>apps/web/src/components/workbench/parameter-panel.tsx, apps/web/src/components/workbench/preview-panel.tsx, apps/web/src/components/workbench/export-panel.tsx, apps/web/src/lib/workbench/store.ts</files>
  <action>Extend `ParameterPanel` draft/create/update logic to edit Phase 6 fields from generated contract types and display `质量提示` warnings per D-01/D-03/D-07/D-09/D-16. Extend `useWorkbenchStore` with `showOverlayLayers`, `showSafeZones`, and toggle/reset actions. In `PreviewPanel`, read `preview_spec` from selected version parameters, render bounded overlay and safe-zone layers inside the existing preview container, add accessible toggles for `文字/Logo 图层` and `安全区`, and render `模板参考区` legend per D-04/D-11/D-16/D-18. Keep all additions inside the existing quiet operational panel structure from Phases 4-5 per D-17. In `ExportPanel`, render a compact PreviewSpec/manifest summary when present while preserving `概念预览，不是生产印刷文件。` per D-05/D-15.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- --run apps/web/src/app/page.test.tsx apps/web/src/lib/workbench/store.test.ts</automated>
    <automated>corepack pnpm --filter @caragent/web lint</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Parameter saves include only changed Phase 6 fields plus existing changed fields.
    - Overlay and safe-zone toggles expose selected state with `aria-pressed`.
    - Preview overlays are contained inside the preview region and do not resize the surrounding layout.
    - The workbench remains the existing operational layout, not a new landing page or separate editor.
    - Export panel never claims print-ready or production-ready output.
  </acceptance_criteria>
  <done>Existing workbench exposes Phase 6 controls and renderer-neutral PreviewSpec inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text to React UI | User-provided Phase 6 text renders in parameter, preview, and manifest surfaces |
| Local UI state to durable state | Preview toggles are local; brief saves are durable API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | `ParameterPanel` save payload | mitigate | Build update payload from generated contract keys and only send changed fields. |
| T-06-11 | Information Disclosure | `ExportPanel` manifest preview | mitigate | Display only manifest/spec fields returned by API; tests reject drive paths/secrets. |
| T-06-12 | Denial of Service | Preview overlays/mobile layout | mitigate | Bound overlay layer inside preview container and verify mobile no-overflow behavior during UAT. |
| T-06-13 | Spoofing | Text/logo overlay copy | mitigate | Copy states deterministic overlay layer; does not claim AI raster text is exact. |
</threat_model>

<verification>
Run focused web tests, lint, and typecheck. Browser UAT is completed in Plan 06-05 after all Phase 6 code is in place.
</verification>

<success_criteria>
- Phase 6 fields are editable in the parameter panel.
- Overlay and safe-zone toggles render accessible selected states.
- PreviewSpec and warning summaries are visible without production-ready claims.
</success_criteria>

<output>
After completion, create `.planning/phases/06-itasha-and-template-intelligence/06-04-SUMMARY.md`.
</output>

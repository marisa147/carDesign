---
phase: 04-workbench-ui-integration
plan: "06"
type: execute
wave: 6
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - "apps/web/src/components/workbench/progress-panel.tsx"
  - "apps/web/src/components/workbench/preview-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/lib/workbench/store.ts"
  - "apps/web/src/lib/workbench/store.test.ts"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["UI-04", "UI-05", "UI-06", "UI-07"]
must_haves:
  truths:
    - "User can see queued/running/succeeded/failed job states and recent events."
    - "User can inspect selected 2D design versions with zoom, pan/reset, thumbnails, and view switching."
    - "Future 3D/export/marketplace controls remain disabled/deferred."
---

<objective>
Implement generation progress, 2D preview controls, and version history switching in the workbench.
</objective>

<context>
@.planning/phases/04-workbench-ui-integration/04-CONTEXT.md
@.planning/phases/04-workbench-ui-integration/04-UI-SPEC.md
@apps/web/src/lib/api/generation.ts
@apps/web/src/lib/api/jobs.ts
@apps/web/src/lib/workbench/store.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06A | Misrepresentation | Preview | mitigate | Label generated images as 2D concept previews and keep production/3D features deferred. |
| T-04-06B | Reliability | Job state | mitigate | Drive progress from durable job/events state and distinguish failed/retryable states. |
| T-04-06C | Accessibility | Canvas controls | mitigate | Use labelled buttons and stable dimensions for zoom/pan/reset/view switching. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Specify progress states</name>
  <files>apps/web/src/app/page.test.tsx</files>
  <action>Write failing tests for queued, running, succeeded, and failed job fixtures. Assert status chips, latest event copy, event count/list, refresh action, and retry button only for failed generation jobs.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before `ProgressPanel` exists.
    - Tests cover retry route request body and idempotency key use.
  </acceptance_criteria>
  <done>Progress behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement progress panel</name>
  <files>apps/web/src/components/workbench/progress-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx</files>
  <action>Add job status, event timeline, refresh, and retry UI. Use durable generation state loading and retry wrapper. Keep retry disabled when the current job is not failed.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Status labels are distinct and localized.
    - Latest failure message is visible when present.
    - Retry keeps original brief context.
  </acceptance_criteria>
  <done>User can follow generation progress and retry failed jobs.</done>
</task>

<task type="tdd">
  <name>Task 3: Specify preview and history behavior</name>
  <files>apps/web/src/app/page.test.tsx, apps/web/src/lib/workbench/store.test.ts</files>
  <action>Write failing tests that render artifact/version fixtures, select a history thumbnail, change view, zoom in/out/reset preview, and assert chat/parameter state remains visible. Test empty preview copy before any artifact exists.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before `PreviewPanel` exists.
    - Tests assert selected version is local UI state, not a server mutation.
  </acceptance_criteria>
  <done>Preview/history behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 4: Implement 2D preview and history</name>
  <files>apps/web/src/components/workbench/preview-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/lib/workbench/store.ts</files>
  <action>Add stable aspect-ratio preview area, artifact metadata/image placeholder rendering, zoom/pan/reset controls, supported view switching, and horizontal version thumbnail strip. Use artifact/version rows and local store selection.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx store.test.ts</automated>
    <automated>corepack pnpm --filter @caragent/web lint</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Empty, loading, succeeded, and failed states have stable layout.
    - Selecting a version updates preview without losing context.
    - 3D controls remain disabled/deferred.
  </acceptance_criteria>
  <done>User can inspect 2D concepts and switch versions.</done>
</task>

</tasks>

<verification>
Run focused page/store tests, lint, and typecheck.
</verification>

<success_criteria>
- UI-04, UI-05, and UI-06 are covered in the workbench.
- Future controls satisfy UI-07 without implying active 3D/export.
- Preview controls are accessible and stable.
</success_criteria>

<output>
After completion, create `.planning/phases/04-workbench-ui-integration/04-06-SUMMARY.md`.
</output>

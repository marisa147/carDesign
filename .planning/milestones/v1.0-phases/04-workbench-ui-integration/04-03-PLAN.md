---
phase: 04-workbench-ui-integration
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - "apps/web/src/components/workbench/chat-panel.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["UI-01", "UI-02", "UI-04"]
must_haves:
  truths:
    - "User can submit GPT-style chat text through the workbench."
    - "Chat submission creates durable user message state and a structured generation brief."
    - "Assistant/system feedback is deterministic and does not imply unsupported streaming LLM behavior."
---

<objective>
Implement the GPT-style chat panel and connect chat submission to durable workspace messages and structured brief creation.
</objective>

<context>
@.planning/phases/04-workbench-ui-integration/04-CONTEXT.md
@apps/web/src/lib/api/workspaces.ts
@apps/web/src/lib/api/generation.ts
@apps/web/src/components/workbench/workbench-app.tsx
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03A | Information Disclosure | Chat submit | mitigate | Send only user prompt/brief fields to API; no provider keys or hidden local config. |
| T-04-03B | Spoofing | Assistant feedback | mitigate | Label deterministic system feedback honestly instead of pretending a live AI assistant streamed a reply. |
| T-04-03C | Tampering | Duplicate jobs | mitigate | Do not submit generation during chat submit; job creation remains an explicit user action. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Specify chat-to-brief behavior</name>
  <files>apps/web/src/app/page.test.tsx</files>
  <action>Write failing tests that type a prompt into `设计需求`, click `发送需求`, and assert the page calls create workspace if needed, creates a durable user message, creates a generation brief with `source_message_id`, and renders the message plus a deterministic system feedback item.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before `ChatPanel` exists.
    - Tests assert no generation job submit route is called by chat submit alone.
  </acceptance_criteria>
  <done>Chat behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement chat panel</name>
  <files>apps/web/src/components/workbench/chat-panel.tsx, apps/web/src/components/workbench/workbench-app.tsx</files>
  <action>Add a chat panel with durable message list, composer, send button, pending/error states, and deterministic system feedback. Use TanStack Query mutations or an equivalent isolated query layer to create workspace/message/brief and invalidate workspace messages/briefs.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Empty prompt cannot submit.
    - User message appears in the chat panel after success.
    - Structured brief status becomes visible to downstream panels.
  </acceptance_criteria>
  <done>Chat panel drives durable message and brief state.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve refresh/resume behavior</name>
  <files>apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Store only the workspace id and latest brief id as safe resume identifiers. On remount, fetch workspace, messages, briefs, jobs, assets, artifacts, and versions from API-backed queries instead of rendering stale browser-only data.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Remount test proves messages and latest brief are refetched.
    - Browser storage contains ids only, not full messages or provider parameters.
  </acceptance_criteria>
  <done>Chat and brief state survives refresh through API-backed state.</done>
</task>

</tasks>

<verification>
Run focused page tests and typecheck.
</verification>

<success_criteria>
- GPT-style chat is usable.
- Durable message and structured brief state are created from chat.
- Chat does not overclaim unsupported AI streaming.
</success_criteria>

<output>
After completion, create `.planning/phases/04-workbench-ui-integration/04-03-SUMMARY.md`.
</output>

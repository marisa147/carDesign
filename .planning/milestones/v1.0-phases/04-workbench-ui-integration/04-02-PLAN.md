---
phase: 04-workbench-ui-integration
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - "apps/web/src/app/page.tsx"
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/components/workbench/workbench-app.tsx"
  - "apps/web/src/components/workbench/workbench-shell.tsx"
  - "apps/web/src/components/workbench/future-gates.tsx"
  - "apps/web/src/app/globals.css"
autonomous: true
requirements: ["UI-01", "UI-02", "UI-03", "UI-04", "UI-05", "UI-06", "UI-07"]
must_haves:
  truths:
    - "The first screen is a real workbench layout, not a proof landing page."
    - "Chat, parameters, assets, progress, preview, history, and future gates are visible as workbench regions."
    - "True 3D, print-ready export, marketplace, and production handoff controls are disabled or deferred."
---

<objective>
Replace the proof-first page shell with the Phase 4 workbench layout and explicit future-feature gates.
</objective>

<context>
@.planning/phases/04-workbench-ui-integration/04-CONTEXT.md
@.planning/phases/04-workbench-ui-integration/04-UI-SPEC.md
@apps/web/src/app/page.tsx
@apps/web/src/app/page.test.tsx
@UI.png
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02A | Misrepresentation | Future features | mitigate | Render true 3D/export/marketplace as disabled or deferred, never as active empty controls. |
| T-04-02B | Accessibility | Dense workbench | mitigate | Labels, aria names, stable button dimensions, and responsive non-overlap are tested. |
| T-04-02C | Reliability | Layout regression | mitigate | Page tests assert all major workbench regions and disabled gates. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Specify workbench layout</name>
  <files>apps/web/src/app/page.test.tsx</files>
  <action>Replace proof-shell-only expectations with failing tests for the Phase 4 workbench regions: `对话`, `2D 预览`, `参数`, `素材`, `进度`, `历史方案`, and explicit deferred controls for `3D 预览后续开放`, `生产导出后续开放`, and `市场功能后续开放`.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail against the current proof page.
    - Tests continue to assert future controls are disabled or aria-disabled.
  </acceptance_criteria>
  <done>Workbench shell behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Create workbench composition components</name>
  <files>apps/web/src/app/page.tsx, apps/web/src/components/workbench/workbench-app.tsx, apps/web/src/components/workbench/workbench-shell.tsx, apps/web/src/components/workbench/future-gates.tsx</files>
  <action>Make `page.tsx` a thin entry that renders the query provider and `WorkbenchApp`. Implement the shell with left chat, central preview/progress, and right inspector layout. Add placeholder panels for later plans, but keep them functional enough to satisfy region/gate tests.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The shell uses stable layout dimensions and responsive stacking.
    - No nested page-section cards are introduced.
    - Local mode/API status remains visible in compact form.
  </acceptance_criteria>
  <done>Phase 4 shell replaces proof-first page.</done>
</task>

<task type="auto">
  <name>Task 3: Tune global workbench colors and spacing</name>
  <files>apps/web/src/app/globals.css, apps/web/src/app/page.test.tsx</files>
  <action>Adjust CSS variables only if needed to match `04-UI-SPEC.md`: neutral dominant surface, restrained accent, stable focus ring, no viewport-scaled font sizes, and no one-note purple page. Keep existing component APIs compatible.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web lint</automated>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Buttons and text remain readable at narrow viewport widths.
    - Accent color is reserved for selected/primary elements.
  </acceptance_criteria>
  <done>Base visual contract is applied.</done>
</task>

</tasks>

<verification>
Run focused page tests, web lint, and typecheck.
</verification>

<success_criteria>
- Workbench regions are visible as the first screen.
- Deferred future capabilities are explicit and disabled.
- The page no longer presents Phase 1-3 proofs as the primary product experience.
</success_criteria>

<output>
After completion, create `.planning/phases/04-workbench-ui-integration/04-02-SUMMARY.md`.
</output>

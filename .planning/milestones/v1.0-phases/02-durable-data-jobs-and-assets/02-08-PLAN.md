---
phase: 02-durable-data-jobs-and-assets
plan: "08"
type: execute
wave: 5
depends_on: ["02-06", "02-07"]
files_modified:
  - "apps/web/src/app/page.tsx"
  - "apps/web/src/app/page.test.tsx"
  - "apps/web/src/lib/api/workspaces.ts"
  - "apps/web/src/lib/api/jobs.ts"
autonomous: true
requirements: ["DATA-01", "DATA-05", "DATA-06"]
must_haves:
  truths:
    - "The web shell can create/resume a workspace ID and refetch canonical state after refresh."
    - "The web shell can show durable job status/events from API-backed state."
    - "The UI remains a minimal proof and does not become the full Phase 4 workbench."
---

<objective>
Add a minimal frontend persistence and job-status proof using generated Phase 2 contracts.
</objective>

<context>
@.planning/phases/02-durable-data-jobs-and-assets/02-CONTEXT.md
@apps/web/src/app/page.tsx
@apps/web/src/app/page.test.tsx
@apps/web/src/lib/api/health.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08A | Information Disclosure | Browser state | mitigate | Store only workspace ID/client idempotency key locally; canonical data comes from API. |
| T-02-08B | Tampering | Job creation | mitigate | Generate/send idempotency keys and show reused job state from API response. |
| T-02-08C | Reliability | Refresh behavior | mitigate | Tests simulate remount/refresh and verify data is fetched again from API. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add web tests for durable proof behavior</name>
  <files>apps/web/src/app/page.test.tsx</files>
  <action>Write failing tests that mock Phase 2 API calls: create/resume a workspace, display persisted message/job counts, create a simulated job with an idempotency key, and remount the page with the same stored workspace ID to prove state is refetched from API rather than only rendered from local component state.</action>
  <verify>
    <automated>pnpm --filter @caragent/web test</automated>
  </verify>
  <acceptance_criteria>
    - Tests do not require a live backend.
    - Tests assert API fetches happen after remount/refresh.
    - Existing Phase 1 health shell tests still pass.
  </acceptance_criteria>
  <done>Frontend persistence proof behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal workspace/job status panel</name>
  <files>apps/web/src/app/page.tsx</files>
  <action>Add a compact Phase 2 status panel to the existing shell. It may include icon buttons and short labels for creating/resuming a workspace and starting a local simulated job. Keep future chat/upload/preview/generation/export regions disabled. Do not add full text prompt input, asset manager, 2D canvas, 3D preview, or export UI.</action>
  <verify>
    <automated>pnpm --filter @caragent/web test</automated>
    <automated>pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - UI text fits in existing responsive shell.
    - Canonical state is fetched through wrappers using generated contracts.
    - Disabled future regions remain disabled.
  </acceptance_criteria>
  <done>Minimal durable-state proof UI is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add browser-refresh UAT notes</name>
  <files>.planning/phases/02-durable-data-jobs-and-assets/02-HUMAN-UAT.md</files>
  <action>Create a human UAT checklist for starting services, creating a workspace, creating a simulated job, refreshing the browser, and confirming workspace/job state remains visible from API-backed state.</action>
  <verify>
    <automated>Test file exists and includes create workspace, create job, refresh browser, and confirm persisted state steps.</automated>
  </verify>
  <acceptance_criteria>
    - UAT does not ask for real image generation.
    - UAT marks full workbench behavior as out of Phase 2 scope.
  </acceptance_criteria>
  <done>Human verification path is documented.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter @caragent/web test`, `pnpm --filter @caragent/web typecheck`, and visually/browser-test the local shell during Phase 2 verification.
</verification>

<success_criteria>
- Web shell proves workspace/job status can be refetched after refresh.
- Phase 4 UI scope remains deferred.
- Generated contract wrappers are used.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-08-SUMMARY.md`.
</output>

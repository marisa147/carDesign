---
phase: 02-durable-data-jobs-and-assets
plan: "07"
type: execute
wave: 4
depends_on: ["02-05"]
files_modified:
  - "services/worker/src/caragent_worker/app.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/tests/test_job_tasks.py"
  - "services/worker/tests/test_worker_app.py"
autonomous: true
requirements: ["DATA-02", "DATA-05", "DATA-06", "DATA-07"]
must_haves:
  truths:
    - "Worker can update durable job/event state through shared core services."
    - "Worker still does not import FastAPI routers or provider SDKs."
    - "No-provider simulation is explicitly local and does not claim real generation."
---

<objective>
Add worker durable job update tasks without real provider calls.
</objective>

<context>
@.planning/phases/02-durable-data-jobs-and-assets/02-CONTEXT.md
@services/worker/src/caragent_worker/app.py
@services/worker/tests/test_worker_app.py
@services/core/src/caragent_core/services/jobs.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07A | Spoofing | Worker job updates | mitigate | Worker updates only known durable job IDs and records events showing local simulation source. |
| T-02-07B | Tampering | Job status | mitigate | Status transitions go through core services and append events. |
| T-02-07C | Information Disclosure | Provider configuration | mitigate | Do not load or log provider secrets; no provider SDK imports. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add worker job task tests first</name>
  <files>services/worker/tests/test_job_tasks.py</files>
  <action>Write failing tests for a local simulated job task that moves a queued job through running to succeeded, appends events, and records a local simulation model run/cost record through core services. Use isolated test database/session wiring; do not require a live Celery worker.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_job_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests call task `.run()` or a pure helper for deterministic behavior.
    - Tests prove job status is read from durable storage after the task completes.
  </acceptance_criteria>
  <done>Worker durable update behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement no-provider simulated job task</name>
  <files>services/worker/src/caragent_worker/tasks/jobs.py, services/worker/src/caragent_worker/app.py</files>
  <action>Add a Celery task included by the worker app that accepts a job ID, uses core job services to append `running` and terminal events, records local simulation model-run/cost data where appropriate, and marks the job succeeded or failed with sanitized error details. Keep it clearly named as local simulation.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_job_tasks.py</automated>
    <automated>cd services/worker && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Task does not import `caragent_api`, FastAPI, OpenAI, fal, BFL, or other provider SDKs.
    - Task records durable events for status changes.
    - Task failure path records a durable failed state and event.
  </acceptance_criteria>
  <done>Worker can update durable job state.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve worker boundary tests</name>
  <files>services/worker/tests/test_worker_app.py</files>
  <action>Update worker source boundary tests to allow `caragent_core` imports while continuing to forbid `caragent_api`, FastAPI, and provider SDK imports. Add `tasks.jobs` to app include assertions if helpful.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_worker_app.py tests/test_job_tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - Boundary test fails if worker imports API routers.
    - Boundary test fails if provider SDK imports appear in worker source.
  </acceptance_criteria>
  <done>Worker architecture guard remains intact.</done>
</task>

</tasks>

<verification>
Run worker pytest, worker ruff, worker mypy, and root tests after this plan.
</verification>

<success_criteria>
- Durable worker simulation updates job state/events.
- Worker boundary remains clean.
- No real provider calls are added.
</success_criteria>

<output>
After completion, create `.planning/phases/02-durable-data-jobs-and-assets/02-07-SUMMARY.md`.
</output>

---
phase: 07-operations-and-provider-strategy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "services/core/src/caragent_core/enums.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/tests/test_jobs.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/tests/test_jobs.py"
  - "services/api/tests/test_openapi_export.py"
autonomous: true
requirements: ["OPS-01", "OPS-02", "OPS-04"]
must_haves:
  truths:
    - "Failure categories and operational metadata are typed in core, not invented separately by API/web."
    - "Job and event response schemas expose safe metadata needed by later operations UI."
    - "No database migration is introduced; existing metadata JSON columns carry v1 operational fields."
  artifacts:
    - "services/core/src/caragent_core/enums.py"
    - "services/core/src/caragent_core/services/jobs.py"
    - "services/api/src/caragent_api/schemas.py"
  key_links:
    - "GenerationJob.metadata_json -> GenerationJobResponse.metadata"
    - "JobEvent.metadata_json -> JobEventResponse.metadata"
    - "transition_job_status(metadata=...) -> structured terminal events"
---

<objective>
Create the core operational metadata contract for Phase 7: failure categories, safe job/event metadata helpers, transition metadata support, and API response visibility.

Purpose: Later Phase 7 plans need one typed vocabulary for operations data instead of ad-hoc strings.
Output: Core/API contract changes and focused tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-operations-and-provider-strategy/07-CONTEXT.md
@.planning/phases/07-operations-and-provider-strategy/07-RESEARCH.md
@.planning/phases/07-operations-and-provider-strategy/07-VALIDATION.md
@services/core/src/caragent_core/enums.py
@services/core/src/caragent_core/services/jobs.py
@services/core/tests/test_jobs.py
@services/api/src/caragent_api/schemas.py
@services/api/tests/test_jobs.py
@services/api/tests/test_openapi_export.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify operational metadata and failure categories</name>
  <files>services/core/tests/test_jobs.py</files>
  <behavior>
    - `FailureCategory` supports `provider`, `provider_configuration`, `validation_rights`, `storage`, `queue_worker`, `canceled`, `timeout`, and `unknown`.
    - `transition_job_status(..., metadata={...})` stores metadata on the emitted job event.
    - Failure transition metadata is also merged into `GenerationJob.metadata_json["operations"]` without removing existing keys.
    - Error text in metadata is sanitized consistently with `latest_error`.
  </behavior>
  <action>Add failing tests to `services/core/tests/test_jobs.py` that create a job, transition it to failed with `failure_category="provider"` and `provider="bfl"`, then assert the latest event metadata and job metadata contain safe operational fields. Add a second test for canceled metadata.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py -k "operational_metadata or failure_category"</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because failure categories and transition metadata are absent.
    - Tests assert OPS-02 and OPS-04 behavior directly.
  </acceptance_criteria>
  <done>Core tests describe the Phase 7 metadata vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement core operations metadata helpers</name>
  <files>services/core/src/caragent_core/enums.py, services/core/src/caragent_core/services/jobs.py</files>
  <action>Add `FailureCategory` to `enums.py`. Extend `transition_job_status()` with optional `metadata: JsonObject | None = None` and optional `secrets: Sequence[str] = ()`. Sanitize string metadata values that represent errors/messages before persistence. Merge terminal operational metadata into `GenerationJob.metadata_json["operations"]` while preserving existing metadata. Pass the same metadata into `append_event()` so structured events and job summaries agree.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
    <automated>cd services/core && uv run ruff check .</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Existing job creation and transition tests still pass.
    - Job metadata merge preserves unrelated metadata such as iteration fields.
    - Failure metadata does not contain newlines or raw secret strings supplied through `secrets`.
  </acceptance_criteria>
  <done>Core can persist typed operational metadata without migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify API metadata response contract</name>
  <files>services/api/tests/test_jobs.py, services/api/tests/test_openapi_export.py</files>
  <behavior>
    - `GenerationJobResponse` includes a `metadata` object sourced from `GenerationJob.metadata_json`.
    - `JobEventResponse` includes a `metadata` object sourced from `JobEvent.metadata_json`.
    - OpenAPI includes the new metadata properties so generated TypeScript contracts can consume them.
  </behavior>
  <action>Add failing API tests that create a job/event with metadata and verify `/jobs/{job_id}` plus `/jobs/{job_id}/events` return `metadata`. Extend the OpenAPI export test to assert `GenerationJobResponse.properties.metadata` and `JobEventResponse.properties.metadata` exist.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_openapi_export.py -k "metadata or GenerationJobResponse or JobEventResponse"</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before schema changes because metadata is omitted.
    - Tests do not require database schema changes.
  </acceptance_criteria>
  <done>API contract tests cover operational metadata exposure.</done>
</task>

<task type="auto">
  <name>Task 4: Implement API response metadata fields</name>
  <files>services/api/src/caragent_api/schemas.py</files>
  <action>Add safe `metadata: dict[str, Any]` fields to `GenerationJobResponse` and `JobEventResponse`, mapping from `metadata_json` through Pydantic validation aliases or explicit response construction. Keep serialized field name as `metadata`. Do not expose SQLAlchemy model metadata internals. Ensure API list/get routes continue to serialize existing jobs/events.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py tests/test_openapi_export.py</automated>
    <automated>cd services/api && uv run ruff check .</automated>
    <automated>cd services/api && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Existing API route tests still pass.
    - Metadata defaults to `{}` for older records.
    - OpenAPI describes metadata as an object.
  </acceptance_criteria>
  <done>API exposes operational metadata needed by later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker/API to durable metadata | Runtime errors and provider details become persisted job/event metadata |
| API to web/contracts | Metadata becomes user/operator-visible JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | job/event metadata | mitigate | Sanitize error strings and preserve existing provider secret redaction. |
| T-07-02 | Tampering | metadata merge | mitigate | Validate failure category against `FailureCategory`; never replace whole job metadata during status transitions. |
| T-07-03 | Denial of Service | metadata payload size | mitigate | Keep metadata fields compact and truncate sanitized error strings using existing error limit behavior. |
</threat_model>

<verification>
Run focused core/API tests, ruff, mypy, and OpenAPI export checks listed above.
</verification>

<success_criteria>
- Failure categories are centralized in core.
- Status transitions can attach structured metadata.
- API responses expose safe job/event metadata without migration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operations-and-provider-strategy/07-01-SUMMARY.md`.
</output>

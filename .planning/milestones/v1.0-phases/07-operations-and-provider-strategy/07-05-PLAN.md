---
phase: 07-operations-and-provider-strategy
plan: "05"
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - "services/worker/src/caragent_worker/config.py"
  - "services/worker/src/caragent_worker/providers/__init__.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/tests/test_config.py"
  - "services/worker/tests/test_image_providers.py"
  - "services/worker/tests/test_generation_tasks.py"
autonomous: true
requirements: ["OPS-03", "OPS-02", "OPS-04"]
must_haves:
  truths:
    - "Provider routing, attempts, fallback, and timeouts are config-driven."
    - "Fallback is visible in model runs, job events, artifact metadata, and version parameters."
    - "Validation/configuration failures fail fast and are not silently retried."
  artifacts:
    - "services/worker/src/caragent_worker/config.py"
    - "services/worker/src/caragent_worker/providers/__init__.py"
    - "services/worker/src/caragent_worker/tasks/jobs.py"
  key_links:
    - "WorkerSettings -> provider attempt policy"
    - "ImageProviderError classification -> retry/fallback eligibility"
    - "model_runs per attempt -> operator visibility"
---

<objective>
Implement bounded provider retry and visible fallback behavior while keeping local deterministic generation as the default.

Purpose: Non-local provider failures should be controlled, visible, and recoverable without hiding original failures.
Output: Worker settings, provider routing helpers, attempt lifecycle, and tests.
</objective>

<execution_context>
@C:/Users/25858/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/25858/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-operations-and-provider-strategy/07-CONTEXT.md
@.planning/phases/07-operations-and-provider-strategy/07-RESEARCH.md
@.planning/phases/07-operations-and-provider-strategy/07-03-SUMMARY.md
@services/worker/src/caragent_worker/config.py
@services/worker/src/caragent_worker/providers/__init__.py
@services/worker/src/caragent_worker/tasks/jobs.py
@services/worker/tests/test_generation_tasks.py
@services/worker/tests/test_image_providers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify provider attempt settings and routing</name>
  <files>services/worker/tests/test_config.py, services/worker/tests/test_image_providers.py</files>
  <behavior>
    - Settings include `AI_GENERATION_MAX_ATTEMPTS`, `AI_PROVIDER_FALLBACK_ENABLED`, and `AI_PROVIDER_FALLBACK_NAME` with safe local defaults.
    - Unsupported hosted provider names fail fast when provider calls are enabled.
    - Disabled or local modes still select `LocalDeterministicImageProvider`.
    - BFL remains the only concrete hosted adapter in v1 unless another adapter is explicitly added.
  </behavior>
  <action>Add failing tests for default settings, hosted BFL routing, unsupported provider fail-fast behavior, and fallback target validation. Assert local default remains deterministic.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_config.py tests/test_image_providers.py -k "provider or fallback or attempts"</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before settings/routing changes.
    - Tests do not require network or real BFL credentials.
  </acceptance_criteria>
  <done>Provider attempt policy is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement provider settings and routing helper</name>
  <files>services/worker/src/caragent_worker/config.py, services/worker/src/caragent_worker/providers/__init__.py</files>
  <action>Add bounded attempt/fallback settings to `WorkerSettings`. Refactor provider selection so it can select a named provider (`local-deterministic`, `bfl`) and can reject unsupported hosted providers when calls are enabled. Keep secret checks inside hosted provider construction. Ensure local fallback uses configured local image size.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_config.py tests/test_image_providers.py</automated>
    <automated>cd services/worker && uv run ruff check .</automated>
    <automated>cd services/worker && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Local deterministic provider remains default.
    - Unsupported hosted provider is classified as configuration error during execution.
    - Existing BFL adapter tests still pass.
  </acceptance_criteria>
  <done>Worker has config-driven provider routing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify retry and visible fallback lifecycle</name>
  <files>services/worker/tests/test_generation_tasks.py</files>
  <behavior>
    - Transient provider/timeout failures retry up to configured max attempts.
    - Provider configuration, rights/validation, and canceled failures do not retry.
    - If hosted provider fails and fallback is enabled, worker records the failed hosted attempt and then creates a local deterministic fallback attempt.
    - Successful fallback sets metadata `fallback_from_provider`, `fallback_reason`, `provider_attempt_count`, and `external_calls=false` on artifact/version/job operations data.
  </behavior>
  <action>Add failing worker tests with a provider factory or scripted provider sequence: fail then succeed, fail then fallback, config failure no retry, rights failure no retry. Assert model-run count/status, event metadata, and final provider in returned result.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py -k "retry or fallback or attempts"</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before worker attempt orchestration.
    - Fallback tests prove original provider failure remains visible.
  </acceptance_criteria>
  <done>Retry/fallback behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 4: Implement bounded retry and fallback orchestration</name>
  <files>services/worker/src/caragent_worker/tasks/jobs.py</files>
  <action>Refactor provider execution into an attempt loop. Create one model run per attempt, with attempt index and provider route metadata. Retry only categories considered transient (`provider`, `timeout`) until `AI_GENERATION_MAX_ATTEMPTS` is reached. If fallback is enabled and primary hosted provider failed for an eligible reason, run one local deterministic fallback attempt and record explicit fallback metadata. Do not retry or fallback on validation/rights/configuration/canceled failures.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py tests/test_image_providers.py</automated>
    <automated>cd services/worker && uv run ruff check .</automated>
    <automated>cd services/worker && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Successful local generation still creates one successful model run.
    - Hosted failure with fallback creates at least one failed hosted model run and one successful local model run.
    - Latest job/artifact/version metadata identifies fallback instead of hiding it.
  </acceptance_criteria>
  <done>Provider retry/fallback is controlled and visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker settings to provider routing | Environment controls which provider gets called |
| Hosted provider to local fallback | External failure may trigger local deterministic generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering | provider routing | mitigate | Validate provider names and fallback names against supported provider set. |
| T-07-13 | Repudiation | fallback | mitigate | Persist failed attempt and fallback metadata in model runs and job events. |
| T-07-14 | Denial of Service | retry loop | mitigate | Bound attempts with config and fail fast for non-retryable categories. |
</threat_model>

<verification>
Run focused worker provider/config/generation tests, ruff, and mypy.
</verification>

<success_criteria>
- Provider routing is config-driven.
- Retries are bounded and category-aware.
- Fallback is visible, not silent.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operations-and-provider-strategy/07-05-SUMMARY.md`.
</output>

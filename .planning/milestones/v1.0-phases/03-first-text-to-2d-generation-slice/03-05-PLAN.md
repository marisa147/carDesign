---
phase: 03-first-text-to-2d-generation-slice
plan: "05"
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-04", "02-06"]
files_modified:
  - "services/api/pyproject.toml"
  - "services/api/src/caragent_api/queue.py"
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/generation.py"
  - "services/api/src/caragent_api/main.py"
  - "services/api/tests/test_generation.py"
  - "services/api/tests/test_openapi_export.py"
autonomous: true
requirements: ["GEN-01", "GEN-02", "GEN-03", "GEN-05", "GEN-07"]
must_haves:
  truths:
    - "API routes create/reuse structured briefs and generation jobs without running provider work synchronously."
    - "Generation submission enqueues the worker task by name through a queue boundary."
    - "Retry creates a new durable attempt from the original brief without overwriting failed records."
---

<objective>
Expose Phase 3 API routes for structured brief review/reuse, generation submission, and failed-job retry while preserving asynchronous worker ownership.
</objective>

<context>
@.planning/phases/03-first-text-to-2d-generation-slice/03-CONTEXT.md
@services/api/src/caragent_api/schemas.py
@services/api/src/caragent_api/routes/workspaces.py
@services/api/src/caragent_api/routes/jobs.py
@services/api/src/caragent_api/main.py
@services/core/src/caragent_core/services/workspaces.py
@services/core/src/caragent_core/services/jobs.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05A | Denial of Service | API generation route | mitigate | API enqueues work and returns pollable job state instead of blocking on provider calls. |
| T-03-05B | Tampering | Retry path | mitigate | Retry creates a new job tied to the original brief and preserves failed attempt records. |
| T-03-05C | Information Disclosure | Queue payload | mitigate | Queue only job IDs and safe metadata, never API keys or prompt secrets. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add failing API generation tests</name>
  <files>services/api/tests/test_generation.py</files>
  <action>Write tests for creating a structured generation brief from natural language, editing/reusing the brief payload, submitting a `generate_2d_concept` job with idempotency, enqueueing the worker task through a fake queue dependency, rejecting missing brief/request data, and retrying a failed job into a new job.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests initially fail until routes/schemas exist.
    - Tests assert API never calls provider adapters directly.
    - Tests assert retry does not overwrite the failed job.
  </acceptance_criteria>
  <done>API behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Add queue boundary</name>
  <files>services/api/pyproject.toml, services/api/src/caragent_api/queue.py</files>
  <action>Add a small queue client abstraction that can send a Celery task by name using Redis/Celery configuration without importing worker modules. Provide a fake queue implementation for tests and no-op/error behavior when queueing is disabled in local test setup.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py</automated>
  </verify>
  <acceptance_criteria>
    - API package does not import `caragent_worker`.
    - Queue payload includes job ID and no secrets.
    - Tests can inject a fake queue.
  </acceptance_criteria>
  <done>API can enqueue worker jobs safely.</done>
</task>

<task type="auto">
  <name>Task 3: Add generation schemas and routes</name>
  <files>services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/routes/generation.py, services/api/src/caragent_api/main.py</files>
  <action>Add Pydantic request/response schemas for natural-language brief creation/update, generation submission, and retry. Add routes under workspace/generation or similarly narrow names that create design briefs, create idempotent generation jobs, enqueue the worker task, and return job/brief payloads.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_generation.py</automated>
    <automated>cd services/api && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Routes return typed structured brief payloads, not raw arbitrary JSON only.
    - Generation job operation is `generate_2d_concept`.
    - Idempotency reuse remains visible in the response.
  </acceptance_criteria>
  <done>Phase 3 API routes exist.</done>
</task>

<task type="auto">
  <name>Task 4: Update OpenAPI route coverage tests</name>
  <files>services/api/tests/test_openapi_export.py</files>
  <action>Add expected Phase 3 paths and schema checks to OpenAPI export tests. Verify generated schemas expose structured brief fields, generation submission, retry, and job references.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_openapi_export.py tests/test_generation.py</automated>
  </verify>
  <acceptance_criteria>
    - OpenAPI includes Phase 3 generation routes.
    - Existing Phase 1/2 paths remain present.
  </acceptance_criteria>
  <done>API contract source reflects generation routes.</done>
</task>

</tasks>

<verification>
Run API generation tests, OpenAPI export tests, API Ruff, API mypy, and existing workspace/job/asset API tests.
</verification>

<success_criteria>
- Users can create/reuse structured briefs and submit/retry generation jobs through the API.
- API remains asynchronous and queue-backed.
- Contracts are ready to regenerate.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-text-to-2d-generation-slice/03-05-SUMMARY.md`.
</output>

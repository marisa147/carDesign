---
phase: 03-first-text-to-2d-generation-slice
plan: "04"
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "02-05", "02-07"]
files_modified:
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/tests/test_generation_jobs.py"
  - "services/worker/src/caragent_worker/tasks/jobs.py"
  - "services/worker/tests/test_generation_tasks.py"
  - "services/worker/tests/test_worker_app.py"
autonomous: true
requirements: ["GEN-04", "GEN-05", "GEN-06", "GEN-07"]
must_haves:
  truths:
    - "Worker generation creates durable model-run, artifact, design-version, and job-event records."
    - "A succeeded generation job cannot lack an output artifact and generated design version."
    - "Failure paths record sanitized errors and leave the original brief intact."
---

<objective>
Wire prompt planning and providers into a worker-owned asynchronous generation task that writes the Phase 2 ledger records required by Phase 3.
</objective>

<context>
@.planning/phases/03-first-text-to-2d-generation-slice/03-CONTEXT.md
@.planning/phases/03-first-text-to-2d-generation-slice/03-VALIDATION.md
@services/core/src/caragent_core/services/jobs.py
@services/core/src/caragent_core/services/assets.py
@services/core/src/caragent_core/storage.py
@services/worker/src/caragent_worker/tasks/jobs.py
@services/worker/src/caragent_worker/providers/base.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04A | Tampering | Job completion | mitigate | Mark succeeded only after artifact, model-run output, and design version are flushed. |
| T-03-04B | Information Disclosure | Worker errors | mitigate | Sanitize provider/config errors before storing latest_error and events. |
| T-03-04C | Repudiation | Generation trace | mitigate | Persist prompt payload, provider/model, input assets, output artifact, and events. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add failing generation ledger tests</name>
  <files>services/core/tests/test_generation_jobs.py, services/worker/tests/test_generation_tasks.py</files>
  <action>Write tests for a queued generation job linked to a brief that the worker moves to running, creates a model run with prompt trace, stores generated image artifact metadata, creates a generated design version, updates job costs, and marks the job succeeded. Add forced provider failure tests that record failed model run/job state and sanitized errors.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
    <automated>cd services/core && uv run pytest -q tests/test_generation_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests initially fail before the new worker pipeline exists.
    - Tests assert generated artifact content type and dimensions.
    - Tests assert prompt_text and prompt_payload are present in model run.
  </acceptance_criteria>
  <done>Generation ledger behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 2: Extend core job helpers for model-run completion</name>
  <files>services/core/src/caragent_core/services/jobs.py, services/core/tests/test_generation_jobs.py</files>
  <action>Add small service helpers to update model-run status, output artifact, timestamps, and sanitized error messages without exposing direct ORM mutations to worker code. Preserve existing Phase 2 tests and APIs.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py tests/test_generation_jobs.py</automated>
    <automated>cd services/core && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Existing `create_model_run` behavior remains compatible.
    - Model-run failure stores sanitized error text.
    - Helpers do not import worker, API, or provider modules.
  </acceptance_criteria>
  <done>Core can finalize model-run records safely.</done>
</task>

<task type="auto">
  <name>Task 3: Implement generation worker helper and Celery task</name>
  <files>services/worker/src/caragent_worker/tasks/jobs.py</files>
  <action>Add `generate_2d_concept_job` Celery task and a pure async helper that loads the job and brief, validates reference asset rights, builds the prompt plan, selects provider, executes generation, stores artifact bytes through object storage, creates design version, updates model run and job events, and handles failure state.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_generation_tasks.py</automated>
    <automated>cd services/worker && uv run mypy src</automated>
  </verify>
  <acceptance_criteria>
    - Worker still does not import FastAPI routes.
    - Local deterministic path requires no external calls.
    - Missing-rights assets fail before provider execution.
    - Succeeded job has model run, output artifact, and generated design version.
  </acceptance_criteria>
  <done>Asynchronous generation task is implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Preserve worker boundary tests</name>
  <files>services/worker/tests/test_worker_app.py</files>
  <action>Update worker boundary tests to allow new provider modules and dependencies while continuing to forbid API route imports and accidental browser/frontend imports. Add assertions that the generation task is included in the Celery app.</action>
  <verify>
    <automated>cd services/worker && uv run pytest -q tests/test_worker_app.py tests/test_generation_tasks.py tests/test_image_providers.py</automated>
  </verify>
  <acceptance_criteria>
    - Boundary tests fail if `caragent_api.routes` appears in worker source.
    - Boundary tests fail if provider secrets are logged in source fixtures.
  </acceptance_criteria>
  <done>Worker architecture guard remains intact.</done>
</task>

</tasks>

<verification>
Run worker generation/provider tests, worker Ruff/mypy, core job/generation tests, and existing Phase 2 worker tests.
</verification>

<success_criteria>
- Worker can complete a local generation job end to end.
- Durable trace records are complete.
- Failure state and rights gate are test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-text-to-2d-generation-slice/03-04-SUMMARY.md`.
</output>

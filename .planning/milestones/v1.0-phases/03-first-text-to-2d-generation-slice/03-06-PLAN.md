---
phase: 03-first-text-to-2d-generation-slice
plan: "06"
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
  - "packages/contracts/src/generated/model/*.ts"
  - "apps/web/src/lib/api/generation.ts"
  - "apps/web/src/lib/api/generation.test.ts"
  - "apps/web/src/app/page.tsx"
  - "apps/web/src/app/page.test.tsx"
autonomous: true
requirements: ["GEN-01", "GEN-02", "GEN-05", "GEN-06", "GEN-07"]
must_haves:
  truths:
    - "Generated TypeScript contracts are current after Phase 3 API changes."
    - "Frontend code uses generated clients through small wrappers."
    - "Any web proof stays minimal and does not become the full Phase 4 workbench."
---

<objective>
Refresh contracts and add a minimal frontend proof that can submit a brief/generation request and display durable job/result state.
</objective>

<context>
@.planning/phases/03-first-text-to-2d-generation-slice/03-CONTEXT.md
@packages/contracts/orval.config.ts
@apps/web/src/lib/api/jobs.ts
@apps/web/src/lib/api/workspaces.ts
@apps/web/src/app/page.tsx
@apps/web/src/app/page.test.tsx
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06A | Spoofing | Browser provider access | mitigate | Browser never receives provider keys and only calls API routes. |
| T-03-06B | Misrepresentation | Web proof | mitigate | Label Phase 3 output as concept generation proof, not full workbench or production export. |
| T-03-06C | Tampering | Contract drift | mitigate | Run contract generation/check and TypeScript tests. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate and check contracts</name>
  <files>packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts, packages/contracts/src/generated/model/*.ts</files>
  <action>Run OpenAPI export and Orval generation through the existing contract scripts. Inspect generated names and update imports only through wrapper modules, not direct page-level ad hoc fetch calls.</action>
  <verify>
    <automated>corepack pnpm contracts:generate</automated>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Generated artifacts include Phase 3 generation routes/schemas.
    - Contract checker reports current artifacts.
  </acceptance_criteria>
  <done>TypeScript contracts are current.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend generation API wrapper tests</name>
  <files>apps/web/src/lib/api/generation.test.ts</files>
  <action>Write tests for wrapper functions that create/update structured briefs, submit generation, retry failed generation, and fetch related job/artifact/version state through generated client functions. Mock generated client calls consistently with existing `jobs.test.ts` and `workspaces.test.ts` patterns.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- generation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before wrapper implementation.
    - Wrapper exposes only API-safe fields; no provider secrets appear in browser code.
  </acceptance_criteria>
  <done>Frontend API wrapper behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement frontend generation wrappers</name>
  <files>apps/web/src/lib/api/generation.ts</files>
  <action>Add small generated-client wrapper functions for Phase 3 brief creation, generation submission, retry, and result/status retrieval. Keep error handling consistent with existing web API wrappers.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- generation.test.ts</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Wrappers import types from `@caragent/contracts`.
    - No direct `fetch` duplication is introduced when generated client functions exist.
  </acceptance_criteria>
  <done>Web can call Phase 3 API through wrappers.</done>
</task>

<task type="auto">
  <name>Task 4: Add minimal Phase 3 web proof</name>
  <files>apps/web/src/app/page.tsx, apps/web/src/app/page.test.tsx</files>
  <action>Extend the existing foundation/Phase 2 shell with a compact Phase 3 proof: natural-language brief textarea or fixed sample button, create structured brief, submit local generation, refresh status/results, and show latest job/artifact/version counts. Keep disabled future workbench regions intact.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- page.test.tsx generation.test.ts</automated>
    <automated>corepack pnpm --filter @caragent/web lint</automated>
  </verify>
  <acceptance_criteria>
    - Page text does not claim full chat, rich preview, export, or production-ready output.
    - Result/status survives refresh through API-backed state.
    - Layout remains responsive without wrapped badge regressions.
  </acceptance_criteria>
  <done>Minimal browser-visible Phase 3 proof exists.</done>
</task>

</tasks>

<verification>
Run web tests, lint, typecheck, contracts check, and inspect the page with Browser during verification after execution.
</verification>

<success_criteria>
- Contracts are current.
- Web has a minimal API-backed Phase 3 proof.
- Full Phase 4 workbench remains deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-text-to-2d-generation-slice/03-06-SUMMARY.md`.
</output>

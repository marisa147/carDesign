---
phase: 01-foundation-and-contracts
plan: "07"
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - "apps/web/package.json"
  - "apps/web/tsconfig.json"
  - "apps/web/postcss.config.mjs"
  - "apps/web/eslint.config.mjs"
  - "apps/web/vitest.config.ts"
  - "apps/web/components.json"
  - "apps/web/src/app/globals.css"
  - "apps/web/src/app/layout.tsx"
  - "apps/web/src/lib/utils.ts"
autonomous: true
requirements: ["FOUND-01", "FOUND-02"]
must_haves:
  truths:
    - "Developer can run a Next.js TypeScript app shell from `apps/web`."
    - "The web foundation uses Tailwind and official shadcn/ui setup inside the approved Phase 1 UI contract."
    - "Frontend lint, type-check, and test infrastructure exists before shell integration."
  artifacts:
    - path: "apps/web/package.json"
      provides: "Web package scripts and dependencies"
      contains: "\"name\": \"@caragent/web\""
    - path: "apps/web/src/app/globals.css"
      provides: "Tailwind/shadcn tokens"
      contains: "--radius"
    - path: "apps/web/vitest.config.ts"
      provides: "Frontend test configuration"
      contains: "vitest"
    - path: "apps/web/components.json"
      provides: "Official shadcn registry configuration"
      contains: "neutral"
  key_links:
    - from: "package.json"
      to: "apps/web/package.json"
      via: "`pnpm --filter @caragent/web` root commands"
      pattern: "@caragent/web"
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/app/globals.css"
      via: "global stylesheet import"
      pattern: "./globals.css"
---

<objective>
Create the Next.js scaffold and design-system baseline.

Purpose: Establish the frontend package, strict TypeScript/test tooling, Tailwind/shadcn setup, and core approved UI components before implementing the shell.
Output: `@caragent/web` package, strict TypeScript/test config, Tailwind globals, and official shadcn registry configuration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-UI-SPEC.md
@.planning/phases/01-foundation-and-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-contracts/01-PATTERNS.md
@package.json

<interfaces>
Root scripts from plan `01-01` call:

```text
pnpm --filter @caragent/web dev
pnpm --filter @caragent/web lint
pnpm --filter @caragent/web typecheck
pnpm --filter @caragent/web test -- --run
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser bundle -> public env | Frontend code may only access `NEXT_PUBLIC_*` values. |
| shadcn registry -> local source | Component code enters the repo from the official registry. |
| root scripts -> web package | Root validation delegates into `apps/web`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02G | Information Disclosure / Elevation | frontend package | mitigate | Do not add backend-only env names, provider keys, or backend imports in scaffold code. |
| T-01-05G | Tampering | shadcn components | mitigate | Add only official registry components allowed by `01-UI-SPEC.md`; no third-party blocks or remote UI kits. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold web package and strict tooling</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/postcss.config.mjs, apps/web/eslint.config.mjs, apps/web/vitest.config.ts</files>
  <read_first>Read `01-UI-SPEC.md` Design System and `01-RESEARCH.md` Frontend Scaffold before editing.</read_first>
  <action>Create `apps/web` as a Next.js App Router TypeScript project named `@caragent/web` per D-20. Add scripts `dev`, `build`, `lint`, `typecheck`, and `test`. Before creating lockfiles, run official npm registry checks for Next.js, React, Tailwind, shadcn dependencies, Vitest, Testing Library, TanStack Query, and TypeScript, then record chosen pins in the plan summary. Add Tailwind CSS, `lucide-react`, `@tanstack/react-query`, Vitest, Testing Library, and TypeScript/ESLint dependencies needed by Phase 1. Configure strict TypeScript, path alias `@/*`, ESLint for Next/TypeScript, and Vitest with jsdom. Do not add product workflow dependencies for chat, uploads, canvas preview, 3D, generation, export, auth, marketplace, or provider SDKs.</action>
  <verify>
    <automated>node -e "const p=require('./apps/web/package.json'); if (p.name !== '@caragent/web') throw new Error('wrong web package name'); for (const key of ['dev','build','lint','typecheck','test']) if (!p.scripts?.[key]) throw new Error('missing web script '+key);"</automated>
    <automated>pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <done>Web package and frontend validation tooling exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tailwind globals, layout, and utilities</name>
  <files>apps/web/components.json, apps/web/src/app/globals.css, apps/web/src/app/layout.tsx, apps/web/src/lib/utils.ts</files>
  <read_first>Read `01-UI-SPEC.md` Color, Typography, Spacing Scale, Tailwind Baseline, and Accessibility Contract before editing.</read_first>
  <action>Initialize official shadcn/ui config through `components.json` with CSS variables, neutral base color, system sans font, and radius capped at `0.5rem`. Create `globals.css` with UI-SPEC color tokens, `--radius`, body background `#F8FAFC`, text color `#111827`, and no gradients or decorative imagery. Create root layout with `lang="zh-CN"`, metadata for `痛车设计 Agent`, and global stylesheet import. Create `cn` utility in `src/lib/utils.ts` using the standard `clsx`/`tailwind-merge` pattern.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const css=fs.readFileSync('apps/web/src/app/globals.css','utf8'); for (const token of ['--radius','#F8FAFC','#111827','#6D5DF6']) if (!css.includes(token)) throw new Error('missing UI token '+token); if (/gradient|radial|conic/i.test(css)) throw new Error('gradients are not allowed');"</automated>
  </verify>
  <done>Global visual baseline matches the Phase 1 UI contract.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter @caragent/web lint` and `pnpm --filter @caragent/web typecheck`.
</verification>

<success_criteria>
- `apps/web` is a runnable Next.js package with lint/type/test commands.
- Tailwind/shadcn configuration follows the approved Phase 1 UI contract.
- Scaffold task file counts stay below checker thresholds and do not include shell behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-07-SUMMARY.md`.
</output>

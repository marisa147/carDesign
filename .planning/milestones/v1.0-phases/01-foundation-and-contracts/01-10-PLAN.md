---
phase: 01-foundation-and-contracts
plan: "10"
type: execute
wave: 7
depends_on: ["01-09"]
gap_closure: true
files_modified:
  - "package.json"
  - "packages/contracts/package.json"
  - "docs/development.md"
autonomous: true
requirements: ["FOUND-02"]
must_haves:
  truths:
    - "Developer can run root `pnpm test` without invoking a missing package script."
    - "`@caragent/contracts` exposes an independently runnable test command."
    - "Developer docs describe the root and contracts test commands accurately."
  artifacts:
    - path: "packages/contracts/package.json"
      provides: "Contracts package test script"
      contains: "\"test\""
    - path: "package.json"
      provides: "Root test command that remains structurally valid"
      contains: "pnpm --filter @caragent/contracts test"
    - path: "docs/development.md"
      provides: "Developer command documentation for tests"
      contains: "pnpm test"
  key_links:
    - from: "package.json"
      to: "packages/contracts/package.json"
      via: "root `pnpm test` contracts leg"
      pattern: "@caragent/contracts test"
    - from: "packages/contracts/package.json"
      to: "packages/contracts/tsconfig.json"
      via: "contracts test script"
      pattern: "tsc --project tsconfig.json --noEmit"
---

<objective>
Close FOUND-02 validation gap FOUND-02 / WR-04.

Purpose: Make the documented root test command structurally runnable by ensuring every delegated package test script exists.
Output: Contracts package test script, deterministic root test wiring, and aligned development docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-contracts/01-VERIFICATION.md
@.planning/phases/01-foundation-and-contracts/01-REVIEW.md
@.planning/phases/01-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-contracts/01-VALIDATION.md
@.planning/phases/01-foundation-and-contracts/01-09-SUMMARY.md
@package.json
@packages/contracts/package.json
@docs/development.md

<interfaces>
Current command relationship:

```json
// package.json
"test": "pnpm --filter @caragent/web test && pnpm --filter @caragent/contracts test && cd services/api && uv run pytest -q && cd ../worker && uv run pytest -q"
```

```json
// packages/contracts/package.json currently has no `test` script.
"typecheck": "tsc --project tsconfig.json --noEmit",
"lint": "tsc --project tsconfig.json --noEmit"
```
</interfaces>
</context>

<source_audit>
## Gap Source Coverage

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Developer/operator can validate frontend/backend/worker stack | Plan 01-10 root test repair |
| REQ | FOUND-02 baseline lint, type-check, and test commands | Plan 01-10 |
| RESEARCH | Root commands should delegate to independently runnable package/service commands per D-06/D-17 | Plan 01-10 |
| CONTEXT | D-06 root convenience commands; D-17 baseline validation commands; D-18 focused foundation tests | Plan 01-10 |
| VERIFICATION | Gap: root `pnpm test` calls missing `@caragent/contracts test` | Plan 01-10 |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell -> package scripts | Root `pnpm test` delegates execution into web, contracts, API, and worker package/service commands. |
| docs -> developer behavior | Development docs must not advertise commands that are structurally broken. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10-01 | Tampering / Repudiation | `package.json` root test command | mitigate | Keep the contracts test leg backed by an actual `packages/contracts` script and verify script presence with a static Node check. |
| T-01-10-02 | Denial of Service | `@caragent/contracts` test command | mitigate | Use the existing TypeScript no-emit check so the contracts test command terminates deterministically. |
| T-01-10-03 | Repudiation | `docs/development.md` command docs | mitigate | Update docs to show the exact root and contracts test commands so failures are attributable to code/tests, not missing script wiring. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add contracts package test script and align root test wiring</name>
  <files>package.json, packages/contracts/package.json</files>
  <read_first>
    - `package.json`
    - `packages/contracts/package.json`
    - `.planning/phases/01-foundation-and-contracts/01-VERIFICATION.md` gap for FOUND-02
  </read_first>
  <action>
    Add a `test` script to `packages/contracts/package.json` that runs `tsc --project tsconfig.json --noEmit`. Keep the root `package.json` test command delegating to `pnpm --filter @caragent/contracts test` per D-06 and D-17. If the root test command is edited, keep the same subsystem order and use the existing web non-watch test behavior (`@caragent/web` already defines `vitest --run`). Do not remove the contracts test leg; the goal is to make the delegated command valid.
  </action>
  <acceptance_criteria>
    - `packages/contracts/package.json` contains a `test` script.
    - Root `pnpm test` still includes web, contracts, API, and worker tests.
    - No new package dependency is added for this gap.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const root=require('./package.json'); const contracts=require('./packages/contracts/package.json'); if (!contracts.scripts.test) throw new Error('contracts test script missing'); if (!root.scripts.test.includes('pnpm --filter @caragent/contracts test')) throw new Error('root test must call contracts test'); if (!contracts.scripts.test.includes('tsc --project tsconfig.json --noEmit')) throw new Error('contracts test must run tsc noEmit');"</automated>
    <automated>pnpm --filter @caragent/contracts test</automated>
    <automated>pnpm test</automated>
  </verify>
  <done>Root `pnpm test` no longer fails because `@caragent/contracts test` is missing.</done>
</task>

<task type="auto">
  <name>Task 2: Document test command surface truthfully</name>
  <files>docs/development.md</files>
  <read_first>
    - `docs/development.md`
    - `package.json`
    - `packages/contracts/package.json`
  </read_first>
  <action>
    Update the testing/validation sections of `docs/development.md` so `pnpm test`, `pnpm --filter @caragent/contracts test`, and `pnpm --filter @caragent/contracts typecheck` are described consistently. State that `pnpm validate` remains the aggregate gate, while `pnpm test` is the root unit-test surface for web, contracts, API, and worker. Preserve Phase 1 boundaries from D-16 and D-21; do not add provider calls, product flows, or UI feature behavior to the docs.
  </action>
  <acceptance_criteria>
    - Docs include `pnpm test`.
    - Docs include `pnpm --filter @caragent/contracts test`.
    - Docs still identify `pnpm validate` as the aggregate validation command.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const doc=fs.readFileSync('docs/development.md','utf8'); for (const token of ['pnpm test','pnpm --filter @caragent/contracts test','pnpm --filter @caragent/contracts typecheck','pnpm validate']) if (!doc.includes(token)) throw new Error('docs missing '+token);"</automated>
  </verify>
  <done>Developer docs no longer advertise a structurally broken root test command.</done>
</task>

</tasks>

<verification>
Intended host verification:

```powershell
pnpm --filter @caragent/contracts test
pnpm test
pnpm validate
```

Current sandbox deterministic alternative:

```powershell
node -e "const root=require('./package.json'); const contracts=require('./packages/contracts/package.json'); if (!contracts.scripts.test) throw new Error('contracts test script missing'); if (!root.scripts.test.includes('pnpm --filter @caragent/contracts test')) throw new Error('root test command mismatch');"
```
</verification>

<success_criteria>
- FOUND-02 gap FOUND-02 / WR-04 is closed.
- Root `pnpm test` has no missing delegated script.
- Contracts package has a deterministic test script.
- Docs and scripts agree on the test command surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-contracts/01-10-SUMMARY.md`.
</output>

---
phase: 05-iteration-feedback-and-concept-export
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "services/api/src/caragent_api/schemas.py"
  - "services/api/src/caragent_api/routes/jobs.py"
  - "services/api/tests/test_jobs.py"
  - "services/core/src/caragent_core/services/jobs.py"
  - "services/core/tests/test_jobs.py"
autonomous: true
requirements: ["ITER-04", "ITER-05", "ITER-06"]
---

<objective>
Add backend create APIs for durable feedback and concept-preview export records.
</objective>

<context>
@.planning/phases/05-iteration-feedback-and-concept-export/05-CONTEXT.md
@.planning/phases/05-iteration-feedback-and-concept-export/05-RESEARCH.md
@services/core/src/caragent_core/services/jobs.py
@services/api/src/caragent_api/routes/jobs.py
@services/api/src/caragent_api/schemas.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01A | Tampering | feedback/export version ownership | mitigate | Validate workspace and version relationship before recording feedback/export. |
| T-05-01B | Repudiation | export manifest | mitigate | Persist explicit manifest fields and concept-preview disclaimer. |
| T-05-01C | Misrepresentation | export label | mitigate | Server-generated manifest must state output is not print-ready. |
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Add backend tests for feedback and export creation</name>
  <files>services/core/tests/test_jobs.py, services/api/tests/test_jobs.py</files>
  <action>Add failing tests for rating validation, workspace/version mismatch rejection, feedback creation route, export creation route, manifest default fields, unsupported export format rejection, and concept-preview disclaimer.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation for missing create routes/validation.
    - Tests assert export does not claim print-ready production output.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add request schemas and route handlers</name>
  <files>services/api/src/caragent_api/schemas.py, services/api/src/caragent_api/routes/jobs.py</files>
  <action>Add `FeedbackCreateRequest` and `ExportCreateRequest`. Add POST routes scoped as `/workspaces/{workspace_id}/versions/{version_id}/feedback` and `/workspaces/{workspace_id}/versions/{version_id}/exports`.</action>
  <verify>
    <automated>cd services/api && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Route responses use existing `FeedbackResponse` and `ExportResponse`.
    - Errors use existing 404/422 style.
    - Manifest includes selected format, version/artifact ids, and concept-preview disclaimer.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Harden core service validation</name>
  <files>services/core/src/caragent_core/services/jobs.py, services/core/tests/test_jobs.py</files>
  <action>Ensure feedback/export creation verifies the version belongs to the workspace and validates export formats to `png`, `jpg`, or `jpeg`.</action>
  <verify>
    <automated>cd services/core && uv run pytest -q tests/test_jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - Rating 1-5 remains enforced.
    - Workspace/version mismatches fail.
    - Export format is normalized and constrained.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run focused core/API tests for jobs plus `uv run ruff check .` in touched Python services.
</verification>

<success_criteria>
- Feedback create API is durable and workspace/version-scoped.
- Concept export create API records safe manifest metadata.
- No production-ready export claim is introduced.
</success_criteria>

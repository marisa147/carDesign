---
phase: 05-iteration-feedback-and-concept-export
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - "packages/contracts/openapi/openapi.json"
  - "packages/contracts/src/generated/client.ts"
  - "apps/web/src/lib/api/iteration.ts"
  - "apps/web/src/lib/api/iteration.test.ts"
  - "apps/web/src/lib/workbench/query-keys.ts"
  - "apps/web/src/lib/api/generation.ts"
  - "apps/web/src/lib/api/generation.test.ts"
autonomous: true
requirements: ["ITER-01", "ITER-04", "ITER-05"]
---

<objective>
Refresh OpenAPI contracts and add frontend API wrappers/query keys for iteration, feedback, and exports.
</objective>

<context>
@.planning/phases/05-iteration-feedback-and-concept-export/05-CONTEXT.md
@.planning/phases/05-iteration-feedback-and-concept-export/05-RESEARCH.md
@apps/web/src/lib/api/generation.ts
@apps/web/src/lib/workbench/query-keys.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03A | Contract drift | generated client | mitigate | Run `corepack pnpm contracts:generate` and `contracts:check`. |
| T-05-03B | Information disclosure | web wrappers | mitigate | Do not expose secrets or storage credentials in browser code. |
| T-05-03C | Data integrity | wrapper payloads | mitigate | Wrapper tests assert exact URLs/methods/bodies. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refresh generated contracts</name>
  <files>packages/contracts/openapi/openapi.json, packages/contracts/src/generated/client.ts</files>
  <action>Run the repo contract generation command after backend routes exist, then inspect generated route helper names for Phase 5 wrappers.</action>
  <verify>
    <automated>corepack pnpm contracts:check</automated>
  </verify>
  <acceptance_criteria>
    - Generated artifacts include feedback create, export create, and iteration submit routes.
    - No unrelated generated drift is introduced.
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Add frontend wrapper tests</name>
  <files>apps/web/src/lib/api/iteration.test.ts, apps/web/src/lib/api/generation.test.ts</files>
  <action>Add tests for submit child iteration, create feedback, create export, list feedback, list exports, and expanded generation state where needed.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- iteration.test.ts generation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert generated URLs, HTTP methods, JSON bodies, and error handling.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement wrappers and query keys</name>
  <files>apps/web/src/lib/api/iteration.ts, apps/web/src/lib/api/generation.ts, apps/web/src/lib/workbench/query-keys.ts</files>
  <action>Add a Phase 5 wrapper module and extend query keys for feedback/exports. Keep API wrapper signature consistent with existing `apiBaseUrl`, `fetch`, and `signal` pattern.</action>
  <verify>
    <automated>corepack pnpm --filter @caragent/web test -- iteration.test.ts generation.test.ts</automated>
    <automated>corepack pnpm --filter @caragent/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - No backend internals are imported into web code.
    - Browser code uses generated contracts only.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run contracts check, focused web API wrapper tests, and web typecheck.
</verification>

<success_criteria>
- Frontend has typed wrappers for all Phase 5 API surfaces.
- Contract drift is checked and committed in generated artifacts.
</success_criteria>

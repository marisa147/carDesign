# carAgent web shell local environment.
# Browser-exposed values must use NEXT_PUBLIC_* only.

RUNTIME_MODE=local
WEB_PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_RUNTIME_MODE=local
